// Code generated by ogen, DO NOT EDIT.

package assistantv1server

import (
	"net/http"
	"net/url"
	"strings"

	"github.com/ogen-go/ogen/uri"
)

var (
	rn40AllowedHeaders = map[string]string{
		"POST": "Authorization,Content-Type",
	}
	rn22AllowedHeaders = map[string]string{
		"GET": "Authorization",
	}
	rn26AllowedHeaders = map[string]string{
		"GET": "Authorization",
	}
	rn18AllowedHeaders = map[string]string{
		"DELETE": "Authorization",
		"GET":    "Authorization",
	}
	rn12AllowedHeaders = map[string]string{
		"GET":  "Authorization",
		"POST": "Authorization,Content-Type",
	}
	rn7AllowedHeaders = map[string]string{
		"DELETE": "Authorization",
		"GET":    "Authorization",
		"PATCH":  "Authorization,Content-Type",
	}
	rn11AllowedHeaders = map[string]string{
		"POST": "Authorization",
	}
	rn27AllowedHeaders = map[string]string{
		"GET": "Authorization",
	}
	rn37AllowedHeaders = map[string]string{
		"POST": "Authorization,Content-Type",
	}
	rn10AllowedHeaders = map[string]string{
		"GET": "Authorization",
	}
	rn8AllowedHeaders = map[string]string{
		"POST": "Authorization",
	}
	rn29AllowedHeaders = map[string]string{
		"GET": "Authorization",
	}
	rn20AllowedHeaders = map[string]string{
		"DELETE": "Authorization",
	}
	rn31AllowedHeaders = map[string]string{
		"GET": "Authorization",
	}
	rn32AllowedHeaders = map[string]string{
		"GET": "Authorization",
	}
	rn13AllowedHeaders = map[string]string{
		"GET":  "Authorization",
		"POST": "Authorization,Content-Type",
	}
	rn2AllowedHeaders = map[string]string{
		"GET":   "Authorization",
		"PATCH": "Authorization,Content-Type",
	}
	rn16AllowedHeaders = map[string]string{
		"POST": "Authorization,Content-Type",
	}
	rn28AllowedHeaders = map[string]string{
		"GET": "Authorization",
	}
	rn25AllowedHeaders = map[string]string{
		"POST": "Authorization",
	}
	rn38AllowedHeaders = map[string]string{
		"POST": "Authorization,Content-Type",
	}
	rn4AllowedHeaders = map[string]string{
		"POST": "Authorization,Content-Type",
	}
	rn33AllowedHeaders = map[string]string{
		"POST": "Authorization",
	}
	rn35AllowedHeaders = map[string]string{
		"POST": "Authorization,Content-Type",
	}
	rn24AllowedHeaders = map[string]string{
		"GET": "Authorization",
	}
	rn39AllowedHeaders = map[string]string{
		"POST": "Authorization,Content-Type",
	}
)

func (s *Server) cutPrefix(path string) (string, bool) {
	prefix := s.cfg.Prefix
	if prefix == "" {
		return path, true
	}
	if !strings.HasPrefix(path, prefix) {
		// Prefix doesn't match.
		return "", false
	}
	// Cut prefix from the path.
	return strings.TrimPrefix(path, prefix), true
}

// ServeHTTP serves http request as defined by OpenAPI v3 specification,
// calling handler that matches the path or returning not found error.
func (s *Server) ServeHTTP(w http.ResponseWriter, r *http.Request) {
	elem := r.URL.Path
	elemIsEscaped := false
	if rawPath := r.URL.RawPath; rawPath != "" {
		if normalized, ok := uri.NormalizeEscapedPath(rawPath); ok {
			elem = normalized
			elemIsEscaped = strings.ContainsRune(elem, '%')
		}
	}

	elem, ok := s.cutPrefix(elem)
	if !ok || len(elem) == 0 {
		s.notFound(w, r)
		return
	}
	args := [2]string{}

	// Static code generated router with unwrapped path search.
	switch {
	default:
		if len(elem) == 0 {
			break
		}
		switch elem[0] {
		case '/': // Prefix: "/api/v1/"

			if l := len("/api/v1/"); len(elem) >= l && elem[0:l] == "/api/v1/" {
				elem = elem[l:]
			} else {
				break
			}

			if len(elem) == 0 {
				break
			}
			switch elem[0] {
			case 'a': // Prefix: "attachments"

				if l := len("attachments"); len(elem) >= l && elem[0:l] == "attachments" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					switch r.Method {
					case "POST":
						s.handleUploadAttachmentRequest([0]string{}, elemIsEscaped, w, r)
					default:
						s.notAllowed(w, r, notAllowedParams{
							allowedMethods: "POST",
							allowedHeaders: rn40AllowedHeaders,
							acceptPost:     "application/octet-stream",
							acceptPatch:    "",
						})
					}

					return
				}
				switch elem[0] {
				case '/': // Prefix: "/"

					if l := len("/"); len(elem) >= l && elem[0:l] == "/" {
						elem = elem[l:]
					} else {
						break
					}

					// Param: "attachment"
					// Leaf parameter, slashes are prohibited
					idx := strings.IndexByte(elem, '/')
					if idx >= 0 {
						break
					}
					args[0] = elem
					elem = ""

					if len(elem) == 0 {
						// Leaf node.
						switch r.Method {
						case "GET":
							s.handleDownloadAttachmentRequest([1]string{
								args[0],
							}, elemIsEscaped, w, r)
						default:
							s.notAllowed(w, r, notAllowedParams{
								allowedMethods: "GET",
								allowedHeaders: rn22AllowedHeaders,
								acceptPost:     "",
								acceptPatch:    "",
							})
						}

						return
					}

				}

			case 'b': // Prefix: "bindings"

				if l := len("bindings"); len(elem) >= l && elem[0:l] == "bindings" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					switch r.Method {
					case "GET":
						s.handleListBindingsRequest([0]string{}, elemIsEscaped, w, r)
					default:
						s.notAllowed(w, r, notAllowedParams{
							allowedMethods: "GET",
							allowedHeaders: rn26AllowedHeaders,
							acceptPost:     "",
							acceptPatch:    "",
						})
					}

					return
				}
				switch elem[0] {
				case '/': // Prefix: "/"

					if l := len("/"); len(elem) >= l && elem[0:l] == "/" {
						elem = elem[l:]
					} else {
						break
					}

					// Param: "binding"
					// Leaf parameter, slashes are prohibited
					idx := strings.IndexByte(elem, '/')
					if idx >= 0 {
						break
					}
					args[0] = elem
					elem = ""

					if len(elem) == 0 {
						// Leaf node.
						switch r.Method {
						case "DELETE":
							s.handleDeleteBindingRequest([1]string{
								args[0],
							}, elemIsEscaped, w, r)
						case "GET":
							s.handleGetBindingRequest([1]string{
								args[0],
							}, elemIsEscaped, w, r)
						default:
							s.notAllowed(w, r, notAllowedParams{
								allowedMethods: "DELETE,GET",
								allowedHeaders: rn18AllowedHeaders,
								acceptPost:     "",
								acceptPatch:    "",
							})
						}

						return
					}

				}

			case 'c': // Prefix: "channels"

				if l := len("channels"); len(elem) >= l && elem[0:l] == "channels" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					switch r.Method {
					case "GET":
						s.handleListChannelsRequest([0]string{}, elemIsEscaped, w, r)
					case "POST":
						s.handleCreateChannelRequest([0]string{}, elemIsEscaped, w, r)
					default:
						s.notAllowed(w, r, notAllowedParams{
							allowedMethods: "GET,POST",
							allowedHeaders: rn12AllowedHeaders,
							acceptPost:     "application/json",
							acceptPatch:    "",
						})
					}

					return
				}
				switch elem[0] {
				case '/': // Prefix: "/"

					if l := len("/"); len(elem) >= l && elem[0:l] == "/" {
						elem = elem[l:]
					} else {
						break
					}

					// Param: "channel"
					// Match until "/"
					idx := strings.IndexByte(elem, '/')
					if idx < 0 {
						idx = len(elem)
					}
					args[0] = elem[:idx]
					elem = elem[idx:]

					if len(elem) == 0 {
						switch r.Method {
						case "DELETE":
							s.handleDeleteChannelRequest([1]string{
								args[0],
							}, elemIsEscaped, w, r)
						case "GET":
							s.handleGetChannelRequest([1]string{
								args[0],
							}, elemIsEscaped, w, r)
						case "PATCH":
							s.handleUpdateChannelRequest([1]string{
								args[0],
							}, elemIsEscaped, w, r)
						default:
							s.notAllowed(w, r, notAllowedParams{
								allowedMethods: "DELETE,GET,PATCH",
								allowedHeaders: rn7AllowedHeaders,
								acceptPost:     "",
								acceptPatch:    "application/json",
							})
						}

						return
					}
					switch elem[0] {
					case '/': // Prefix: "/"

						if l := len("/"); len(elem) >= l && elem[0:l] == "/" {
							elem = elem[l:]
						} else {
							break
						}

						if len(elem) == 0 {
							break
						}
						switch elem[0] {
						case 'b': // Prefix: "binding-codes"

							if l := len("binding-codes"); len(elem) >= l && elem[0:l] == "binding-codes" {
								elem = elem[l:]
							} else {
								break
							}

							if len(elem) == 0 {
								// Leaf node.
								switch r.Method {
								case "POST":
									s.handleCreateBindingCodeRequest([1]string{
										args[0],
									}, elemIsEscaped, w, r)
								default:
									s.notAllowed(w, r, notAllowedParams{
										allowedMethods: "POST",
										allowedHeaders: rn11AllowedHeaders,
										acceptPost:     "",
										acceptPatch:    "",
									})
								}

								return
							}

						case 'r': // Prefix: "rejections"

							if l := len("rejections"); len(elem) >= l && elem[0:l] == "rejections" {
								elem = elem[l:]
							} else {
								break
							}

							if len(elem) == 0 {
								// Leaf node.
								switch r.Method {
								case "GET":
									s.handleListChannelRejectionsRequest([1]string{
										args[0],
									}, elemIsEscaped, w, r)
								default:
									s.notAllowed(w, r, notAllowedParams{
										allowedMethods: "GET",
										allowedHeaders: rn27AllowedHeaders,
										acceptPost:     "",
										acceptPatch:    "",
									})
								}

								return
							}

						case 's': // Prefix: "se"

							if l := len("se"); len(elem) >= l && elem[0:l] == "se" {
								elem = elem[l:]
							} else {
								break
							}

							if len(elem) == 0 {
								break
							}
							switch elem[0] {
							case 'c': // Prefix: "cret"

								if l := len("cret"); len(elem) >= l && elem[0:l] == "cret" {
									elem = elem[l:]
								} else {
									break
								}

								if len(elem) == 0 {
									// Leaf node.
									switch r.Method {
									case "POST":
										s.handleRotateChannelSecretRequest([1]string{
											args[0],
										}, elemIsEscaped, w, r)
									default:
										s.notAllowed(w, r, notAllowedParams{
											allowedMethods: "POST",
											allowedHeaders: rn37AllowedHeaders,
											acceptPost:     "application/json",
											acceptPatch:    "",
										})
									}

									return
								}

							case 'n': // Prefix: "nder-check"

								if l := len("nder-check"); len(elem) >= l && elem[0:l] == "nder-check" {
									elem = elem[l:]
								} else {
									break
								}

								if len(elem) == 0 {
									// Leaf node.
									switch r.Method {
									case "GET":
										s.handleCheckSenderRequest([1]string{
											args[0],
										}, elemIsEscaped, w, r)
									default:
										s.notAllowed(w, r, notAllowedParams{
											allowedMethods: "GET",
											allowedHeaders: rn10AllowedHeaders,
											acceptPost:     "",
											acceptPatch:    "",
										})
									}

									return
								}

							}

						case 'w': // Prefix: "weixin-logins"

							if l := len("weixin-logins"); len(elem) >= l && elem[0:l] == "weixin-logins" {
								elem = elem[l:]
							} else {
								break
							}

							if len(elem) == 0 {
								// Leaf node.
								switch r.Method {
								case "POST":
									s.handleBeginWeixinLoginRequest([1]string{
										args[0],
									}, elemIsEscaped, w, r)
								default:
									s.notAllowed(w, r, notAllowedParams{
										allowedMethods: "POST",
										allowedHeaders: rn8AllowedHeaders,
										acceptPost:     "",
										acceptPatch:    "",
									})
								}

								return
							}

						}

					}

				}

			case 'm': // Prefix: "m"

				if l := len("m"); len(elem) >= l && elem[0:l] == "m" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					break
				}
				switch elem[0] {
				case 'e': // Prefix: "emories"

					if l := len("emories"); len(elem) >= l && elem[0:l] == "emories" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						switch r.Method {
						case "GET":
							s.handleListMemoriesRequest([0]string{}, elemIsEscaped, w, r)
						default:
							s.notAllowed(w, r, notAllowedParams{
								allowedMethods: "GET",
								allowedHeaders: rn29AllowedHeaders,
								acceptPost:     "",
								acceptPatch:    "",
							})
						}

						return
					}
					switch elem[0] {
					case '/': // Prefix: "/"

						if l := len("/"); len(elem) >= l && elem[0:l] == "/" {
							elem = elem[l:]
						} else {
							break
						}

						// Param: "memory"
						// Leaf parameter, slashes are prohibited
						idx := strings.IndexByte(elem, '/')
						if idx >= 0 {
							break
						}
						args[0] = elem
						elem = ""

						if len(elem) == 0 {
							// Leaf node.
							switch r.Method {
							case "DELETE":
								s.handleDeleteMemoryRequest([1]string{
									args[0],
								}, elemIsEscaped, w, r)
							default:
								s.notAllowed(w, r, notAllowedParams{
									allowedMethods: "DELETE",
									allowedHeaders: rn20AllowedHeaders,
									acceptPost:     "",
									acceptPatch:    "",
								})
							}

							return
						}

					}

				case 'o': // Prefix: "odels"

					if l := len("odels"); len(elem) >= l && elem[0:l] == "odels" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						// Leaf node.
						switch r.Method {
						case "GET":
							s.handleListModelsRequest([0]string{}, elemIsEscaped, w, r)
						default:
							s.notAllowed(w, r, notAllowedParams{
								allowedMethods: "GET",
								allowedHeaders: rn31AllowedHeaders,
								acceptPost:     "",
								acceptPatch:    "",
							})
						}

						return
					}

				}

			case 'p': // Prefix: "platforms"

				if l := len("platforms"); len(elem) >= l && elem[0:l] == "platforms" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					// Leaf node.
					switch r.Method {
					case "GET":
						s.handleListPlatformsRequest([0]string{}, elemIsEscaped, w, r)
					default:
						s.notAllowed(w, r, notAllowedParams{
							allowedMethods: "GET",
							allowedHeaders: rn32AllowedHeaders,
							acceptPost:     "",
							acceptPatch:    "",
						})
					}

					return
				}

			case 't': // Prefix: "threads"

				if l := len("threads"); len(elem) >= l && elem[0:l] == "threads" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					switch r.Method {
					case "GET":
						s.handleListThreadsRequest([0]string{}, elemIsEscaped, w, r)
					case "POST":
						s.handleCreateThreadRequest([0]string{}, elemIsEscaped, w, r)
					default:
						s.notAllowed(w, r, notAllowedParams{
							allowedMethods: "GET,POST",
							allowedHeaders: rn13AllowedHeaders,
							acceptPost:     "application/json",
							acceptPatch:    "",
						})
					}

					return
				}
				switch elem[0] {
				case '/': // Prefix: "/"

					if l := len("/"); len(elem) >= l && elem[0:l] == "/" {
						elem = elem[l:]
					} else {
						break
					}

					// Param: "thread"
					// Match until "/"
					idx := strings.IndexByte(elem, '/')
					if idx < 0 {
						idx = len(elem)
					}
					args[0] = elem[:idx]
					elem = elem[idx:]

					if len(elem) == 0 {
						switch r.Method {
						case "GET":
							s.handleGetThreadRequest([1]string{
								args[0],
							}, elemIsEscaped, w, r)
						case "PATCH":
							s.handleUpdateThreadRequest([1]string{
								args[0],
							}, elemIsEscaped, w, r)
						default:
							s.notAllowed(w, r, notAllowedParams{
								allowedMethods: "GET,PATCH",
								allowedHeaders: rn2AllowedHeaders,
								acceptPost:     "",
								acceptPatch:    "application/json",
							})
						}

						return
					}
					switch elem[0] {
					case '/': // Prefix: "/"

						if l := len("/"); len(elem) >= l && elem[0:l] == "/" {
							elem = elem[l:]
						} else {
							break
						}

						if len(elem) == 0 {
							break
						}
						switch elem[0] {
						case 'a': // Prefix: "approvals/"

							if l := len("approvals/"); len(elem) >= l && elem[0:l] == "approvals/" {
								elem = elem[l:]
							} else {
								break
							}

							// Param: "batch"
							// Leaf parameter, slashes are prohibited
							idx := strings.IndexByte(elem, '/')
							if idx >= 0 {
								break
							}
							args[1] = elem
							elem = ""

							if len(elem) == 0 {
								// Leaf node.
								switch r.Method {
								case "POST":
									s.handleDecideApprovalRequest([2]string{
										args[0],
										args[1],
									}, elemIsEscaped, w, r)
								default:
									s.notAllowed(w, r, notAllowedParams{
										allowedMethods: "POST",
										allowedHeaders: rn16AllowedHeaders,
										acceptPost:     "application/json",
										acceptPatch:    "",
									})
								}

								return
							}

						case 'e': // Prefix: "earlier"

							if l := len("earlier"); len(elem) >= l && elem[0:l] == "earlier" {
								elem = elem[l:]
							} else {
								break
							}

							if len(elem) == 0 {
								// Leaf node.
								switch r.Method {
								case "GET":
									s.handleListEarlierItemsRequest([1]string{
										args[0],
									}, elemIsEscaped, w, r)
								default:
									s.notAllowed(w, r, notAllowedParams{
										allowedMethods: "GET",
										allowedHeaders: rn28AllowedHeaders,
										acceptPost:     "",
										acceptPatch:    "",
									})
								}

								return
							}

						case 'i': // Prefix: "interrupt"

							if l := len("interrupt"); len(elem) >= l && elem[0:l] == "interrupt" {
								elem = elem[l:]
							} else {
								break
							}

							if len(elem) == 0 {
								// Leaf node.
								switch r.Method {
								case "POST":
									s.handleInterruptThreadRequest([1]string{
										args[0],
									}, elemIsEscaped, w, r)
								default:
									s.notAllowed(w, r, notAllowedParams{
										allowedMethods: "POST",
										allowedHeaders: rn25AllowedHeaders,
										acceptPost:     "",
										acceptPatch:    "",
									})
								}

								return
							}

						case 'm': // Prefix: "messages"

							if l := len("messages"); len(elem) >= l && elem[0:l] == "messages" {
								elem = elem[l:]
							} else {
								break
							}

							if len(elem) == 0 {
								// Leaf node.
								switch r.Method {
								case "POST":
									s.handleSendMessageRequest([1]string{
										args[0],
									}, elemIsEscaped, w, r)
								default:
									s.notAllowed(w, r, notAllowedParams{
										allowedMethods: "POST",
										allowedHeaders: rn38AllowedHeaders,
										acceptPost:     "application/json",
										acceptPatch:    "",
									})
								}

								return
							}

						case 'q': // Prefix: "questions/"

							if l := len("questions/"); len(elem) >= l && elem[0:l] == "questions/" {
								elem = elem[l:]
							} else {
								break
							}

							// Param: "item"
							// Leaf parameter, slashes are prohibited
							idx := strings.IndexByte(elem, '/')
							if idx >= 0 {
								break
							}
							args[1] = elem
							elem = ""

							if len(elem) == 0 {
								// Leaf node.
								switch r.Method {
								case "POST":
									s.handleAnswerQuestionRequest([2]string{
										args[0],
										args[1],
									}, elemIsEscaped, w, r)
								default:
									s.notAllowed(w, r, notAllowedParams{
										allowedMethods: "POST",
										allowedHeaders: rn4AllowedHeaders,
										acceptPost:     "application/json",
										acceptPatch:    "",
									})
								}

								return
							}

						case 'r': // Prefix: "re"

							if l := len("re"); len(elem) >= l && elem[0:l] == "re" {
								elem = elem[l:]
							} else {
								break
							}

							if len(elem) == 0 {
								break
							}
							switch elem[0] {
							case 'a': // Prefix: "ad"

								if l := len("ad"); len(elem) >= l && elem[0:l] == "ad" {
									elem = elem[l:]
								} else {
									break
								}

								if len(elem) == 0 {
									// Leaf node.
									switch r.Method {
									case "POST":
										s.handleMarkThreadReadRequest([1]string{
											args[0],
										}, elemIsEscaped, w, r)
									default:
										s.notAllowed(w, r, notAllowedParams{
											allowedMethods: "POST",
											allowedHeaders: rn33AllowedHeaders,
											acceptPost:     "",
											acceptPatch:    "",
										})
									}

									return
								}

							case 'v': // Prefix: "vert"

								if l := len("vert"); len(elem) >= l && elem[0:l] == "vert" {
									elem = elem[l:]
								} else {
									break
								}

								if len(elem) == 0 {
									// Leaf node.
									switch r.Method {
									case "POST":
										s.handleRevertThreadRequest([1]string{
											args[0],
										}, elemIsEscaped, w, r)
									default:
										s.notAllowed(w, r, notAllowedParams{
											allowedMethods: "POST",
											allowedHeaders: rn35AllowedHeaders,
											acceptPost:     "application/json",
											acceptPatch:    "",
										})
									}

									return
								}

							}

						}

					}

				}

			case 'w': // Prefix: "weixin-logins/"

				if l := len("weixin-logins/"); len(elem) >= l && elem[0:l] == "weixin-logins/" {
					elem = elem[l:]
				} else {
					break
				}

				// Param: "login"
				// Match until "/"
				idx := strings.IndexByte(elem, '/')
				if idx < 0 {
					idx = len(elem)
				}
				args[0] = elem[:idx]
				elem = elem[idx:]

				if len(elem) == 0 {
					switch r.Method {
					case "GET":
						s.handleGetWeixinLoginRequest([1]string{
							args[0],
						}, elemIsEscaped, w, r)
					default:
						s.notAllowed(w, r, notAllowedParams{
							allowedMethods: "GET",
							allowedHeaders: rn24AllowedHeaders,
							acceptPost:     "",
							acceptPatch:    "",
						})
					}

					return
				}
				switch elem[0] {
				case '/': // Prefix: "/verify-code"

					if l := len("/verify-code"); len(elem) >= l && elem[0:l] == "/verify-code" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						// Leaf node.
						switch r.Method {
						case "POST":
							s.handleSubmitWeixinVerifyCodeRequest([1]string{
								args[0],
							}, elemIsEscaped, w, r)
						default:
							s.notAllowed(w, r, notAllowedParams{
								allowedMethods: "POST",
								allowedHeaders: rn39AllowedHeaders,
								acceptPost:     "application/json",
								acceptPatch:    "",
							})
						}

						return
					}

				}

			}

		}
	}
	s.notFound(w, r)
}

// Route is route object.
type Route struct {
	name           string
	summary        string
	operationID    string
	operationGroup string
	pathPattern    string
	count          int
	args           [2]string
}

// Name returns ogen operation name.
//
// It is guaranteed to be unique and not empty.
func (r Route) Name() string {
	return r.name
}

// Summary returns OpenAPI summary.
func (r Route) Summary() string {
	return r.summary
}

// OperationID returns OpenAPI operationId.
func (r Route) OperationID() string {
	return r.operationID
}

// OperationGroup returns the x-ogen-operation-group value.
func (r Route) OperationGroup() string {
	return r.operationGroup
}

// PathPattern returns OpenAPI path.
func (r Route) PathPattern() string {
	return r.pathPattern
}

// Args returns parsed arguments.
func (r Route) Args() []string {
	return r.args[:r.count]
}

// FindRoute finds Route for given method and path.
//
// Note: this method does not unescape path or handle reserved characters in path properly. Use FindPath instead.
func (s *Server) FindRoute(method, path string) (Route, bool) {
	return s.FindPath(method, &url.URL{Path: path})
}

// FindPath finds Route for given method and URL.
func (s *Server) FindPath(method string, u *url.URL) (r Route, _ bool) {
	var (
		elem = u.Path
		args = r.args
	)
	if rawPath := u.RawPath; rawPath != "" {
		if normalized, ok := uri.NormalizeEscapedPath(rawPath); ok {
			elem = normalized
		}
		defer func() {
			for i, arg := range r.args[:r.count] {
				if unescaped, err := url.PathUnescape(arg); err == nil {
					r.args[i] = unescaped
				}
			}
		}()
	}

	elem, ok := s.cutPrefix(elem)
	if !ok {
		return r, false
	}

	// Static code generated router with unwrapped path search.
	switch {
	default:
		if len(elem) == 0 {
			break
		}
		switch elem[0] {
		case '/': // Prefix: "/api/v1/"

			if l := len("/api/v1/"); len(elem) >= l && elem[0:l] == "/api/v1/" {
				elem = elem[l:]
			} else {
				break
			}

			if len(elem) == 0 {
				break
			}
			switch elem[0] {
			case 'a': // Prefix: "attachments"

				if l := len("attachments"); len(elem) >= l && elem[0:l] == "attachments" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					switch method {
					case "POST":
						r.name = UploadAttachmentOperation
						r.summary = "Upload an image"
						r.operationID = "upload-attachment"
						r.operationGroup = ""
						r.pathPattern = "/api/v1/attachments"
						r.args = args
						r.count = 0
						return r, true
					default:
						return
					}
				}
				switch elem[0] {
				case '/': // Prefix: "/"

					if l := len("/"); len(elem) >= l && elem[0:l] == "/" {
						elem = elem[l:]
					} else {
						break
					}

					// Param: "attachment"
					// Leaf parameter, slashes are prohibited
					idx := strings.IndexByte(elem, '/')
					if idx >= 0 {
						break
					}
					args[0] = elem
					elem = ""

					if len(elem) == 0 {
						// Leaf node.
						switch method {
						case "GET":
							r.name = DownloadAttachmentOperation
							r.summary = "Fetch an image"
							r.operationID = "download-attachment"
							r.operationGroup = ""
							r.pathPattern = "/api/v1/attachments/{attachment}"
							r.args = args
							r.count = 1
							return r, true
						default:
							return
						}
					}

				}

			case 'b': // Prefix: "bindings"

				if l := len("bindings"); len(elem) >= l && elem[0:l] == "bindings" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					switch method {
					case "GET":
						r.name = ListBindingsOperation
						r.summary = "List bindings"
						r.operationID = "list-bindings"
						r.operationGroup = ""
						r.pathPattern = "/api/v1/bindings"
						r.args = args
						r.count = 0
						return r, true
					default:
						return
					}
				}
				switch elem[0] {
				case '/': // Prefix: "/"

					if l := len("/"); len(elem) >= l && elem[0:l] == "/" {
						elem = elem[l:]
					} else {
						break
					}

					// Param: "binding"
					// Leaf parameter, slashes are prohibited
					idx := strings.IndexByte(elem, '/')
					if idx >= 0 {
						break
					}
					args[0] = elem
					elem = ""

					if len(elem) == 0 {
						// Leaf node.
						switch method {
						case "DELETE":
							r.name = DeleteBindingOperation
							r.summary = "Remove a binding"
							r.operationID = "delete-binding"
							r.operationGroup = ""
							r.pathPattern = "/api/v1/bindings/{binding}"
							r.args = args
							r.count = 1
							return r, true
						case "GET":
							r.name = GetBindingOperation
							r.summary = "Get a binding"
							r.operationID = "get-binding"
							r.operationGroup = ""
							r.pathPattern = "/api/v1/bindings/{binding}"
							r.args = args
							r.count = 1
							return r, true
						default:
							return
						}
					}

				}

			case 'c': // Prefix: "channels"

				if l := len("channels"); len(elem) >= l && elem[0:l] == "channels" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					switch method {
					case "GET":
						r.name = ListChannelsOperation
						r.summary = "List channels"
						r.operationID = "list-channels"
						r.operationGroup = ""
						r.pathPattern = "/api/v1/channels"
						r.args = args
						r.count = 0
						return r, true
					case "POST":
						r.name = CreateChannelOperation
						r.summary = "Create a channel"
						r.operationID = "create-channel"
						r.operationGroup = ""
						r.pathPattern = "/api/v1/channels"
						r.args = args
						r.count = 0
						return r, true
					default:
						return
					}
				}
				switch elem[0] {
				case '/': // Prefix: "/"

					if l := len("/"); len(elem) >= l && elem[0:l] == "/" {
						elem = elem[l:]
					} else {
						break
					}

					// Param: "channel"
					// Match until "/"
					idx := strings.IndexByte(elem, '/')
					if idx < 0 {
						idx = len(elem)
					}
					args[0] = elem[:idx]
					elem = elem[idx:]

					if len(elem) == 0 {
						switch method {
						case "DELETE":
							r.name = DeleteChannelOperation
							r.summary = "Delete a channel"
							r.operationID = "delete-channel"
							r.operationGroup = ""
							r.pathPattern = "/api/v1/channels/{channel}"
							r.args = args
							r.count = 1
							return r, true
						case "GET":
							r.name = GetChannelOperation
							r.summary = "Get a channel"
							r.operationID = "get-channel"
							r.operationGroup = ""
							r.pathPattern = "/api/v1/channels/{channel}"
							r.args = args
							r.count = 1
							return r, true
						case "PATCH":
							r.name = UpdateChannelOperation
							r.summary = "Update a channel"
							r.operationID = "update-channel"
							r.operationGroup = ""
							r.pathPattern = "/api/v1/channels/{channel}"
							r.args = args
							r.count = 1
							return r, true
						default:
							return
						}
					}
					switch elem[0] {
					case '/': // Prefix: "/"

						if l := len("/"); len(elem) >= l && elem[0:l] == "/" {
							elem = elem[l:]
						} else {
							break
						}

						if len(elem) == 0 {
							break
						}
						switch elem[0] {
						case 'b': // Prefix: "binding-codes"

							if l := len("binding-codes"); len(elem) >= l && elem[0:l] == "binding-codes" {
								elem = elem[l:]
							} else {
								break
							}

							if len(elem) == 0 {
								// Leaf node.
								switch method {
								case "POST":
									r.name = CreateBindingCodeOperation
									r.summary = "Issue a binding code"
									r.operationID = "create-binding-code"
									r.operationGroup = ""
									r.pathPattern = "/api/v1/channels/{channel}/binding-codes"
									r.args = args
									r.count = 1
									return r, true
								default:
									return
								}
							}

						case 'r': // Prefix: "rejections"

							if l := len("rejections"); len(elem) >= l && elem[0:l] == "rejections" {
								elem = elem[l:]
							} else {
								break
							}

							if len(elem) == 0 {
								// Leaf node.
								switch method {
								case "GET":
									r.name = ListChannelRejectionsOperation
									r.summary = "List recently rejected inbound messages"
									r.operationID = "list-channel-rejections"
									r.operationGroup = ""
									r.pathPattern = "/api/v1/channels/{channel}/rejections"
									r.args = args
									r.count = 1
									return r, true
								default:
									return
								}
							}

						case 's': // Prefix: "se"

							if l := len("se"); len(elem) >= l && elem[0:l] == "se" {
								elem = elem[l:]
							} else {
								break
							}

							if len(elem) == 0 {
								break
							}
							switch elem[0] {
							case 'c': // Prefix: "cret"

								if l := len("cret"); len(elem) >= l && elem[0:l] == "cret" {
									elem = elem[l:]
								} else {
									break
								}

								if len(elem) == 0 {
									// Leaf node.
									switch method {
									case "POST":
										r.name = RotateChannelSecretOperation
										r.summary = "Rotate the webhook secret"
										r.operationID = "rotate-channel-secret"
										r.operationGroup = ""
										r.pathPattern = "/api/v1/channels/{channel}/secret"
										r.args = args
										r.count = 1
										return r, true
									default:
										return
									}
								}

							case 'n': // Prefix: "nder-check"

								if l := len("nder-check"); len(elem) >= l && elem[0:l] == "nder-check" {
									elem = elem[l:]
								} else {
									break
								}

								if len(elem) == 0 {
									// Leaf node.
									switch method {
									case "GET":
										r.name = CheckSenderOperation
										r.summary = "Test whether a sender would be let through"
										r.operationID = "check-sender"
										r.operationGroup = ""
										r.pathPattern = "/api/v1/channels/{channel}/sender-check"
										r.args = args
										r.count = 1
										return r, true
									default:
										return
									}
								}

							}

						case 'w': // Prefix: "weixin-logins"

							if l := len("weixin-logins"); len(elem) >= l && elem[0:l] == "weixin-logins" {
								elem = elem[l:]
							} else {
								break
							}

							if len(elem) == 0 {
								// Leaf node.
								switch method {
								case "POST":
									r.name = BeginWeixinLoginOperation
									r.summary = "Begin a WeChat QR login"
									r.operationID = "begin-weixin-login"
									r.operationGroup = ""
									r.pathPattern = "/api/v1/channels/{channel}/weixin-logins"
									r.args = args
									r.count = 1
									return r, true
								default:
									return
								}
							}

						}

					}

				}

			case 'm': // Prefix: "m"

				if l := len("m"); len(elem) >= l && elem[0:l] == "m" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					break
				}
				switch elem[0] {
				case 'e': // Prefix: "emories"

					if l := len("emories"); len(elem) >= l && elem[0:l] == "emories" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						switch method {
						case "GET":
							r.name = ListMemoriesOperation
							r.summary = "List what the assistant remembers"
							r.operationID = "list-memories"
							r.operationGroup = ""
							r.pathPattern = "/api/v1/memories"
							r.args = args
							r.count = 0
							return r, true
						default:
							return
						}
					}
					switch elem[0] {
					case '/': // Prefix: "/"

						if l := len("/"); len(elem) >= l && elem[0:l] == "/" {
							elem = elem[l:]
						} else {
							break
						}

						// Param: "memory"
						// Leaf parameter, slashes are prohibited
						idx := strings.IndexByte(elem, '/')
						if idx >= 0 {
							break
						}
						args[0] = elem
						elem = ""

						if len(elem) == 0 {
							// Leaf node.
							switch method {
							case "DELETE":
								r.name = DeleteMemoryOperation
								r.summary = "Delete one memory"
								r.operationID = "delete-memory"
								r.operationGroup = ""
								r.pathPattern = "/api/v1/memories/{memory}"
								r.args = args
								r.count = 1
								return r, true
							default:
								return
							}
						}

					}

				case 'o': // Prefix: "odels"

					if l := len("odels"); len(elem) >= l && elem[0:l] == "odels" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						// Leaf node.
						switch method {
						case "GET":
							r.name = ListModelsOperation
							r.summary = "List available models"
							r.operationID = "list-models"
							r.operationGroup = ""
							r.pathPattern = "/api/v1/models"
							r.args = args
							r.count = 0
							return r, true
						default:
							return
						}
					}

				}

			case 'p': // Prefix: "platforms"

				if l := len("platforms"); len(elem) >= l && elem[0:l] == "platforms" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					// Leaf node.
					switch method {
					case "GET":
						r.name = ListPlatformsOperation
						r.summary = "List platforms that can be connected"
						r.operationID = "list-platforms"
						r.operationGroup = ""
						r.pathPattern = "/api/v1/platforms"
						r.args = args
						r.count = 0
						return r, true
					default:
						return
					}
				}

			case 't': // Prefix: "threads"

				if l := len("threads"); len(elem) >= l && elem[0:l] == "threads" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					switch method {
					case "GET":
						r.name = ListThreadsOperation
						r.summary = "List conversations"
						r.operationID = "list-threads"
						r.operationGroup = ""
						r.pathPattern = "/api/v1/threads"
						r.args = args
						r.count = 0
						return r, true
					case "POST":
						r.name = CreateThreadOperation
						r.summary = "Create a conversation"
						r.operationID = "create-thread"
						r.operationGroup = ""
						r.pathPattern = "/api/v1/threads"
						r.args = args
						r.count = 0
						return r, true
					default:
						return
					}
				}
				switch elem[0] {
				case '/': // Prefix: "/"

					if l := len("/"); len(elem) >= l && elem[0:l] == "/" {
						elem = elem[l:]
					} else {
						break
					}

					// Param: "thread"
					// Match until "/"
					idx := strings.IndexByte(elem, '/')
					if idx < 0 {
						idx = len(elem)
					}
					args[0] = elem[:idx]
					elem = elem[idx:]

					if len(elem) == 0 {
						switch method {
						case "GET":
							r.name = GetThreadOperation
							r.summary = "Fetch the conversation document"
							r.operationID = "get-thread"
							r.operationGroup = ""
							r.pathPattern = "/api/v1/threads/{thread}"
							r.args = args
							r.count = 1
							return r, true
						case "PATCH":
							r.name = UpdateThreadOperation
							r.summary = "Update conversation settings"
							r.operationID = "update-thread"
							r.operationGroup = ""
							r.pathPattern = "/api/v1/threads/{thread}"
							r.args = args
							r.count = 1
							return r, true
						default:
							return
						}
					}
					switch elem[0] {
					case '/': // Prefix: "/"

						if l := len("/"); len(elem) >= l && elem[0:l] == "/" {
							elem = elem[l:]
						} else {
							break
						}

						if len(elem) == 0 {
							break
						}
						switch elem[0] {
						case 'a': // Prefix: "approvals/"

							if l := len("approvals/"); len(elem) >= l && elem[0:l] == "approvals/" {
								elem = elem[l:]
							} else {
								break
							}

							// Param: "batch"
							// Leaf parameter, slashes are prohibited
							idx := strings.IndexByte(elem, '/')
							if idx >= 0 {
								break
							}
							args[1] = elem
							elem = ""

							if len(elem) == 0 {
								// Leaf node.
								switch method {
								case "POST":
									r.name = DecideApprovalOperation
									r.summary = "Approve or decline a batch of tool calls"
									r.operationID = "decide-approval"
									r.operationGroup = ""
									r.pathPattern = "/api/v1/threads/{thread}/approvals/{batch}"
									r.args = args
									r.count = 2
									return r, true
								default:
									return
								}
							}

						case 'e': // Prefix: "earlier"

							if l := len("earlier"); len(elem) >= l && elem[0:l] == "earlier" {
								elem = elem[l:]
							} else {
								break
							}

							if len(elem) == 0 {
								// Leaf node.
								switch method {
								case "GET":
									r.name = ListEarlierItemsOperation
									r.summary = "Fetch earlier parts of a conversation"
									r.operationID = "list-earlier-items"
									r.operationGroup = ""
									r.pathPattern = "/api/v1/threads/{thread}/earlier"
									r.args = args
									r.count = 1
									return r, true
								default:
									return
								}
							}

						case 'i': // Prefix: "interrupt"

							if l := len("interrupt"); len(elem) >= l && elem[0:l] == "interrupt" {
								elem = elem[l:]
							} else {
								break
							}

							if len(elem) == 0 {
								// Leaf node.
								switch method {
								case "POST":
									r.name = InterruptThreadOperation
									r.summary = "Interrupt a running turn"
									r.operationID = "interrupt-thread"
									r.operationGroup = ""
									r.pathPattern = "/api/v1/threads/{thread}/interrupt"
									r.args = args
									r.count = 1
									return r, true
								default:
									return
								}
							}

						case 'm': // Prefix: "messages"

							if l := len("messages"); len(elem) >= l && elem[0:l] == "messages" {
								elem = elem[l:]
							} else {
								break
							}

							if len(elem) == 0 {
								// Leaf node.
								switch method {
								case "POST":
									r.name = SendMessageOperation
									r.summary = "Send a message and start a turn"
									r.operationID = "send-message"
									r.operationGroup = ""
									r.pathPattern = "/api/v1/threads/{thread}/messages"
									r.args = args
									r.count = 1
									return r, true
								default:
									return
								}
							}

						case 'q': // Prefix: "questions/"

							if l := len("questions/"); len(elem) >= l && elem[0:l] == "questions/" {
								elem = elem[l:]
							} else {
								break
							}

							// Param: "item"
							// Leaf parameter, slashes are prohibited
							idx := strings.IndexByte(elem, '/')
							if idx >= 0 {
								break
							}
							args[1] = elem
							elem = ""

							if len(elem) == 0 {
								// Leaf node.
								switch method {
								case "POST":
									r.name = AnswerQuestionOperation
									r.summary = "Answer the assistant's questions"
									r.operationID = "answer-question"
									r.operationGroup = ""
									r.pathPattern = "/api/v1/threads/{thread}/questions/{item}"
									r.args = args
									r.count = 2
									return r, true
								default:
									return
								}
							}

						case 'r': // Prefix: "re"

							if l := len("re"); len(elem) >= l && elem[0:l] == "re" {
								elem = elem[l:]
							} else {
								break
							}

							if len(elem) == 0 {
								break
							}
							switch elem[0] {
							case 'a': // Prefix: "ad"

								if l := len("ad"); len(elem) >= l && elem[0:l] == "ad" {
									elem = elem[l:]
								} else {
									break
								}

								if len(elem) == 0 {
									// Leaf node.
									switch method {
									case "POST":
										r.name = MarkThreadReadOperation
										r.summary = "Mark a conversation as read"
										r.operationID = "mark-thread-read"
										r.operationGroup = ""
										r.pathPattern = "/api/v1/threads/{thread}/read"
										r.args = args
										r.count = 1
										return r, true
									default:
										return
									}
								}

							case 'v': // Prefix: "vert"

								if l := len("vert"); len(elem) >= l && elem[0:l] == "vert" {
									elem = elem[l:]
								} else {
									break
								}

								if len(elem) == 0 {
									// Leaf node.
									switch method {
									case "POST":
										r.name = RevertThreadOperation
										r.summary = "Revert from a given point"
										r.operationID = "revert-thread"
										r.operationGroup = ""
										r.pathPattern = "/api/v1/threads/{thread}/revert"
										r.args = args
										r.count = 1
										return r, true
									default:
										return
									}
								}

							}

						}

					}

				}

			case 'w': // Prefix: "weixin-logins/"

				if l := len("weixin-logins/"); len(elem) >= l && elem[0:l] == "weixin-logins/" {
					elem = elem[l:]
				} else {
					break
				}

				// Param: "login"
				// Match until "/"
				idx := strings.IndexByte(elem, '/')
				if idx < 0 {
					idx = len(elem)
				}
				args[0] = elem[:idx]
				elem = elem[idx:]

				if len(elem) == 0 {
					switch method {
					case "GET":
						r.name = GetWeixinLoginOperation
						r.summary = "Get the state of a QR login"
						r.operationID = "get-weixin-login"
						r.operationGroup = ""
						r.pathPattern = "/api/v1/weixin-logins/{login}"
						r.args = args
						r.count = 1
						return r, true
					default:
						return
					}
				}
				switch elem[0] {
				case '/': // Prefix: "/verify-code"

					if l := len("/verify-code"); len(elem) >= l && elem[0:l] == "/verify-code" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						// Leaf node.
						switch method {
						case "POST":
							r.name = SubmitWeixinVerifyCodeOperation
							r.summary = "Submit a login verification code"
							r.operationID = "submit-weixin-verify-code"
							r.operationGroup = ""
							r.pathPattern = "/api/v1/weixin-logins/{login}/verify-code"
							r.args = args
							r.count = 1
							return r, true
						default:
							return
						}
					}

				}

			}

		}
	}
	return r, false
}
