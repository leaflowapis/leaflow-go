// Package notificationv1 provides primitives to interact with the openapi HTTP API.
//
// Code generated by github.com/oapi-codegen/oapi-codegen/v2 version v2.8.0 DO NOT EDIT.
package notificationv1

import (
	"bytes"
	"context"
	"encoding/json"
	"fmt"
	"io"
	"net/http"
	"net/url"
	"strings"
	"time"

	externalRef0 "github.com/leaflowapis/leaflow-go/type/v1"
	"github.com/oapi-codegen/runtime"
	openapi_types "github.com/oapi-codegen/runtime/types"
)

// Defines values for ChannelKind.
const (
	Bark           ChannelKind = "bark"
	GenericWebhook ChannelKind = "generic_webhook"
	LarkWebhook    ChannelKind = "lark_webhook"
	WecomWebhook   ChannelKind = "wecom_webhook"
)

// Valid indicates whether the value is a known member of the ChannelKind enum.
func (e ChannelKind) Valid() bool {
	switch e {
	case Bark:
		return true
	case GenericWebhook:
		return true
	case LarkWebhook:
		return true
	case WecomWebhook:
		return true
	default:
		return false
	}
}

// Defines values for ChannelScope.
const (
	Project ChannelScope = "project"
	User    ChannelScope = "user"
)

// Valid indicates whether the value is a known member of the ChannelScope enum.
func (e ChannelScope) Valid() bool {
	switch e {
	case Project:
		return true
	case User:
		return true
	default:
		return false
	}
}

// Defines values for CredentialSubjectKind.
const (
	ComputeInstance CredentialSubjectKind = "compute_instance"
)

// Valid indicates whether the value is a known member of the CredentialSubjectKind enum.
func (e CredentialSubjectKind) Valid() bool {
	switch e {
	case ComputeInstance:
		return true
	default:
		return false
	}
}

// Defines values for NotificationAudience.
const (
	Account        NotificationAudience = "account"
	Address        NotificationAudience = "address"
	Broadcast      NotificationAudience = "broadcast"
	Explicit       NotificationAudience = "explicit"
	ProjectAdmins  NotificationAudience = "project_admins"
	ProjectMembers NotificationAudience = "project_members"
	ProjectOwner   NotificationAudience = "project_owner"
)

// Valid indicates whether the value is a known member of the NotificationAudience enum.
func (e NotificationAudience) Valid() bool {
	switch e {
	case Account:
		return true
	case Address:
		return true
	case Broadcast:
		return true
	case Explicit:
		return true
	case ProjectAdmins:
		return true
	case ProjectMembers:
		return true
	case ProjectOwner:
		return true
	default:
		return false
	}
}

// Defines values for NotificationChannel.
const (
	Email   NotificationChannel = "email"
	Inbox   NotificationChannel = "inbox"
	Webhook NotificationChannel = "webhook"
)

// Valid indicates whether the value is a known member of the NotificationChannel enum.
func (e NotificationChannel) Valid() bool {
	switch e {
	case Email:
		return true
	case Inbox:
		return true
	case Webhook:
		return true
	default:
		return false
	}
}

// Defines values for NotificationKind.
const (
	Announcement NotificationKind = "announcement"
	Notification NotificationKind = "notification"
)

// Valid indicates whether the value is a known member of the NotificationKind enum.
func (e NotificationKind) Valid() bool {
	switch e {
	case Announcement:
		return true
	case Notification:
		return true
	default:
		return false
	}
}

// Defines values for NotificationScope.
const (
	All            NotificationScope = "all"
	CurrentProject NotificationScope = "current_project"
)

// Valid indicates whether the value is a known member of the NotificationScope enum.
func (e NotificationScope) Valid() bool {
	switch e {
	case All:
		return true
	case CurrentProject:
		return true
	default:
		return false
	}
}

// Defines values for NotificationSeverity.
const (
	NotificationSeverityCritical NotificationSeverity = "critical"
	NotificationSeverityError    NotificationSeverity = "error"
	NotificationSeverityInfo     NotificationSeverity = "info"
	NotificationSeverityWarning  NotificationSeverity = "warning"
)

// Valid indicates whether the value is a known member of the NotificationSeverity enum.
func (e NotificationSeverity) Valid() bool {
	switch e {
	case NotificationSeverityCritical:
		return true
	case NotificationSeverityError:
		return true
	case NotificationSeverityInfo:
		return true
	case NotificationSeverityWarning:
		return true
	default:
		return false
	}
}

// AuthorizeRealtimeChannelRequestBody defines model for AuthorizeRealtimeChannelRequestBody.
type AuthorizeRealtimeChannelRequestBody struct {
	// ChannelName The channel being subscribed to
	ChannelName string `json:"channel_name"`

	// SocketId The connection identifier the realtime server assigned to this client
	SocketId string `json:"socket_id"`
}

// ChannelKind The kind of endpoint, which determines both how the message is shaped and what the
// endpoint is.
//
// `lark_webhook` and `wecom_webhook` are group robot webhook URLs and receive a card.
// `bark` is a Bark server URL including the device key and receives a short line of text.
// `generic_webhook` receives a JSON document and is the option for anything else.
type ChannelKind string

// ChannelListResponseBody defines model for ChannelListResponseBody.
type ChannelListResponseBody struct {
	Items []ChannelResource `json:"items"`
}

// ChannelResource A registered outbound channel. The endpoint itself is never included.
type ChannelResource struct {
	// ChannelScope `user` is yours alone. `project` is shared with everyone in the project and requires
	// `notification:channels.manage` to register or delete.
	ChannelScope ChannelScope `json:"channel_scope"`

	// ConsecutiveFailures Deliveries that have failed in a row. Reset by a success, and by re-enabling the channel
	ConsecutiveFailures int64     `json:"consecutive_failures"`
	CreatedAt           time.Time `json:"created_at"`

	// DisabledAt When the channel was disabled after repeated failures; null if it is enabled
	DisabledAt *time.Time `json:"disabled_at"`

	// DisabledReason Why it was disabled; null if it is enabled
	DisabledReason *string            `json:"disabled_reason"`
	Id             openapi_types.UUID `json:"id"`

	// Kind The kind of endpoint, which determines both how the message is shaped and what the
	// endpoint is.
	//
	// `lark_webhook` and `wecom_webhook` are group robot webhook URLs and receive a card.
	// `bark` is a Bark server URL including the device key and receives a short line of text.
	// `generic_webhook` receives a JSON document and is the option for anything else.
	Kind ChannelKind `json:"kind"`

	// LastError Why the most recent attempt failed, in the endpoint's own wording; null if the most recent attempt succeeded
	LastError     *string    `json:"last_error"`
	LastFailureAt *time.Time `json:"last_failure_at"`
	LastSuccessAt *time.Time `json:"last_success_at"`
	Name          string     `json:"name"`

	// ProjectId The project this channel is shared with; null for a channel of scope user
	ProjectId *openapi_types.UUID `json:"project_id"`

	// Redacted A partially masked fragment of the endpoint, for visual identification only
	Redacted  string    `json:"redacted"`
	UpdatedAt time.Time `json:"updated_at"`

	// VerifiedAt When the endpoint last acknowledged a verification message; null while it never has, during which the channel is skipped
	VerifiedAt *time.Time `json:"verified_at"`
}

// ChannelScope `user` is yours alone. `project` is shared with everyone in the project and requires
// `notification:channels.manage` to register or delete.
type ChannelScope string

// CreateChannelRequestBody defines model for CreateChannelRequestBody.
type CreateChannelRequestBody struct {
	// ChannelScope `user` is yours alone. `project` is shared with everyone in the project and requires
	// `notification:channels.manage` to register or delete.
	ChannelScope ChannelScope `json:"channel_scope"`

	// Endpoint The address messages are sent to. It must be an https URL reachable over the public
	// internet.
	//
	// For `lark_webhook` and `wecom_webhook` this is the group robot webhook URL. For `bark`
	// it is the push URL including the device key, such as
	// `https://api.day.app/your-device-key`. For `generic_webhook` it is any endpoint
	// accepting a JSON POST.
	Endpoint string `json:"endpoint"`

	// Kind The kind of endpoint, which determines both how the message is shaped and what the
	// endpoint is.
	//
	// `lark_webhook` and `wecom_webhook` are group robot webhook URLs and receive a card.
	// `bark` is a Bark server URL including the device key and receives a short line of text.
	// `generic_webhook` receives a JSON document and is the option for anything else.
	Kind ChannelKind `json:"kind"`

	// Name A display name for this channel
	Name string `json:"name"`

	// Secret The signing secret, where the endpoint has one. It is required for a Lark group robot
	// configured with signature verification, and optional for `generic_webhook`, where it
	// signs the request so the receiver can confirm the message came from this platform.
	//
	// It is not returned by any operation.
	Secret *string `json:"secret,omitempty"`
}

// CredentialSubjectKind What the credential belongs to.
type CredentialSubjectKind string

// CredentialTicketResource A one-time credential, described without being consumed.
type CredentialTicketResource struct {
	// Available True when the credential can still be revealed. False once it has been revealed, has
	// expired, or was withdrawn — in all three cases it is already destroyed
	Available bool `json:"available"`

	// ExpiresAt After this moment the credential is destroyed whether or not it was read
	ExpiresAt time.Time          `json:"expires_at"`
	Id        openapi_types.UUID `json:"id"`

	// RevealedAt When it was revealed; null if it has not been
	RevealedAt *time.Time `json:"revealed_at"`

	// RevokedAt When the service that issued it withdrew it; null if it did not
	RevokedAt *time.Time `json:"revoked_at"`

	// SubjectId The resource the credential belongs to
	SubjectId string `json:"subject_id"`

	// SubjectKind What the credential belongs to.
	SubjectKind CredentialSubjectKind `json:"subject_kind"`
}

// Error defines model for Error.
type Error = externalRef0.Error

// LengthAwarePageNotificationResource defines model for LengthAwarePageNotificationResource.
type LengthAwarePageNotificationResource struct {
	// Items The contents of this page
	Items []NotificationResource `json:"items"`

	// Limit Maximum items per page, echoing the value in the request
	Limit int64 `json:"limit"`

	// Offset Items skipped, echoing the value in the request
	Offset int64 `json:"offset"`

	// Total Total number of matching items, not only those in this page
	Total int64 `json:"total"`

	// UnreadCount Unread notifications in the same scope, not only in this page
	UnreadCount int64 `json:"unread_count"`
}

// MarkNotificationsReadRequestBody An empty list marks everything currently unread as read.
type MarkNotificationsReadRequestBody struct {
	NotificationIds []openapi_types.UUID `json:"notification_ids"`
}

// NotificationAudience Who a type is addressed to. It is a property of the type, so it is the same for every
// occurrence.
//
// `explicit` is whoever the reporting service names, usually the person who asked for the
// action. `account` is one person, independently of any project. `address` is an email
// address belonging to someone who may not have an account yet. `broadcast` is everyone.
type NotificationAudience string

// NotificationChannel Where a notification is delivered.
//
// `inbox` is this API. `email` is the address on your preferences. `webhook` covers every
// outbound channel you have registered — the message is shaped for each one.
type NotificationChannel string

// NotificationKind `notification` is addressed to you. `announcement` was published to the whole platform and
// appears in everyone's inbox.
type NotificationKind string

// NotificationResource One notification, with its text rendered in the language on your preferences at the moment
// it was read.
type NotificationResource struct {
	// ArchivedAt When you archived it; null if you did not
	ArchivedAt *time.Time `json:"archived_at"`

	// Body The text, as a fragment of HTML. Contains formatting and links, no scripts and no styles
	Body string `json:"body"`

	// BodyText The same text without markup, for clients that cannot render HTML
	BodyText  string             `json:"body_text"`
	CreatedAt time.Time          `json:"created_at"`
	Id        openapi_types.UUID `json:"id"`

	// Kind `notification` is addressed to you. `announcement` was published to the whole platform and
	// appears in everyone's inbox.
	Kind NotificationKind `json:"kind"`

	// Locale The language this text was rendered in
	Locale string `json:"locale"`

	// OccurredAt When the event itself happened, which precedes created_at
	OccurredAt time.Time `json:"occurred_at"`

	// Params The facts the text was rendered from, for clients that want to link to the resource the
	// notification is about. The names are documented per type in the catalogue.
	Params map[string]interface{} `json:"params"`

	// ProjectId The project the event belongs to; null for events that belong to no project
	ProjectId *openapi_types.UUID `json:"project_id"`

	// ReadAt When you read it; null if you have not
	ReadAt *time.Time `json:"read_at"`

	// Severity How much attention a notification asks for. It is assigned by the type and by the event,
	// not by the recipient.
	//
	// `critical` announces something that is either irreversible or actively costing the
	// recipient something.
	Severity NotificationSeverity `json:"severity"`

	// Title A single line, without markup
	Title string `json:"title"`

	// Type The type identifier; empty for announcements, which have no type
	Type string `json:"type"`
}

// NotificationScope `all` covers every project you belong to as well as notifications that belong to no
// project. `current_project` covers only the project in the token.
type NotificationScope string

// NotificationSeverity How much attention a notification asks for. It is assigned by the type and by the event,
// not by the recipient.
//
// `critical` announces something that is either irreversible or actively costing the
// recipient something.
type NotificationSeverity string

// NotificationTypeDescriptor One entry in the catalogue of everything the platform can notify about.
type NotificationTypeDescriptor struct {
	// Audience Who a type is addressed to. It is a property of the type, so it is the same for every
	// occurrence.
	//
	// `explicit` is whoever the reporting service names, usually the person who asked for the
	// action. `account` is one person, independently of any project. `address` is an email
	// address belonging to someone who may not have an account yet. `broadcast` is everyone.
	Audience NotificationAudience `json:"audience"`

	// AvailableChannels The channels this type may be delivered on. Configuring any other channel is rejected
	AvailableChannels []NotificationChannel `json:"available_channels"`

	// DefaultChannels The channels this type is delivered on when nothing has been configured
	DefaultChannels []NotificationChannel `json:"default_channels"`

	// Mandatory True when this type cannot be configured or turned off. Such types announce something
	// irreversible, or carry the only route to something that cannot be obtained again
	Mandatory bool `json:"mandatory"`

	// Params The names of the facts this type carries, as they appear on NotificationResource.params
	Params []string `json:"params"`

	// Service The service that reports this type
	Service string `json:"service"`

	// Severity How much attention a notification asks for. It is assigned by the type and by the event,
	// not by the recipient.
	//
	// `critical` announces something that is either irreversible or actively costing the
	// recipient something.
	Severity NotificationSeverity `json:"severity"`
	Type     string               `json:"type"`
}

// NotificationTypeListResponseBody defines model for NotificationTypeListResponseBody.
type NotificationTypeListResponseBody struct {
	Items []NotificationTypeDescriptor `json:"items"`
}

// PreferencesResource defines model for PreferencesResource.
type PreferencesResource struct {
	// EmailAddress Where email is delivered right now, which is the account address unless a verified override replaces it
	EmailAddress string `json:"email_address"`

	// EmailOverride An address to receive email instead of the account address; null when the account address is used
	EmailOverride *string `json:"email_override"`

	// EmailOverrideVerifiedAt When the override was confirmed; null while it is pending, during which email still goes to the account address
	EmailOverrideVerifiedAt *time.Time `json:"email_override_verified_at"`

	// Locale The language notifications are written in, as an IETF language tag
	Locale string `json:"locale"`

	// Timezone The IANA time zone times in notifications are stated in
	Timezone string `json:"timezone"`
}

// RealtimeAuthResource defines model for RealtimeAuthResource.
type RealtimeAuthResource struct {
	// Auth The authorization token for this subscription, passed back to the realtime server by the client library
	Auth string `json:"auth"`
}

// RealtimeConnectionResource Everything needed to open a realtime connection. The connection speaks the Pusher channels
// protocol, so any client library for it works.
type RealtimeConnectionResource struct {
	// AuthPath The path a client posts to while subscribing, relative to this API
	AuthPath string `json:"auth_path"`

	// Cluster The cluster name some client libraries require; empty when the deployment does not use one
	Cluster string `json:"cluster"`

	// Enabled False when realtime delivery is not configured in this deployment. Every other field is empty and clients should not attempt to connect
	Enabled bool   `json:"enabled"`
	Host    string `json:"host"`

	// Key The application key, which is public and identifies the deployment rather than the caller
	Key  string `json:"key"`
	Port int64  `json:"port"`

	// ProjectChannel The channel carrying events about the project in the token; empty when the token names no project
	ProjectChannel string `json:"project_channel"`

	// Secure Whether to connect over TLS
	Secure bool `json:"secure"`

	// UserChannel The channel carrying events addressed to you
	UserChannel string `json:"user_channel"`
}

// RevealedCredentialResource The credential itself. This shape is returned by exactly one operation, and that operation
// succeeds at most once per ticket.
type RevealedCredentialResource struct {
	RevealedAt time.Time `json:"revealed_at"`

	// Secret The credential. It is destroyed as this response is produced and cannot be obtained again
	Secret    string `json:"secret"`
	SubjectId string `json:"subject_id"`

	// SubjectKind What the credential belongs to.
	SubjectKind CredentialSubjectKind `json:"subject_kind"`

	// Username The account the credential belongs to, where the resource has one; null otherwise
	Username *string `json:"username"`
}

// TypePreferenceListResponseBody defines model for TypePreferenceListResponseBody.
type TypePreferenceListResponseBody struct {
	Items []TypePreferenceResource `json:"items"`
}

// TypePreferenceResource defines model for TypePreferenceResource.
type TypePreferenceResource struct {
	// Channels Where this type is delivered
	Channels []NotificationChannel `json:"channels"`

	// Inherited True when nothing has been configured and these are the defaults from the catalogue
	Inherited bool `json:"inherited"`

	// Mandatory True when this type cannot be configured; muting it is rejected
	Mandatory bool `json:"mandatory"`

	// MinSeverity Deliver only at or above this severity. Null delivers every severity. It applies to the
	// channels named here and not to the inbox, which always holds everything
	MinSeverity *NotificationSeverity `json:"min_severity"`

	// Muted True when this type is not delivered anywhere except the inbox
	Muted bool `json:"muted"`

	// ProjectId The project this preference applies to; null when it applies to every project
	ProjectId *openapi_types.UUID `json:"project_id"`
	Type      string              `json:"type"`
}

// UnreadCountResource defines model for UnreadCountResource.
type UnreadCountResource struct {
	UnreadCount int64 `json:"unread_count"`
}

// UpdateChannelRequestBody Fields that are omitted are left alone.
type UpdateChannelRequestBody struct {
	// Enabled Set true to re-enable a channel that was disabled after repeated failures, clearing the failure count
	Enabled *bool   `json:"enabled,omitempty"`
	Name    *string `json:"name,omitempty"`
}

// UpdatePreferencesRequestBody Fields that are omitted are left alone.
type UpdatePreferencesRequestBody struct {
	// EmailOverride An address to receive email instead of the account address. Null returns delivery to the account address
	EmailOverride *string `json:"email_override,omitempty"`

	// Locale An IETF language tag. A language that is not supported is rejected rather than approximated
	Locale *string `json:"locale,omitempty"`

	// Timezone An IANA time zone name, such as Asia/Shanghai
	Timezone *string `json:"timezone,omitempty"`
}

// UpdateTypePreferenceRequestBody States how this type should be delivered, replacing whatever was configured before.
type UpdateTypePreferenceRequestBody struct {
	// Channels Where to deliver this type. An empty list delivers to the inbox only
	Channels []NotificationChannel `json:"channels"`

	// MinSeverity Deliver only at or above this severity. Null delivers every severity
	MinSeverity *NotificationSeverity `json:"min_severity,omitempty"`

	// Muted Stop delivering this type anywhere except the inbox
	Muted *bool `json:"muted,omitempty"`
}

// ChannelId defines model for ChannelId.
type ChannelId = openapi_types.UUID

// NotificationId defines model for NotificationId.
type NotificationId = openapi_types.UUID

// NotificationType defines model for NotificationType.
type NotificationType = string

// PreferenceProjectId Apply to this project alone; omit to apply to every project
type PreferenceProjectId = openapi_types.UUID

// TicketId defines model for TicketId.
type TicketId = openapi_types.UUID

// ListUserChannelsParams defines parameters for ListUserChannels.
type ListUserChannelsParams struct {
	// ChannelScope Return only channels of this scope; omit for both
	ChannelScope *ChannelScope `form:"channel_scope,omitempty" json:"channel_scope,omitempty"`

	// Kind Return only channels of this kind; omit for all kinds
	Kind *ChannelKind `form:"kind,omitempty" json:"kind,omitempty"`
}

// ListNotificationsParams defines parameters for ListNotifications.
type ListNotificationsParams struct {
	// Limit Maximum number of items to return in this page
	Limit *int64 `form:"limit,omitempty" json:"limit,omitempty"`

	// Offset Number of items to skip
	Offset *int64 `form:"offset,omitempty" json:"offset,omitempty"`

	// Scope Which projects to include; defaults to every project
	Scope *NotificationScope `form:"scope,omitempty" json:"scope,omitempty"`

	// Type Return only notifications of this type; omit for all types
	Type *string `form:"type,omitempty" json:"type,omitempty"`

	// MinSeverity Return only notifications at or above this severity; omit for all severities
	MinSeverity *NotificationSeverity `form:"min_severity,omitempty" json:"min_severity,omitempty"`

	// Read Restrict to unread, to read, or to neither; omit for both
	Read *bool `form:"read,omitempty" json:"read,omitempty"`

	// IncludeArchived Include archived notifications; they are excluded by default
	IncludeArchived *bool `form:"include_archived,omitempty" json:"include_archived,omitempty"`
}

// CountUnreadNotificationsParams defines parameters for CountUnreadNotifications.
type CountUnreadNotificationsParams struct {
	// Scope Which projects to include; defaults to every project
	Scope *NotificationScope `form:"scope,omitempty" json:"scope,omitempty"`
}

// ListTypePreferencesParams defines parameters for ListTypePreferences.
type ListTypePreferencesParams struct {
	// ProjectId Return preferences for this project in addition to the account-wide ones; omit for the account-wide ones only
	ProjectId *openapi_types.UUID `form:"project_id,omitempty" json:"project_id,omitempty"`
}

// DeleteTypePreferenceParams defines parameters for DeleteTypePreference.
type DeleteTypePreferenceParams struct {
	// ProjectId Apply to this project alone; omit to apply to every project
	ProjectId *PreferenceProjectId `form:"project_id,omitempty" json:"project_id,omitempty"`
}

// UpdateTypePreferenceParams defines parameters for UpdateTypePreference.
type UpdateTypePreferenceParams struct {
	// ProjectId Apply to this project alone; omit to apply to every project
	ProjectId *PreferenceProjectId `form:"project_id,omitempty" json:"project_id,omitempty"`
}

// CreateUserChannelJSONRequestBody defines body for CreateUserChannel for application/json ContentType.
type CreateUserChannelJSONRequestBody = CreateChannelRequestBody

// UpdateUserChannelJSONRequestBody defines body for UpdateUserChannel for application/json ContentType.
type UpdateUserChannelJSONRequestBody = UpdateChannelRequestBody

// MarkNotificationsReadJSONRequestBody defines body for MarkNotificationsRead for application/json ContentType.
type MarkNotificationsReadJSONRequestBody = MarkNotificationsReadRequestBody

// UpdateNotificationPreferencesJSONRequestBody defines body for UpdateNotificationPreferences for application/json ContentType.
type UpdateNotificationPreferencesJSONRequestBody = UpdatePreferencesRequestBody

// UpdateTypePreferenceJSONRequestBody defines body for UpdateTypePreference for application/json ContentType.
type UpdateTypePreferenceJSONRequestBody = UpdateTypePreferenceRequestBody

// AuthorizeRealtimeChannelJSONRequestBody defines body for AuthorizeRealtimeChannel for application/json ContentType.
type AuthorizeRealtimeChannelJSONRequestBody = AuthorizeRealtimeChannelRequestBody

// RequestEditorFn is the function signature for the RequestEditor callback function
type RequestEditorFn func(ctx context.Context, req *http.Request) error

// Doer performs HTTP requests.
//
// The standard http.Client implements this interface.
type HttpRequestDoer interface {
	Do(req *http.Request) (*http.Response, error)
}

// Client which conforms to the OpenAPI3 specification for this service.
type Client struct {
	// The endpoint of the server conforming to this interface, with scheme,
	// https://api.deepmap.com for example. This can contain a path relative
	// to the server, such as https://api.deepmap.com/dev-test, and all the
	// paths in the swagger spec will be appended to the server.
	Server string

	// Doer for performing requests, typically a *http.Client with any
	// customized settings, such as certificate chains.
	Client HttpRequestDoer

	// A list of callbacks for modifying requests which are generated before sending over
	// the network.
	RequestEditors []RequestEditorFn
}

// ClientOption allows setting custom parameters during construction
type ClientOption func(*Client) error

// Creates a new Client, with reasonable defaults
func NewClient(server string, opts ...ClientOption) (*Client, error) {
	// create a client with sane default values
	client := Client{
		Server: server,
	}
	// mutate client and add all optional params
	for _, o := range opts {
		if err := o(&client); err != nil {
			return nil, err
		}
	}
	// ensure the server URL always has a trailing slash
	if !strings.HasSuffix(client.Server, "/") {
		client.Server += "/"
	}
	// create httpClient, if not already present
	if client.Client == nil {
		client.Client = &http.Client{}
	}
	return &client, nil
}

// WithHTTPClient allows overriding the default Doer, which is
// automatically created using http.Client. This is useful for tests.
func WithHTTPClient(doer HttpRequestDoer) ClientOption {
	return func(c *Client) error {
		c.Client = doer
		return nil
	}
}

// WithRequestEditorFn allows setting up a callback function, which will be
// called right before sending the request. This can be used to mutate the request.
func WithRequestEditorFn(fn RequestEditorFn) ClientOption {
	return func(c *Client) error {
		c.RequestEditors = append(c.RequestEditors, fn)
		return nil
	}
}

// The interface specification for the client above.
type ClientInterface interface {

	// ListUserChannels List outbound channels
	//
	// Returns the channels you have registered, and the channels registered for the project in
	// the token. The address of each is not included.
	//
	// Corresponds with GET /api/v1/channels (the `ListUserChannels` operationId).
	ListUserChannels(ctx context.Context, params *ListUserChannelsParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// CreateUserChannelWithBody Register an outbound channel
	//
	// Registering a channel does not contact it and does not make it deliver anything: a new
	// channel is unverified, and unverified channels are skipped. Call verify to send it one
	// message and make it usable.
	//
	// A `project` channel is shared with everyone in the project and requires
	// `notification:channels.manage`. A `user` channel is yours alone.
	//
	// The endpoint must be reachable over the public internet. Addresses that resolve to private
	// or link-local ranges are rejected with `CHANNEL_ENDPOINT_NOT_PUBLIC`, both here and again
	// at delivery time. Registering an endpoint already registered in the same scope returns
	// `CHANNEL_DUPLICATE`.
	//
	// The submitted endpoint is not returned by this or any other operation.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /api/v1/channels (the `CreateUserChannel` operationId).
	CreateUserChannelWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// CreateUserChannel Register an outbound channel
	//
	// Registering a channel does not contact it and does not make it deliver anything: a new
	// channel is unverified, and unverified channels are skipped. Call verify to send it one
	// message and make it usable.
	//
	// A `project` channel is shared with everyone in the project and requires
	// `notification:channels.manage`. A `user` channel is yours alone.
	//
	// The endpoint must be reachable over the public internet. Addresses that resolve to private
	// or link-local ranges are rejected with `CHANNEL_ENDPOINT_NOT_PUBLIC`, both here and again
	// at delivery time. Registering an endpoint already registered in the same scope returns
	// `CHANNEL_DUPLICATE`.
	//
	// The submitted endpoint is not returned by this or any other operation.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /api/v1/channels (the `CreateUserChannel` operationId).
	CreateUserChannel(ctx context.Context, body CreateUserChannelJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// DeleteUserChannel Delete an outbound channel
	//
	// Deleting a channel stops delivery to it and destroys the stored endpoint. Deliveries
	// already queued for it are abandoned.
	//
	// Corresponds with DELETE /api/v1/channels/{channelId} (the `DeleteUserChannel` operationId).
	DeleteUserChannel(ctx context.Context, channelId ChannelId, reqEditors ...RequestEditorFn) (*http.Response, error)

	// UpdateUserChannelWithBody Rename or re-enable an outbound channel
	//
	// Fields that are omitted are left alone.
	//
	// A channel disabled after repeated failures is re-enabled by setting `enabled` to true,
	// which also clears the failure count. It remains verified, so it starts delivering again
	// immediately — verify it first if the cause of the failures is not known to be resolved.
	//
	// Changing the endpoint is not possible; register a new channel and delete this one.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with PATCH /api/v1/channels/{channelId} (the `UpdateUserChannel` operationId).
	UpdateUserChannelWithBody(ctx context.Context, channelId ChannelId, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// UpdateUserChannel Rename or re-enable an outbound channel
	//
	// Fields that are omitted are left alone.
	//
	// A channel disabled after repeated failures is re-enabled by setting `enabled` to true,
	// which also clears the failure count. It remains verified, so it starts delivering again
	// immediately — verify it first if the cause of the failures is not known to be resolved.
	//
	// Changing the endpoint is not possible; register a new channel and delete this one.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with PATCH /api/v1/channels/{channelId} (the `UpdateUserChannel` operationId).
	UpdateUserChannel(ctx context.Context, channelId ChannelId, body UpdateUserChannelJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// VerifyUserChannel Verify an outbound channel
	//
	// Sends one message to the channel and marks it verified if the endpoint accepted it.
	//
	// **The endpoint accepting the message is not proof that it arrived where it was meant to.**
	// A webhook URL belonging to the wrong group answers exactly the same way. Confirm the
	// message appeared before relying on the channel.
	//
	// A channel that fails verification is not marked verified and continues to be skipped. The
	// reason is reported in `last_error`; it is the provider's own wording and is not translated.
	//
	// Corresponds with POST /api/v1/channels/{channelId}/verify (the `VerifyUserChannel` operationId).
	VerifyUserChannel(ctx context.Context, channelId ChannelId, reqEditors ...RequestEditorFn) (*http.Response, error)

	// DescribeCredentialTicket Describe a one-time credential
	//
	// Reports whether the credential is still available without consuming it. It never contains
	// the credential itself.
	//
	// A ticket addressed to someone else returns `CREDENTIAL_TICKET_NOT_FOUND`, the same answer
	// as one that does not exist.
	//
	// Corresponds with GET /api/v1/credentials/{ticketId} (the `DescribeCredentialTicket` operationId).
	DescribeCredentialTicket(ctx context.Context, ticketId TicketId, reqEditors ...RequestEditorFn) (*http.Response, error)

	// RevealCredential Reveal a one-time credential
	//
	// Returns the credential and destroys it in the same step. **This is the only operation that
	// ever returns it, and it succeeds only once.**
	//
	// Every subsequent request returns `CREDENTIAL_TICKET_CONSUMED`, including one issued
	// concurrently with the first: exactly one caller receives the credential.
	//
	// A ticket that has expired returns `CREDENTIAL_TICKET_EXPIRED`, and one withdrawn by the
	// service that created it returns `CREDENTIAL_TICKET_REVOKED`. In all three cases the
	// credential is already gone and cannot be recovered; obtain a new one from the service that
	// owns the resource.
	//
	// This operation is a POST rather than a GET because a GET would be followed by link
	// scanners in mail clients and by browser prefetching, either of which would consume the
	// credential before the recipient opened it.
	//
	// Corresponds with POST /api/v1/credentials/{ticketId}/reveal (the `RevealCredential` operationId).
	RevealCredential(ctx context.Context, ticketId TicketId, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ListNotificationTypes List every notification type
	//
	// The catalogue of everything this platform can notify about, with the default delivery for
	// each type and whether it can be configured. It is the source for a settings interface.
	//
	// This operation does not require authentication: the catalogue is the same for everyone and
	// describes the product rather than any account.
	//
	// Corresponds with GET /api/v1/notification-types (the `ListNotificationTypes` operationId).
	ListNotificationTypes(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ListNotifications List notifications addressed to you
	//
	// Returns notifications in reverse chronological order, newest first.
	//
	// Announcements published to the whole platform appear here alongside notifications
	// addressed to you individually and are distinguished by `kind`. They are read and archived
	// through the same operations.
	//
	// Corresponds with GET /api/v1/notifications (the `ListNotifications` operationId).
	ListNotifications(ctx context.Context, params *ListNotificationsParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// MarkNotificationsReadWithBody Mark several notifications as read
	//
	// Marks every listed notification as read. Notifications already read are left alone, and the
	// operation succeeds whether or not any of them changed.
	//
	// Passing an empty list marks everything currently unread as read.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /api/v1/notifications/read (the `MarkNotificationsRead` operationId).
	MarkNotificationsReadWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// MarkNotificationsRead Mark several notifications as read
	//
	// Marks every listed notification as read. Notifications already read are left alone, and the
	// operation succeeds whether or not any of them changed.
	//
	// Passing an empty list marks everything currently unread as read.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /api/v1/notifications/read (the `MarkNotificationsRead` operationId).
	MarkNotificationsRead(ctx context.Context, body MarkNotificationsReadJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// CountUnreadNotifications Count unread notifications
	//
	// Returns the number of unread notifications. This is the value behind the badge in the
	// console, and it is also carried on every realtime event, so it does not normally need to be
	// requested on its own.
	//
	// Corresponds with GET /api/v1/notifications/unread-count (the `CountUnreadNotifications` operationId).
	CountUnreadNotifications(ctx context.Context, params *CountUnreadNotificationsParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetNotification Get one notification
	//
	// A notification addressed to someone else returns `NOTIFICATION_NOT_FOUND`, the same answer
	// as one that does not exist.
	//
	// Corresponds with GET /api/v1/notifications/{notificationId} (the `GetNotification` operationId).
	GetNotification(ctx context.Context, notificationId NotificationId, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ArchiveNotification Archive one notification
	//
	// Archiving removes a notification from the default listing without deleting it; pass
	// `include_archived` to see it again. Archiving also marks it read.
	//
	// Corresponds with POST /api/v1/notifications/{notificationId}/archive (the `ArchiveNotification` operationId).
	ArchiveNotification(ctx context.Context, notificationId NotificationId, reqEditors ...RequestEditorFn) (*http.Response, error)

	// MarkNotificationRead Mark one notification as read
	//
	// Marking a notification that is already read succeeds and changes nothing.
	//
	// Corresponds with POST /api/v1/notifications/{notificationId}/read (the `MarkNotificationRead` operationId).
	MarkNotificationRead(ctx context.Context, notificationId NotificationId, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetNotificationPreferences Get your delivery preferences
	//
	// Returns the account-wide settings: the language notifications are written in, the time zone
	// times are stated in, and the address email is delivered to.
	//
	// A person who has never configured anything receives the defaults rather than an error.
	//
	// Corresponds with GET /api/v1/preferences (the `GetNotificationPreferences` operationId).
	GetNotificationPreferences(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error)

	// UpdateNotificationPreferencesWithBody Update your delivery preferences
	//
	// Fields that are omitted are left alone.
	//
	// Setting `email_override` starts verification of that address: email continues to be
	// delivered to the address on the account until the new one is confirmed. Setting it to null
	// returns delivery to the account address.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with PATCH /api/v1/preferences (the `UpdateNotificationPreferences` operationId).
	UpdateNotificationPreferencesWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// UpdateNotificationPreferences Update your delivery preferences
	//
	// Fields that are omitted are left alone.
	//
	// Setting `email_override` starts verification of that address: email continues to be
	// delivered to the address on the account until the new one is confirmed. Setting it to null
	// returns delivery to the account address.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with PATCH /api/v1/preferences (the `UpdateNotificationPreferences` operationId).
	UpdateNotificationPreferences(ctx context.Context, body UpdateNotificationPreferencesJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ListTypePreferences List your per-type preferences
	//
	// Returns one entry per notification type, whether or not it has been configured. Entries
	// that have never been configured are reported with `inherited: true` and carry the defaults
	// from the catalogue.
	//
	// A preference applies either to one project or to every project. Both are returned; the one
	// naming a project takes precedence for that project.
	//
	// Corresponds with GET /api/v1/preferences/types (the `ListTypePreferences` operationId).
	ListTypePreferences(ctx context.Context, params *ListTypePreferencesParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// DeleteTypePreference Restore the default for one type
	//
	// Removes the configured preference so the type follows the default in the catalogue again.
	//
	// **This does not stop the type from being delivered.** To stop receiving it, set `muted`
	// instead. Deleting a preference that was never configured succeeds and changes nothing.
	//
	// Corresponds with DELETE /api/v1/preferences/types/{type} (the `DeleteTypePreference` operationId).
	DeleteTypePreference(ctx context.Context, pType NotificationType, params *DeleteTypePreferenceParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// UpdateTypePreferenceWithBody Set the preference for one type
	//
	// States how this type should be delivered, replacing whatever was configured before.
	//
	// Omitting `project_id` configures every project. Naming one configures that project alone
	// and takes precedence there.
	//
	// Configuring a type marked `mandatory` returns `NOTIFICATION_TYPE_MANDATORY`. Naming a
	// channel the type does not support returns `NOTIFICATION_CHANNEL_UNSUPPORTED`.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with PUT /api/v1/preferences/types/{type} (the `UpdateTypePreference` operationId).
	UpdateTypePreferenceWithBody(ctx context.Context, pType NotificationType, params *UpdateTypePreferenceParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// UpdateTypePreference Set the preference for one type
	//
	// States how this type should be delivered, replacing whatever was configured before.
	//
	// Omitting `project_id` configures every project. Naming one configures that project alone
	// and takes precedence there.
	//
	// Configuring a type marked `mandatory` returns `NOTIFICATION_TYPE_MANDATORY`. Naming a
	// channel the type does not support returns `NOTIFICATION_CHANNEL_UNSUPPORTED`.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with PUT /api/v1/preferences/types/{type} (the `UpdateTypePreference` operationId).
	UpdateTypePreference(ctx context.Context, pType NotificationType, params *UpdateTypePreferenceParams, body UpdateTypePreferenceJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// DescribeRealtimeConnection Describe the realtime connection
	//
	// Returns everything needed to open a realtime connection, and the names of the channels to
	// subscribe to. The parameters are not constants: they differ between environments, so they
	// must be requested rather than compiled in.
	//
	// Realtime delivery is an optimisation. Everything it announces is also readable through the
	// listing operations, so a client that cannot connect stays correct by polling or by
	// refreshing.
	//
	// Corresponds with GET /api/v1/realtime (the `DescribeRealtimeConnection` operationId).
	DescribeRealtimeConnection(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error)

	// AuthorizeRealtimeChannelWithBody Authorize a realtime channel subscription
	//
	// Authorizes one subscription. Realtime clients call this on their own while subscribing; it
	// is not normally called directly.
	//
	// Only two channels are authorized: the one naming you, and the one naming the project in
	// the token. Any other channel returns `REALTIME_CHANNEL_FORBIDDEN`.
	//
	// **Authorization is evaluated once, when the subscription is established.** A subscription
	// already open is not revoked when project membership changes, so realtime events carry
	// identifiers and counts only, never the contents of a notification.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /api/v1/realtime/auth (the `AuthorizeRealtimeChannel` operationId).
	AuthorizeRealtimeChannelWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// AuthorizeRealtimeChannel Authorize a realtime channel subscription
	//
	// Authorizes one subscription. Realtime clients call this on their own while subscribing; it
	// is not normally called directly.
	//
	// Only two channels are authorized: the one naming you, and the one naming the project in
	// the token. Any other channel returns `REALTIME_CHANNEL_FORBIDDEN`.
	//
	// **Authorization is evaluated once, when the subscription is established.** A subscription
	// already open is not revoked when project membership changes, so realtime events carry
	// identifiers and counts only, never the contents of a notification.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /api/v1/realtime/auth (the `AuthorizeRealtimeChannel` operationId).
	AuthorizeRealtimeChannel(ctx context.Context, body AuthorizeRealtimeChannelJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)
}

// ListUserChannels List outbound channels
//
// Returns the channels you have registered, and the channels registered for the project in
// the token. The address of each is not included.
//
// Corresponds with GET /api/v1/channels (the `ListUserChannels` operationId).
func (c *Client) ListUserChannels(ctx context.Context, params *ListUserChannelsParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewListUserChannelsRequest(c.Server, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// CreateUserChannelWithBody Register an outbound channel
//
// Registering a channel does not contact it and does not make it deliver anything: a new
// channel is unverified, and unverified channels are skipped. Call verify to send it one
// message and make it usable.
//
// A `project` channel is shared with everyone in the project and requires
// `notification:channels.manage`. A `user` channel is yours alone.
//
// The endpoint must be reachable over the public internet. Addresses that resolve to private
// or link-local ranges are rejected with `CHANNEL_ENDPOINT_NOT_PUBLIC`, both here and again
// at delivery time. Registering an endpoint already registered in the same scope returns
// `CHANNEL_DUPLICATE`.
//
// The submitted endpoint is not returned by this or any other operation.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /api/v1/channels (the `CreateUserChannel` operationId).
func (c *Client) CreateUserChannelWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewCreateUserChannelRequestWithBody(c.Server, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// CreateUserChannel Register an outbound channel
//
// Registering a channel does not contact it and does not make it deliver anything: a new
// channel is unverified, and unverified channels are skipped. Call verify to send it one
// message and make it usable.
//
// A `project` channel is shared with everyone in the project and requires
// `notification:channels.manage`. A `user` channel is yours alone.
//
// The endpoint must be reachable over the public internet. Addresses that resolve to private
// or link-local ranges are rejected with `CHANNEL_ENDPOINT_NOT_PUBLIC`, both here and again
// at delivery time. Registering an endpoint already registered in the same scope returns
// `CHANNEL_DUPLICATE`.
//
// The submitted endpoint is not returned by this or any other operation.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /api/v1/channels (the `CreateUserChannel` operationId).
func (c *Client) CreateUserChannel(ctx context.Context, body CreateUserChannelJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewCreateUserChannelRequest(c.Server, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// DeleteUserChannel Delete an outbound channel
//
// Deleting a channel stops delivery to it and destroys the stored endpoint. Deliveries
// already queued for it are abandoned.
//
// Corresponds with DELETE /api/v1/channels/{channelId} (the `DeleteUserChannel` operationId).
func (c *Client) DeleteUserChannel(ctx context.Context, channelId ChannelId, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewDeleteUserChannelRequest(c.Server, channelId)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// UpdateUserChannelWithBody Rename or re-enable an outbound channel
//
// Fields that are omitted are left alone.
//
// A channel disabled after repeated failures is re-enabled by setting `enabled` to true,
// which also clears the failure count. It remains verified, so it starts delivering again
// immediately — verify it first if the cause of the failures is not known to be resolved.
//
// Changing the endpoint is not possible; register a new channel and delete this one.
//
// Takes any type of body and a specified content type.
//
// Corresponds with PATCH /api/v1/channels/{channelId} (the `UpdateUserChannel` operationId).
func (c *Client) UpdateUserChannelWithBody(ctx context.Context, channelId ChannelId, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewUpdateUserChannelRequestWithBody(c.Server, channelId, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// UpdateUserChannel Rename or re-enable an outbound channel
//
// Fields that are omitted are left alone.
//
// A channel disabled after repeated failures is re-enabled by setting `enabled` to true,
// which also clears the failure count. It remains verified, so it starts delivering again
// immediately — verify it first if the cause of the failures is not known to be resolved.
//
// Changing the endpoint is not possible; register a new channel and delete this one.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with PATCH /api/v1/channels/{channelId} (the `UpdateUserChannel` operationId).
func (c *Client) UpdateUserChannel(ctx context.Context, channelId ChannelId, body UpdateUserChannelJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewUpdateUserChannelRequest(c.Server, channelId, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// VerifyUserChannel Verify an outbound channel
//
// Sends one message to the channel and marks it verified if the endpoint accepted it.
//
// **The endpoint accepting the message is not proof that it arrived where it was meant to.**
// A webhook URL belonging to the wrong group answers exactly the same way. Confirm the
// message appeared before relying on the channel.
//
// A channel that fails verification is not marked verified and continues to be skipped. The
// reason is reported in `last_error`; it is the provider's own wording and is not translated.
//
// Corresponds with POST /api/v1/channels/{channelId}/verify (the `VerifyUserChannel` operationId).
func (c *Client) VerifyUserChannel(ctx context.Context, channelId ChannelId, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewVerifyUserChannelRequest(c.Server, channelId)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// DescribeCredentialTicket Describe a one-time credential
//
// Reports whether the credential is still available without consuming it. It never contains
// the credential itself.
//
// A ticket addressed to someone else returns `CREDENTIAL_TICKET_NOT_FOUND`, the same answer
// as one that does not exist.
//
// Corresponds with GET /api/v1/credentials/{ticketId} (the `DescribeCredentialTicket` operationId).
func (c *Client) DescribeCredentialTicket(ctx context.Context, ticketId TicketId, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewDescribeCredentialTicketRequest(c.Server, ticketId)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// RevealCredential Reveal a one-time credential
//
// Returns the credential and destroys it in the same step. **This is the only operation that
// ever returns it, and it succeeds only once.**
//
// Every subsequent request returns `CREDENTIAL_TICKET_CONSUMED`, including one issued
// concurrently with the first: exactly one caller receives the credential.
//
// A ticket that has expired returns `CREDENTIAL_TICKET_EXPIRED`, and one withdrawn by the
// service that created it returns `CREDENTIAL_TICKET_REVOKED`. In all three cases the
// credential is already gone and cannot be recovered; obtain a new one from the service that
// owns the resource.
//
// This operation is a POST rather than a GET because a GET would be followed by link
// scanners in mail clients and by browser prefetching, either of which would consume the
// credential before the recipient opened it.
//
// Corresponds with POST /api/v1/credentials/{ticketId}/reveal (the `RevealCredential` operationId).
func (c *Client) RevealCredential(ctx context.Context, ticketId TicketId, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewRevealCredentialRequest(c.Server, ticketId)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ListNotificationTypes List every notification type
//
// The catalogue of everything this platform can notify about, with the default delivery for
// each type and whether it can be configured. It is the source for a settings interface.
//
// This operation does not require authentication: the catalogue is the same for everyone and
// describes the product rather than any account.
//
// Corresponds with GET /api/v1/notification-types (the `ListNotificationTypes` operationId).
func (c *Client) ListNotificationTypes(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewListNotificationTypesRequest(c.Server)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ListNotifications List notifications addressed to you
//
// Returns notifications in reverse chronological order, newest first.
//
// Announcements published to the whole platform appear here alongside notifications
// addressed to you individually and are distinguished by `kind`. They are read and archived
// through the same operations.
//
// Corresponds with GET /api/v1/notifications (the `ListNotifications` operationId).
func (c *Client) ListNotifications(ctx context.Context, params *ListNotificationsParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewListNotificationsRequest(c.Server, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// MarkNotificationsReadWithBody Mark several notifications as read
//
// Marks every listed notification as read. Notifications already read are left alone, and the
// operation succeeds whether or not any of them changed.
//
// Passing an empty list marks everything currently unread as read.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /api/v1/notifications/read (the `MarkNotificationsRead` operationId).
func (c *Client) MarkNotificationsReadWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewMarkNotificationsReadRequestWithBody(c.Server, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// MarkNotificationsRead Mark several notifications as read
//
// Marks every listed notification as read. Notifications already read are left alone, and the
// operation succeeds whether or not any of them changed.
//
// Passing an empty list marks everything currently unread as read.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /api/v1/notifications/read (the `MarkNotificationsRead` operationId).
func (c *Client) MarkNotificationsRead(ctx context.Context, body MarkNotificationsReadJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewMarkNotificationsReadRequest(c.Server, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// CountUnreadNotifications Count unread notifications
//
// Returns the number of unread notifications. This is the value behind the badge in the
// console, and it is also carried on every realtime event, so it does not normally need to be
// requested on its own.
//
// Corresponds with GET /api/v1/notifications/unread-count (the `CountUnreadNotifications` operationId).
func (c *Client) CountUnreadNotifications(ctx context.Context, params *CountUnreadNotificationsParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewCountUnreadNotificationsRequest(c.Server, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetNotification Get one notification
//
// A notification addressed to someone else returns `NOTIFICATION_NOT_FOUND`, the same answer
// as one that does not exist.
//
// Corresponds with GET /api/v1/notifications/{notificationId} (the `GetNotification` operationId).
func (c *Client) GetNotification(ctx context.Context, notificationId NotificationId, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetNotificationRequest(c.Server, notificationId)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ArchiveNotification Archive one notification
//
// Archiving removes a notification from the default listing without deleting it; pass
// `include_archived` to see it again. Archiving also marks it read.
//
// Corresponds with POST /api/v1/notifications/{notificationId}/archive (the `ArchiveNotification` operationId).
func (c *Client) ArchiveNotification(ctx context.Context, notificationId NotificationId, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewArchiveNotificationRequest(c.Server, notificationId)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// MarkNotificationRead Mark one notification as read
//
// Marking a notification that is already read succeeds and changes nothing.
//
// Corresponds with POST /api/v1/notifications/{notificationId}/read (the `MarkNotificationRead` operationId).
func (c *Client) MarkNotificationRead(ctx context.Context, notificationId NotificationId, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewMarkNotificationReadRequest(c.Server, notificationId)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetNotificationPreferences Get your delivery preferences
//
// Returns the account-wide settings: the language notifications are written in, the time zone
// times are stated in, and the address email is delivered to.
//
// A person who has never configured anything receives the defaults rather than an error.
//
// Corresponds with GET /api/v1/preferences (the `GetNotificationPreferences` operationId).
func (c *Client) GetNotificationPreferences(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetNotificationPreferencesRequest(c.Server)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// UpdateNotificationPreferencesWithBody Update your delivery preferences
//
// Fields that are omitted are left alone.
//
// Setting `email_override` starts verification of that address: email continues to be
// delivered to the address on the account until the new one is confirmed. Setting it to null
// returns delivery to the account address.
//
// Takes any type of body and a specified content type.
//
// Corresponds with PATCH /api/v1/preferences (the `UpdateNotificationPreferences` operationId).
func (c *Client) UpdateNotificationPreferencesWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewUpdateNotificationPreferencesRequestWithBody(c.Server, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// UpdateNotificationPreferences Update your delivery preferences
//
// Fields that are omitted are left alone.
//
// Setting `email_override` starts verification of that address: email continues to be
// delivered to the address on the account until the new one is confirmed. Setting it to null
// returns delivery to the account address.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with PATCH /api/v1/preferences (the `UpdateNotificationPreferences` operationId).
func (c *Client) UpdateNotificationPreferences(ctx context.Context, body UpdateNotificationPreferencesJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewUpdateNotificationPreferencesRequest(c.Server, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ListTypePreferences List your per-type preferences
//
// Returns one entry per notification type, whether or not it has been configured. Entries
// that have never been configured are reported with `inherited: true` and carry the defaults
// from the catalogue.
//
// A preference applies either to one project or to every project. Both are returned; the one
// naming a project takes precedence for that project.
//
// Corresponds with GET /api/v1/preferences/types (the `ListTypePreferences` operationId).
func (c *Client) ListTypePreferences(ctx context.Context, params *ListTypePreferencesParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewListTypePreferencesRequest(c.Server, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// DeleteTypePreference Restore the default for one type
//
// Removes the configured preference so the type follows the default in the catalogue again.
//
// **This does not stop the type from being delivered.** To stop receiving it, set `muted`
// instead. Deleting a preference that was never configured succeeds and changes nothing.
//
// Corresponds with DELETE /api/v1/preferences/types/{type} (the `DeleteTypePreference` operationId).
func (c *Client) DeleteTypePreference(ctx context.Context, pType NotificationType, params *DeleteTypePreferenceParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewDeleteTypePreferenceRequest(c.Server, pType, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// UpdateTypePreferenceWithBody Set the preference for one type
//
// States how this type should be delivered, replacing whatever was configured before.
//
// Omitting `project_id` configures every project. Naming one configures that project alone
// and takes precedence there.
//
// Configuring a type marked `mandatory` returns `NOTIFICATION_TYPE_MANDATORY`. Naming a
// channel the type does not support returns `NOTIFICATION_CHANNEL_UNSUPPORTED`.
//
// Takes any type of body and a specified content type.
//
// Corresponds with PUT /api/v1/preferences/types/{type} (the `UpdateTypePreference` operationId).
func (c *Client) UpdateTypePreferenceWithBody(ctx context.Context, pType NotificationType, params *UpdateTypePreferenceParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewUpdateTypePreferenceRequestWithBody(c.Server, pType, params, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// UpdateTypePreference Set the preference for one type
//
// States how this type should be delivered, replacing whatever was configured before.
//
// Omitting `project_id` configures every project. Naming one configures that project alone
// and takes precedence there.
//
// Configuring a type marked `mandatory` returns `NOTIFICATION_TYPE_MANDATORY`. Naming a
// channel the type does not support returns `NOTIFICATION_CHANNEL_UNSUPPORTED`.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with PUT /api/v1/preferences/types/{type} (the `UpdateTypePreference` operationId).
func (c *Client) UpdateTypePreference(ctx context.Context, pType NotificationType, params *UpdateTypePreferenceParams, body UpdateTypePreferenceJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewUpdateTypePreferenceRequest(c.Server, pType, params, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// DescribeRealtimeConnection Describe the realtime connection
//
// Returns everything needed to open a realtime connection, and the names of the channels to
// subscribe to. The parameters are not constants: they differ between environments, so they
// must be requested rather than compiled in.
//
// Realtime delivery is an optimisation. Everything it announces is also readable through the
// listing operations, so a client that cannot connect stays correct by polling or by
// refreshing.
//
// Corresponds with GET /api/v1/realtime (the `DescribeRealtimeConnection` operationId).
func (c *Client) DescribeRealtimeConnection(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewDescribeRealtimeConnectionRequest(c.Server)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// AuthorizeRealtimeChannelWithBody Authorize a realtime channel subscription
//
// Authorizes one subscription. Realtime clients call this on their own while subscribing; it
// is not normally called directly.
//
// Only two channels are authorized: the one naming you, and the one naming the project in
// the token. Any other channel returns `REALTIME_CHANNEL_FORBIDDEN`.
//
// **Authorization is evaluated once, when the subscription is established.** A subscription
// already open is not revoked when project membership changes, so realtime events carry
// identifiers and counts only, never the contents of a notification.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /api/v1/realtime/auth (the `AuthorizeRealtimeChannel` operationId).
func (c *Client) AuthorizeRealtimeChannelWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewAuthorizeRealtimeChannelRequestWithBody(c.Server, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// AuthorizeRealtimeChannel Authorize a realtime channel subscription
//
// Authorizes one subscription. Realtime clients call this on their own while subscribing; it
// is not normally called directly.
//
// Only two channels are authorized: the one naming you, and the one naming the project in
// the token. Any other channel returns `REALTIME_CHANNEL_FORBIDDEN`.
//
// **Authorization is evaluated once, when the subscription is established.** A subscription
// already open is not revoked when project membership changes, so realtime events carry
// identifiers and counts only, never the contents of a notification.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /api/v1/realtime/auth (the `AuthorizeRealtimeChannel` operationId).
func (c *Client) AuthorizeRealtimeChannel(ctx context.Context, body AuthorizeRealtimeChannelJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewAuthorizeRealtimeChannelRequest(c.Server, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// NewListUserChannelsRequest constructs an http.Request for the ListUserChannels method
func NewListUserChannelsRequest(server string, params *ListUserChannelsParams) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/api/v1/channels")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if params.ChannelScope != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", false, "channel_scope", *params.ChannelScope, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Kind != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", false, "kind", *params.Kind, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewCreateUserChannelRequest calls the generic CreateUserChannel builder with application/json body
func NewCreateUserChannelRequest(server string, body CreateUserChannelJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewCreateUserChannelRequestWithBody(server, "application/json", bodyReader)
}

// NewCreateUserChannelRequestWithBody constructs an http.Request for the CreateUserChannel method, with any body, and a specified content type
func NewCreateUserChannelRequestWithBody(server string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/api/v1/channels")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewDeleteUserChannelRequest constructs an http.Request for the DeleteUserChannel method
func NewDeleteUserChannelRequest(server string, channelId ChannelId) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "channelId", channelId, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: "uuid"})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/api/v1/channels/%s", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodDelete, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewUpdateUserChannelRequest calls the generic UpdateUserChannel builder with application/json body
func NewUpdateUserChannelRequest(server string, channelId ChannelId, body UpdateUserChannelJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewUpdateUserChannelRequestWithBody(server, channelId, "application/json", bodyReader)
}

// NewUpdateUserChannelRequestWithBody constructs an http.Request for the UpdateUserChannel method, with any body, and a specified content type
func NewUpdateUserChannelRequestWithBody(server string, channelId ChannelId, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "channelId", channelId, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: "uuid"})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/api/v1/channels/%s", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPatch, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewVerifyUserChannelRequest constructs an http.Request for the VerifyUserChannel method
func NewVerifyUserChannelRequest(server string, channelId ChannelId) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "channelId", channelId, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: "uuid"})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/api/v1/channels/%s/verify", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewDescribeCredentialTicketRequest constructs an http.Request for the DescribeCredentialTicket method
func NewDescribeCredentialTicketRequest(server string, ticketId TicketId) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "ticketId", ticketId, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: "uuid"})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/api/v1/credentials/%s", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewRevealCredentialRequest constructs an http.Request for the RevealCredential method
func NewRevealCredentialRequest(server string, ticketId TicketId) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "ticketId", ticketId, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: "uuid"})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/api/v1/credentials/%s/reveal", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewListNotificationTypesRequest constructs an http.Request for the ListNotificationTypes method
func NewListNotificationTypesRequest(server string) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/api/v1/notification-types")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewListNotificationsRequest constructs an http.Request for the ListNotifications method
func NewListNotificationsRequest(server string, params *ListNotificationsParams) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/api/v1/notifications")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if params.Limit != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", false, "limit", *params.Limit, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "integer", Format: "int64"}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Offset != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", false, "offset", *params.Offset, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "integer", Format: "int64"}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Scope != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", false, "scope", *params.Scope, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Type != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", false, "type", *params.Type, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.MinSeverity != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", false, "min_severity", *params.MinSeverity, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Read != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", false, "read", *params.Read, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "boolean", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.IncludeArchived != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", false, "include_archived", *params.IncludeArchived, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "boolean", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewMarkNotificationsReadRequest calls the generic MarkNotificationsRead builder with application/json body
func NewMarkNotificationsReadRequest(server string, body MarkNotificationsReadJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewMarkNotificationsReadRequestWithBody(server, "application/json", bodyReader)
}

// NewMarkNotificationsReadRequestWithBody constructs an http.Request for the MarkNotificationsRead method, with any body, and a specified content type
func NewMarkNotificationsReadRequestWithBody(server string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/api/v1/notifications/read")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewCountUnreadNotificationsRequest constructs an http.Request for the CountUnreadNotifications method
func NewCountUnreadNotificationsRequest(server string, params *CountUnreadNotificationsParams) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/api/v1/notifications/unread-count")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if params.Scope != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", false, "scope", *params.Scope, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewGetNotificationRequest constructs an http.Request for the GetNotification method
func NewGetNotificationRequest(server string, notificationId NotificationId) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "notificationId", notificationId, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: "uuid"})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/api/v1/notifications/%s", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewArchiveNotificationRequest constructs an http.Request for the ArchiveNotification method
func NewArchiveNotificationRequest(server string, notificationId NotificationId) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "notificationId", notificationId, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: "uuid"})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/api/v1/notifications/%s/archive", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewMarkNotificationReadRequest constructs an http.Request for the MarkNotificationRead method
func NewMarkNotificationReadRequest(server string, notificationId NotificationId) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "notificationId", notificationId, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: "uuid"})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/api/v1/notifications/%s/read", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewGetNotificationPreferencesRequest constructs an http.Request for the GetNotificationPreferences method
func NewGetNotificationPreferencesRequest(server string) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/api/v1/preferences")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewUpdateNotificationPreferencesRequest calls the generic UpdateNotificationPreferences builder with application/json body
func NewUpdateNotificationPreferencesRequest(server string, body UpdateNotificationPreferencesJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewUpdateNotificationPreferencesRequestWithBody(server, "application/json", bodyReader)
}

// NewUpdateNotificationPreferencesRequestWithBody constructs an http.Request for the UpdateNotificationPreferences method, with any body, and a specified content type
func NewUpdateNotificationPreferencesRequestWithBody(server string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/api/v1/preferences")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPatch, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewListTypePreferencesRequest constructs an http.Request for the ListTypePreferences method
func NewListTypePreferencesRequest(server string, params *ListTypePreferencesParams) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/api/v1/preferences/types")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if params.ProjectId != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", false, "project_id", *params.ProjectId, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: "uuid"}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewDeleteTypePreferenceRequest constructs an http.Request for the DeleteTypePreference method
func NewDeleteTypePreferenceRequest(server string, pType NotificationType, params *DeleteTypePreferenceParams) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "type", pType, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/api/v1/preferences/types/%s", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if params.ProjectId != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", false, "project_id", *params.ProjectId, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: "uuid"}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodDelete, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewUpdateTypePreferenceRequest calls the generic UpdateTypePreference builder with application/json body
func NewUpdateTypePreferenceRequest(server string, pType NotificationType, params *UpdateTypePreferenceParams, body UpdateTypePreferenceJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewUpdateTypePreferenceRequestWithBody(server, pType, params, "application/json", bodyReader)
}

// NewUpdateTypePreferenceRequestWithBody constructs an http.Request for the UpdateTypePreference method, with any body, and a specified content type
func NewUpdateTypePreferenceRequestWithBody(server string, pType NotificationType, params *UpdateTypePreferenceParams, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "type", pType, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/api/v1/preferences/types/%s", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if params.ProjectId != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", false, "project_id", *params.ProjectId, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: "uuid"}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodPut, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewDescribeRealtimeConnectionRequest constructs an http.Request for the DescribeRealtimeConnection method
func NewDescribeRealtimeConnectionRequest(server string) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/api/v1/realtime")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewAuthorizeRealtimeChannelRequest calls the generic AuthorizeRealtimeChannel builder with application/json body
func NewAuthorizeRealtimeChannelRequest(server string, body AuthorizeRealtimeChannelJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewAuthorizeRealtimeChannelRequestWithBody(server, "application/json", bodyReader)
}

// NewAuthorizeRealtimeChannelRequestWithBody constructs an http.Request for the AuthorizeRealtimeChannel method, with any body, and a specified content type
func NewAuthorizeRealtimeChannelRequestWithBody(server string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/api/v1/realtime/auth")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

func (c *Client) applyEditors(ctx context.Context, req *http.Request, additionalEditors []RequestEditorFn) error {
	for _, r := range c.RequestEditors {
		if err := r(ctx, req); err != nil {
			return err
		}
	}
	for _, r := range additionalEditors {
		if err := r(ctx, req); err != nil {
			return err
		}
	}
	return nil
}

// ClientWithResponses builds on ClientInterface to offer response payloads
type ClientWithResponses struct {
	ClientInterface
}

// NewClientWithResponses creates a new ClientWithResponses, which wraps
// Client with return type handling
func NewClientWithResponses(server string, opts ...ClientOption) (*ClientWithResponses, error) {
	client, err := NewClient(server, opts...)
	if err != nil {
		return nil, err
	}
	return &ClientWithResponses{client}, nil
}

// WithBaseURL overrides the baseURL.
func WithBaseURL(baseURL string) ClientOption {
	return func(c *Client) error {
		newBaseURL, err := url.Parse(baseURL)
		if err != nil {
			return err
		}
		c.Server = newBaseURL.String()
		return nil
	}
}

// ClientWithResponsesInterface is the interface specification for the client with responses above.
type ClientWithResponsesInterface interface {

	// ListUserChannelsWithResponse List outbound channels
	//
	// Returns the channels you have registered, and the channels registered for the project in
	// the token. The address of each is not included.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /api/v1/channels (the `ListUserChannels` operationId).
	ListUserChannelsWithResponse(ctx context.Context, params *ListUserChannelsParams, reqEditors ...RequestEditorFn) (*ListUserChannelsResponse, error)

	// CreateUserChannelWithBodyWithResponse Register an outbound channel
	//
	// Registering a channel does not contact it and does not make it deliver anything: a new
	// channel is unverified, and unverified channels are skipped. Call verify to send it one
	// message and make it usable.
	//
	// A `project` channel is shared with everyone in the project and requires
	// `notification:channels.manage`. A `user` channel is yours alone.
	//
	// The endpoint must be reachable over the public internet. Addresses that resolve to private
	// or link-local ranges are rejected with `CHANNEL_ENDPOINT_NOT_PUBLIC`, both here and again
	// at delivery time. Registering an endpoint already registered in the same scope returns
	// `CHANNEL_DUPLICATE`.
	//
	// The submitted endpoint is not returned by this or any other operation.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /api/v1/channels (the `CreateUserChannel` operationId).
	CreateUserChannelWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*CreateUserChannelResponse, error)

	// CreateUserChannelWithResponse Register an outbound channel
	//
	// Registering a channel does not contact it and does not make it deliver anything: a new
	// channel is unverified, and unverified channels are skipped. Call verify to send it one
	// message and make it usable.
	//
	// A `project` channel is shared with everyone in the project and requires
	// `notification:channels.manage`. A `user` channel is yours alone.
	//
	// The endpoint must be reachable over the public internet. Addresses that resolve to private
	// or link-local ranges are rejected with `CHANNEL_ENDPOINT_NOT_PUBLIC`, both here and again
	// at delivery time. Registering an endpoint already registered in the same scope returns
	// `CHANNEL_DUPLICATE`.
	//
	// The submitted endpoint is not returned by this or any other operation.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /api/v1/channels (the `CreateUserChannel` operationId).
	CreateUserChannelWithResponse(ctx context.Context, body CreateUserChannelJSONRequestBody, reqEditors ...RequestEditorFn) (*CreateUserChannelResponse, error)

	// DeleteUserChannelWithResponse Delete an outbound channel
	//
	// Deleting a channel stops delivery to it and destroys the stored endpoint. Deliveries
	// already queued for it are abandoned.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with DELETE /api/v1/channels/{channelId} (the `DeleteUserChannel` operationId).
	DeleteUserChannelWithResponse(ctx context.Context, channelId ChannelId, reqEditors ...RequestEditorFn) (*DeleteUserChannelResponse, error)

	// UpdateUserChannelWithBodyWithResponse Rename or re-enable an outbound channel
	//
	// Fields that are omitted are left alone.
	//
	// A channel disabled after repeated failures is re-enabled by setting `enabled` to true,
	// which also clears the failure count. It remains verified, so it starts delivering again
	// immediately — verify it first if the cause of the failures is not known to be resolved.
	//
	// Changing the endpoint is not possible; register a new channel and delete this one.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with PATCH /api/v1/channels/{channelId} (the `UpdateUserChannel` operationId).
	UpdateUserChannelWithBodyWithResponse(ctx context.Context, channelId ChannelId, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*UpdateUserChannelResponse, error)

	// UpdateUserChannelWithResponse Rename or re-enable an outbound channel
	//
	// Fields that are omitted are left alone.
	//
	// A channel disabled after repeated failures is re-enabled by setting `enabled` to true,
	// which also clears the failure count. It remains verified, so it starts delivering again
	// immediately — verify it first if the cause of the failures is not known to be resolved.
	//
	// Changing the endpoint is not possible; register a new channel and delete this one.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with PATCH /api/v1/channels/{channelId} (the `UpdateUserChannel` operationId).
	UpdateUserChannelWithResponse(ctx context.Context, channelId ChannelId, body UpdateUserChannelJSONRequestBody, reqEditors ...RequestEditorFn) (*UpdateUserChannelResponse, error)

	// VerifyUserChannelWithResponse Verify an outbound channel
	//
	// Sends one message to the channel and marks it verified if the endpoint accepted it.
	//
	// **The endpoint accepting the message is not proof that it arrived where it was meant to.**
	// A webhook URL belonging to the wrong group answers exactly the same way. Confirm the
	// message appeared before relying on the channel.
	//
	// A channel that fails verification is not marked verified and continues to be skipped. The
	// reason is reported in `last_error`; it is the provider's own wording and is not translated.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /api/v1/channels/{channelId}/verify (the `VerifyUserChannel` operationId).
	VerifyUserChannelWithResponse(ctx context.Context, channelId ChannelId, reqEditors ...RequestEditorFn) (*VerifyUserChannelResponse, error)

	// DescribeCredentialTicketWithResponse Describe a one-time credential
	//
	// Reports whether the credential is still available without consuming it. It never contains
	// the credential itself.
	//
	// A ticket addressed to someone else returns `CREDENTIAL_TICKET_NOT_FOUND`, the same answer
	// as one that does not exist.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /api/v1/credentials/{ticketId} (the `DescribeCredentialTicket` operationId).
	DescribeCredentialTicketWithResponse(ctx context.Context, ticketId TicketId, reqEditors ...RequestEditorFn) (*DescribeCredentialTicketResponse, error)

	// RevealCredentialWithResponse Reveal a one-time credential
	//
	// Returns the credential and destroys it in the same step. **This is the only operation that
	// ever returns it, and it succeeds only once.**
	//
	// Every subsequent request returns `CREDENTIAL_TICKET_CONSUMED`, including one issued
	// concurrently with the first: exactly one caller receives the credential.
	//
	// A ticket that has expired returns `CREDENTIAL_TICKET_EXPIRED`, and one withdrawn by the
	// service that created it returns `CREDENTIAL_TICKET_REVOKED`. In all three cases the
	// credential is already gone and cannot be recovered; obtain a new one from the service that
	// owns the resource.
	//
	// This operation is a POST rather than a GET because a GET would be followed by link
	// scanners in mail clients and by browser prefetching, either of which would consume the
	// credential before the recipient opened it.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /api/v1/credentials/{ticketId}/reveal (the `RevealCredential` operationId).
	RevealCredentialWithResponse(ctx context.Context, ticketId TicketId, reqEditors ...RequestEditorFn) (*RevealCredentialResponse, error)

	// ListNotificationTypesWithResponse List every notification type
	//
	// The catalogue of everything this platform can notify about, with the default delivery for
	// each type and whether it can be configured. It is the source for a settings interface.
	//
	// This operation does not require authentication: the catalogue is the same for everyone and
	// describes the product rather than any account.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /api/v1/notification-types (the `ListNotificationTypes` operationId).
	ListNotificationTypesWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*ListNotificationTypesResponse, error)

	// ListNotificationsWithResponse List notifications addressed to you
	//
	// Returns notifications in reverse chronological order, newest first.
	//
	// Announcements published to the whole platform appear here alongside notifications
	// addressed to you individually and are distinguished by `kind`. They are read and archived
	// through the same operations.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /api/v1/notifications (the `ListNotifications` operationId).
	ListNotificationsWithResponse(ctx context.Context, params *ListNotificationsParams, reqEditors ...RequestEditorFn) (*ListNotificationsResponse, error)

	// MarkNotificationsReadWithBodyWithResponse Mark several notifications as read
	//
	// Marks every listed notification as read. Notifications already read are left alone, and the
	// operation succeeds whether or not any of them changed.
	//
	// Passing an empty list marks everything currently unread as read.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /api/v1/notifications/read (the `MarkNotificationsRead` operationId).
	MarkNotificationsReadWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*MarkNotificationsReadResponse, error)

	// MarkNotificationsReadWithResponse Mark several notifications as read
	//
	// Marks every listed notification as read. Notifications already read are left alone, and the
	// operation succeeds whether or not any of them changed.
	//
	// Passing an empty list marks everything currently unread as read.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /api/v1/notifications/read (the `MarkNotificationsRead` operationId).
	MarkNotificationsReadWithResponse(ctx context.Context, body MarkNotificationsReadJSONRequestBody, reqEditors ...RequestEditorFn) (*MarkNotificationsReadResponse, error)

	// CountUnreadNotificationsWithResponse Count unread notifications
	//
	// Returns the number of unread notifications. This is the value behind the badge in the
	// console, and it is also carried on every realtime event, so it does not normally need to be
	// requested on its own.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /api/v1/notifications/unread-count (the `CountUnreadNotifications` operationId).
	CountUnreadNotificationsWithResponse(ctx context.Context, params *CountUnreadNotificationsParams, reqEditors ...RequestEditorFn) (*CountUnreadNotificationsResponse, error)

	// GetNotificationWithResponse Get one notification
	//
	// A notification addressed to someone else returns `NOTIFICATION_NOT_FOUND`, the same answer
	// as one that does not exist.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /api/v1/notifications/{notificationId} (the `GetNotification` operationId).
	GetNotificationWithResponse(ctx context.Context, notificationId NotificationId, reqEditors ...RequestEditorFn) (*GetNotificationResponse, error)

	// ArchiveNotificationWithResponse Archive one notification
	//
	// Archiving removes a notification from the default listing without deleting it; pass
	// `include_archived` to see it again. Archiving also marks it read.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /api/v1/notifications/{notificationId}/archive (the `ArchiveNotification` operationId).
	ArchiveNotificationWithResponse(ctx context.Context, notificationId NotificationId, reqEditors ...RequestEditorFn) (*ArchiveNotificationResponse, error)

	// MarkNotificationReadWithResponse Mark one notification as read
	//
	// Marking a notification that is already read succeeds and changes nothing.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /api/v1/notifications/{notificationId}/read (the `MarkNotificationRead` operationId).
	MarkNotificationReadWithResponse(ctx context.Context, notificationId NotificationId, reqEditors ...RequestEditorFn) (*MarkNotificationReadResponse, error)

	// GetNotificationPreferencesWithResponse Get your delivery preferences
	//
	// Returns the account-wide settings: the language notifications are written in, the time zone
	// times are stated in, and the address email is delivered to.
	//
	// A person who has never configured anything receives the defaults rather than an error.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /api/v1/preferences (the `GetNotificationPreferences` operationId).
	GetNotificationPreferencesWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*GetNotificationPreferencesResponse, error)

	// UpdateNotificationPreferencesWithBodyWithResponse Update your delivery preferences
	//
	// Fields that are omitted are left alone.
	//
	// Setting `email_override` starts verification of that address: email continues to be
	// delivered to the address on the account until the new one is confirmed. Setting it to null
	// returns delivery to the account address.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with PATCH /api/v1/preferences (the `UpdateNotificationPreferences` operationId).
	UpdateNotificationPreferencesWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*UpdateNotificationPreferencesResponse, error)

	// UpdateNotificationPreferencesWithResponse Update your delivery preferences
	//
	// Fields that are omitted are left alone.
	//
	// Setting `email_override` starts verification of that address: email continues to be
	// delivered to the address on the account until the new one is confirmed. Setting it to null
	// returns delivery to the account address.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with PATCH /api/v1/preferences (the `UpdateNotificationPreferences` operationId).
	UpdateNotificationPreferencesWithResponse(ctx context.Context, body UpdateNotificationPreferencesJSONRequestBody, reqEditors ...RequestEditorFn) (*UpdateNotificationPreferencesResponse, error)

	// ListTypePreferencesWithResponse List your per-type preferences
	//
	// Returns one entry per notification type, whether or not it has been configured. Entries
	// that have never been configured are reported with `inherited: true` and carry the defaults
	// from the catalogue.
	//
	// A preference applies either to one project or to every project. Both are returned; the one
	// naming a project takes precedence for that project.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /api/v1/preferences/types (the `ListTypePreferences` operationId).
	ListTypePreferencesWithResponse(ctx context.Context, params *ListTypePreferencesParams, reqEditors ...RequestEditorFn) (*ListTypePreferencesResponse, error)

	// DeleteTypePreferenceWithResponse Restore the default for one type
	//
	// Removes the configured preference so the type follows the default in the catalogue again.
	//
	// **This does not stop the type from being delivered.** To stop receiving it, set `muted`
	// instead. Deleting a preference that was never configured succeeds and changes nothing.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with DELETE /api/v1/preferences/types/{type} (the `DeleteTypePreference` operationId).
	DeleteTypePreferenceWithResponse(ctx context.Context, pType NotificationType, params *DeleteTypePreferenceParams, reqEditors ...RequestEditorFn) (*DeleteTypePreferenceResponse, error)

	// UpdateTypePreferenceWithBodyWithResponse Set the preference for one type
	//
	// States how this type should be delivered, replacing whatever was configured before.
	//
	// Omitting `project_id` configures every project. Naming one configures that project alone
	// and takes precedence there.
	//
	// Configuring a type marked `mandatory` returns `NOTIFICATION_TYPE_MANDATORY`. Naming a
	// channel the type does not support returns `NOTIFICATION_CHANNEL_UNSUPPORTED`.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with PUT /api/v1/preferences/types/{type} (the `UpdateTypePreference` operationId).
	UpdateTypePreferenceWithBodyWithResponse(ctx context.Context, pType NotificationType, params *UpdateTypePreferenceParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*UpdateTypePreferenceResponse, error)

	// UpdateTypePreferenceWithResponse Set the preference for one type
	//
	// States how this type should be delivered, replacing whatever was configured before.
	//
	// Omitting `project_id` configures every project. Naming one configures that project alone
	// and takes precedence there.
	//
	// Configuring a type marked `mandatory` returns `NOTIFICATION_TYPE_MANDATORY`. Naming a
	// channel the type does not support returns `NOTIFICATION_CHANNEL_UNSUPPORTED`.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with PUT /api/v1/preferences/types/{type} (the `UpdateTypePreference` operationId).
	UpdateTypePreferenceWithResponse(ctx context.Context, pType NotificationType, params *UpdateTypePreferenceParams, body UpdateTypePreferenceJSONRequestBody, reqEditors ...RequestEditorFn) (*UpdateTypePreferenceResponse, error)

	// DescribeRealtimeConnectionWithResponse Describe the realtime connection
	//
	// Returns everything needed to open a realtime connection, and the names of the channels to
	// subscribe to. The parameters are not constants: they differ between environments, so they
	// must be requested rather than compiled in.
	//
	// Realtime delivery is an optimisation. Everything it announces is also readable through the
	// listing operations, so a client that cannot connect stays correct by polling or by
	// refreshing.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /api/v1/realtime (the `DescribeRealtimeConnection` operationId).
	DescribeRealtimeConnectionWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*DescribeRealtimeConnectionResponse, error)

	// AuthorizeRealtimeChannelWithBodyWithResponse Authorize a realtime channel subscription
	//
	// Authorizes one subscription. Realtime clients call this on their own while subscribing; it
	// is not normally called directly.
	//
	// Only two channels are authorized: the one naming you, and the one naming the project in
	// the token. Any other channel returns `REALTIME_CHANNEL_FORBIDDEN`.
	//
	// **Authorization is evaluated once, when the subscription is established.** A subscription
	// already open is not revoked when project membership changes, so realtime events carry
	// identifiers and counts only, never the contents of a notification.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /api/v1/realtime/auth (the `AuthorizeRealtimeChannel` operationId).
	AuthorizeRealtimeChannelWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*AuthorizeRealtimeChannelResponse, error)

	// AuthorizeRealtimeChannelWithResponse Authorize a realtime channel subscription
	//
	// Authorizes one subscription. Realtime clients call this on their own while subscribing; it
	// is not normally called directly.
	//
	// Only two channels are authorized: the one naming you, and the one naming the project in
	// the token. Any other channel returns `REALTIME_CHANNEL_FORBIDDEN`.
	//
	// **Authorization is evaluated once, when the subscription is established.** A subscription
	// already open is not revoked when project membership changes, so realtime events carry
	// identifiers and counts only, never the contents of a notification.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /api/v1/realtime/auth (the `AuthorizeRealtimeChannel` operationId).
	AuthorizeRealtimeChannelWithResponse(ctx context.Context, body AuthorizeRealtimeChannelJSONRequestBody, reqEditors ...RequestEditorFn) (*AuthorizeRealtimeChannelResponse, error)
}

type ListUserChannelsResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *ChannelListResponseBody
	// JSONDefault the response for an HTTP default `application/json` response
	JSONDefault *Error
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r ListUserChannelsResponse) GetJSON200() *ChannelListResponseBody {
	return r.JSON200
}

// GetJSONDefault returns the response for an HTTP default `application/json` response
func (r ListUserChannelsResponse) GetJSONDefault() *Error {
	return r.JSONDefault
}

// GetBody returns the raw response body bytes
func (r ListUserChannelsResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ListUserChannelsResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ListUserChannelsResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ListUserChannelsResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type CreateUserChannelResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON201 the response for an HTTP 201 `application/json` response
	JSON201 *ChannelResource
	// JSONDefault the response for an HTTP default `application/json` response
	JSONDefault *Error
}

// GetJSON201 returns the response for an HTTP 201 `application/json` response
func (r CreateUserChannelResponse) GetJSON201() *ChannelResource {
	return r.JSON201
}

// GetJSONDefault returns the response for an HTTP default `application/json` response
func (r CreateUserChannelResponse) GetJSONDefault() *Error {
	return r.JSONDefault
}

// GetBody returns the raw response body bytes
func (r CreateUserChannelResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r CreateUserChannelResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r CreateUserChannelResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r CreateUserChannelResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type DeleteUserChannelResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSONDefault the response for an HTTP default `application/json` response
	JSONDefault *Error
}

// GetJSONDefault returns the response for an HTTP default `application/json` response
func (r DeleteUserChannelResponse) GetJSONDefault() *Error {
	return r.JSONDefault
}

// GetBody returns the raw response body bytes
func (r DeleteUserChannelResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r DeleteUserChannelResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r DeleteUserChannelResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r DeleteUserChannelResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type UpdateUserChannelResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *ChannelResource
	// JSONDefault the response for an HTTP default `application/json` response
	JSONDefault *Error
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r UpdateUserChannelResponse) GetJSON200() *ChannelResource {
	return r.JSON200
}

// GetJSONDefault returns the response for an HTTP default `application/json` response
func (r UpdateUserChannelResponse) GetJSONDefault() *Error {
	return r.JSONDefault
}

// GetBody returns the raw response body bytes
func (r UpdateUserChannelResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r UpdateUserChannelResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r UpdateUserChannelResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r UpdateUserChannelResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type VerifyUserChannelResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *ChannelResource
	// JSONDefault the response for an HTTP default `application/json` response
	JSONDefault *Error
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r VerifyUserChannelResponse) GetJSON200() *ChannelResource {
	return r.JSON200
}

// GetJSONDefault returns the response for an HTTP default `application/json` response
func (r VerifyUserChannelResponse) GetJSONDefault() *Error {
	return r.JSONDefault
}

// GetBody returns the raw response body bytes
func (r VerifyUserChannelResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r VerifyUserChannelResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r VerifyUserChannelResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r VerifyUserChannelResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type DescribeCredentialTicketResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *CredentialTicketResource
	// JSONDefault the response for an HTTP default `application/json` response
	JSONDefault *Error
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r DescribeCredentialTicketResponse) GetJSON200() *CredentialTicketResource {
	return r.JSON200
}

// GetJSONDefault returns the response for an HTTP default `application/json` response
func (r DescribeCredentialTicketResponse) GetJSONDefault() *Error {
	return r.JSONDefault
}

// GetBody returns the raw response body bytes
func (r DescribeCredentialTicketResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r DescribeCredentialTicketResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r DescribeCredentialTicketResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r DescribeCredentialTicketResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type RevealCredentialResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *RevealedCredentialResource
	// JSONDefault the response for an HTTP default `application/json` response
	JSONDefault *Error
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r RevealCredentialResponse) GetJSON200() *RevealedCredentialResource {
	return r.JSON200
}

// GetJSONDefault returns the response for an HTTP default `application/json` response
func (r RevealCredentialResponse) GetJSONDefault() *Error {
	return r.JSONDefault
}

// GetBody returns the raw response body bytes
func (r RevealCredentialResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r RevealCredentialResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r RevealCredentialResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r RevealCredentialResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type ListNotificationTypesResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *NotificationTypeListResponseBody
	// JSONDefault the response for an HTTP default `application/json` response
	JSONDefault *Error
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r ListNotificationTypesResponse) GetJSON200() *NotificationTypeListResponseBody {
	return r.JSON200
}

// GetJSONDefault returns the response for an HTTP default `application/json` response
func (r ListNotificationTypesResponse) GetJSONDefault() *Error {
	return r.JSONDefault
}

// GetBody returns the raw response body bytes
func (r ListNotificationTypesResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ListNotificationTypesResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ListNotificationTypesResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ListNotificationTypesResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type ListNotificationsResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *LengthAwarePageNotificationResource
	// JSONDefault the response for an HTTP default `application/json` response
	JSONDefault *Error
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r ListNotificationsResponse) GetJSON200() *LengthAwarePageNotificationResource {
	return r.JSON200
}

// GetJSONDefault returns the response for an HTTP default `application/json` response
func (r ListNotificationsResponse) GetJSONDefault() *Error {
	return r.JSONDefault
}

// GetBody returns the raw response body bytes
func (r ListNotificationsResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ListNotificationsResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ListNotificationsResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ListNotificationsResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type MarkNotificationsReadResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *UnreadCountResource
	// JSONDefault the response for an HTTP default `application/json` response
	JSONDefault *Error
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r MarkNotificationsReadResponse) GetJSON200() *UnreadCountResource {
	return r.JSON200
}

// GetJSONDefault returns the response for an HTTP default `application/json` response
func (r MarkNotificationsReadResponse) GetJSONDefault() *Error {
	return r.JSONDefault
}

// GetBody returns the raw response body bytes
func (r MarkNotificationsReadResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r MarkNotificationsReadResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r MarkNotificationsReadResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r MarkNotificationsReadResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type CountUnreadNotificationsResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *UnreadCountResource
	// JSONDefault the response for an HTTP default `application/json` response
	JSONDefault *Error
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r CountUnreadNotificationsResponse) GetJSON200() *UnreadCountResource {
	return r.JSON200
}

// GetJSONDefault returns the response for an HTTP default `application/json` response
func (r CountUnreadNotificationsResponse) GetJSONDefault() *Error {
	return r.JSONDefault
}

// GetBody returns the raw response body bytes
func (r CountUnreadNotificationsResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r CountUnreadNotificationsResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r CountUnreadNotificationsResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r CountUnreadNotificationsResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type GetNotificationResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *NotificationResource
	// JSONDefault the response for an HTTP default `application/json` response
	JSONDefault *Error
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r GetNotificationResponse) GetJSON200() *NotificationResource {
	return r.JSON200
}

// GetJSONDefault returns the response for an HTTP default `application/json` response
func (r GetNotificationResponse) GetJSONDefault() *Error {
	return r.JSONDefault
}

// GetBody returns the raw response body bytes
func (r GetNotificationResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetNotificationResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetNotificationResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetNotificationResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type ArchiveNotificationResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *NotificationResource
	// JSONDefault the response for an HTTP default `application/json` response
	JSONDefault *Error
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r ArchiveNotificationResponse) GetJSON200() *NotificationResource {
	return r.JSON200
}

// GetJSONDefault returns the response for an HTTP default `application/json` response
func (r ArchiveNotificationResponse) GetJSONDefault() *Error {
	return r.JSONDefault
}

// GetBody returns the raw response body bytes
func (r ArchiveNotificationResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ArchiveNotificationResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ArchiveNotificationResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ArchiveNotificationResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type MarkNotificationReadResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *NotificationResource
	// JSONDefault the response for an HTTP default `application/json` response
	JSONDefault *Error
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r MarkNotificationReadResponse) GetJSON200() *NotificationResource {
	return r.JSON200
}

// GetJSONDefault returns the response for an HTTP default `application/json` response
func (r MarkNotificationReadResponse) GetJSONDefault() *Error {
	return r.JSONDefault
}

// GetBody returns the raw response body bytes
func (r MarkNotificationReadResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r MarkNotificationReadResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r MarkNotificationReadResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r MarkNotificationReadResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type GetNotificationPreferencesResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *PreferencesResource
	// JSONDefault the response for an HTTP default `application/json` response
	JSONDefault *Error
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r GetNotificationPreferencesResponse) GetJSON200() *PreferencesResource {
	return r.JSON200
}

// GetJSONDefault returns the response for an HTTP default `application/json` response
func (r GetNotificationPreferencesResponse) GetJSONDefault() *Error {
	return r.JSONDefault
}

// GetBody returns the raw response body bytes
func (r GetNotificationPreferencesResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetNotificationPreferencesResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetNotificationPreferencesResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetNotificationPreferencesResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type UpdateNotificationPreferencesResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *PreferencesResource
	// JSONDefault the response for an HTTP default `application/json` response
	JSONDefault *Error
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r UpdateNotificationPreferencesResponse) GetJSON200() *PreferencesResource {
	return r.JSON200
}

// GetJSONDefault returns the response for an HTTP default `application/json` response
func (r UpdateNotificationPreferencesResponse) GetJSONDefault() *Error {
	return r.JSONDefault
}

// GetBody returns the raw response body bytes
func (r UpdateNotificationPreferencesResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r UpdateNotificationPreferencesResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r UpdateNotificationPreferencesResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r UpdateNotificationPreferencesResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type ListTypePreferencesResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *TypePreferenceListResponseBody
	// JSONDefault the response for an HTTP default `application/json` response
	JSONDefault *Error
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r ListTypePreferencesResponse) GetJSON200() *TypePreferenceListResponseBody {
	return r.JSON200
}

// GetJSONDefault returns the response for an HTTP default `application/json` response
func (r ListTypePreferencesResponse) GetJSONDefault() *Error {
	return r.JSONDefault
}

// GetBody returns the raw response body bytes
func (r ListTypePreferencesResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ListTypePreferencesResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ListTypePreferencesResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ListTypePreferencesResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type DeleteTypePreferenceResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSONDefault the response for an HTTP default `application/json` response
	JSONDefault *Error
}

// GetJSONDefault returns the response for an HTTP default `application/json` response
func (r DeleteTypePreferenceResponse) GetJSONDefault() *Error {
	return r.JSONDefault
}

// GetBody returns the raw response body bytes
func (r DeleteTypePreferenceResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r DeleteTypePreferenceResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r DeleteTypePreferenceResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r DeleteTypePreferenceResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type UpdateTypePreferenceResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *TypePreferenceResource
	// JSONDefault the response for an HTTP default `application/json` response
	JSONDefault *Error
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r UpdateTypePreferenceResponse) GetJSON200() *TypePreferenceResource {
	return r.JSON200
}

// GetJSONDefault returns the response for an HTTP default `application/json` response
func (r UpdateTypePreferenceResponse) GetJSONDefault() *Error {
	return r.JSONDefault
}

// GetBody returns the raw response body bytes
func (r UpdateTypePreferenceResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r UpdateTypePreferenceResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r UpdateTypePreferenceResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r UpdateTypePreferenceResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type DescribeRealtimeConnectionResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *RealtimeConnectionResource
	// JSONDefault the response for an HTTP default `application/json` response
	JSONDefault *Error
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r DescribeRealtimeConnectionResponse) GetJSON200() *RealtimeConnectionResource {
	return r.JSON200
}

// GetJSONDefault returns the response for an HTTP default `application/json` response
func (r DescribeRealtimeConnectionResponse) GetJSONDefault() *Error {
	return r.JSONDefault
}

// GetBody returns the raw response body bytes
func (r DescribeRealtimeConnectionResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r DescribeRealtimeConnectionResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r DescribeRealtimeConnectionResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r DescribeRealtimeConnectionResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type AuthorizeRealtimeChannelResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *RealtimeAuthResource
	// JSONDefault the response for an HTTP default `application/json` response
	JSONDefault *Error
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r AuthorizeRealtimeChannelResponse) GetJSON200() *RealtimeAuthResource {
	return r.JSON200
}

// GetJSONDefault returns the response for an HTTP default `application/json` response
func (r AuthorizeRealtimeChannelResponse) GetJSONDefault() *Error {
	return r.JSONDefault
}

// GetBody returns the raw response body bytes
func (r AuthorizeRealtimeChannelResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r AuthorizeRealtimeChannelResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r AuthorizeRealtimeChannelResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r AuthorizeRealtimeChannelResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

// ListUserChannelsWithResponse List outbound channels
//
// Returns the channels you have registered, and the channels registered for the project in
// the token. The address of each is not included.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /api/v1/channels (the `ListUserChannels` operationId).
func (c *ClientWithResponses) ListUserChannelsWithResponse(ctx context.Context, params *ListUserChannelsParams, reqEditors ...RequestEditorFn) (*ListUserChannelsResponse, error) {
	rsp, err := c.ListUserChannels(ctx, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseListUserChannelsResponse(rsp)
}

// CreateUserChannelWithBodyWithResponse Register an outbound channel
//
// Registering a channel does not contact it and does not make it deliver anything: a new
// channel is unverified, and unverified channels are skipped. Call verify to send it one
// message and make it usable.
//
// A `project` channel is shared with everyone in the project and requires
// `notification:channels.manage`. A `user` channel is yours alone.
//
// The endpoint must be reachable over the public internet. Addresses that resolve to private
// or link-local ranges are rejected with `CHANNEL_ENDPOINT_NOT_PUBLIC`, both here and again
// at delivery time. Registering an endpoint already registered in the same scope returns
// `CHANNEL_DUPLICATE`.
//
// The submitted endpoint is not returned by this or any other operation.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /api/v1/channels (the `CreateUserChannel` operationId).
func (c *ClientWithResponses) CreateUserChannelWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*CreateUserChannelResponse, error) {
	rsp, err := c.CreateUserChannelWithBody(ctx, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseCreateUserChannelResponse(rsp)
}

// CreateUserChannelWithResponse Register an outbound channel
//
// Registering a channel does not contact it and does not make it deliver anything: a new
// channel is unverified, and unverified channels are skipped. Call verify to send it one
// message and make it usable.
//
// A `project` channel is shared with everyone in the project and requires
// `notification:channels.manage`. A `user` channel is yours alone.
//
// The endpoint must be reachable over the public internet. Addresses that resolve to private
// or link-local ranges are rejected with `CHANNEL_ENDPOINT_NOT_PUBLIC`, both here and again
// at delivery time. Registering an endpoint already registered in the same scope returns
// `CHANNEL_DUPLICATE`.
//
// The submitted endpoint is not returned by this or any other operation.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /api/v1/channels (the `CreateUserChannel` operationId).
func (c *ClientWithResponses) CreateUserChannelWithResponse(ctx context.Context, body CreateUserChannelJSONRequestBody, reqEditors ...RequestEditorFn) (*CreateUserChannelResponse, error) {
	rsp, err := c.CreateUserChannel(ctx, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseCreateUserChannelResponse(rsp)
}

// DeleteUserChannelWithResponse Delete an outbound channel
//
// Deleting a channel stops delivery to it and destroys the stored endpoint. Deliveries
// already queued for it are abandoned.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with DELETE /api/v1/channels/{channelId} (the `DeleteUserChannel` operationId).
func (c *ClientWithResponses) DeleteUserChannelWithResponse(ctx context.Context, channelId ChannelId, reqEditors ...RequestEditorFn) (*DeleteUserChannelResponse, error) {
	rsp, err := c.DeleteUserChannel(ctx, channelId, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseDeleteUserChannelResponse(rsp)
}

// UpdateUserChannelWithBodyWithResponse Rename or re-enable an outbound channel
//
// Fields that are omitted are left alone.
//
// A channel disabled after repeated failures is re-enabled by setting `enabled` to true,
// which also clears the failure count. It remains verified, so it starts delivering again
// immediately — verify it first if the cause of the failures is not known to be resolved.
//
// Changing the endpoint is not possible; register a new channel and delete this one.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with PATCH /api/v1/channels/{channelId} (the `UpdateUserChannel` operationId).
func (c *ClientWithResponses) UpdateUserChannelWithBodyWithResponse(ctx context.Context, channelId ChannelId, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*UpdateUserChannelResponse, error) {
	rsp, err := c.UpdateUserChannelWithBody(ctx, channelId, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseUpdateUserChannelResponse(rsp)
}

// UpdateUserChannelWithResponse Rename or re-enable an outbound channel
//
// Fields that are omitted are left alone.
//
// A channel disabled after repeated failures is re-enabled by setting `enabled` to true,
// which also clears the failure count. It remains verified, so it starts delivering again
// immediately — verify it first if the cause of the failures is not known to be resolved.
//
// Changing the endpoint is not possible; register a new channel and delete this one.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with PATCH /api/v1/channels/{channelId} (the `UpdateUserChannel` operationId).
func (c *ClientWithResponses) UpdateUserChannelWithResponse(ctx context.Context, channelId ChannelId, body UpdateUserChannelJSONRequestBody, reqEditors ...RequestEditorFn) (*UpdateUserChannelResponse, error) {
	rsp, err := c.UpdateUserChannel(ctx, channelId, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseUpdateUserChannelResponse(rsp)
}

// VerifyUserChannelWithResponse Verify an outbound channel
//
// Sends one message to the channel and marks it verified if the endpoint accepted it.
//
// **The endpoint accepting the message is not proof that it arrived where it was meant to.**
// A webhook URL belonging to the wrong group answers exactly the same way. Confirm the
// message appeared before relying on the channel.
//
// A channel that fails verification is not marked verified and continues to be skipped. The
// reason is reported in `last_error`; it is the provider's own wording and is not translated.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /api/v1/channels/{channelId}/verify (the `VerifyUserChannel` operationId).
func (c *ClientWithResponses) VerifyUserChannelWithResponse(ctx context.Context, channelId ChannelId, reqEditors ...RequestEditorFn) (*VerifyUserChannelResponse, error) {
	rsp, err := c.VerifyUserChannel(ctx, channelId, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseVerifyUserChannelResponse(rsp)
}

// DescribeCredentialTicketWithResponse Describe a one-time credential
//
// Reports whether the credential is still available without consuming it. It never contains
// the credential itself.
//
// A ticket addressed to someone else returns `CREDENTIAL_TICKET_NOT_FOUND`, the same answer
// as one that does not exist.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /api/v1/credentials/{ticketId} (the `DescribeCredentialTicket` operationId).
func (c *ClientWithResponses) DescribeCredentialTicketWithResponse(ctx context.Context, ticketId TicketId, reqEditors ...RequestEditorFn) (*DescribeCredentialTicketResponse, error) {
	rsp, err := c.DescribeCredentialTicket(ctx, ticketId, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseDescribeCredentialTicketResponse(rsp)
}

// RevealCredentialWithResponse Reveal a one-time credential
//
// Returns the credential and destroys it in the same step. **This is the only operation that
// ever returns it, and it succeeds only once.**
//
// Every subsequent request returns `CREDENTIAL_TICKET_CONSUMED`, including one issued
// concurrently with the first: exactly one caller receives the credential.
//
// A ticket that has expired returns `CREDENTIAL_TICKET_EXPIRED`, and one withdrawn by the
// service that created it returns `CREDENTIAL_TICKET_REVOKED`. In all three cases the
// credential is already gone and cannot be recovered; obtain a new one from the service that
// owns the resource.
//
// This operation is a POST rather than a GET because a GET would be followed by link
// scanners in mail clients and by browser prefetching, either of which would consume the
// credential before the recipient opened it.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /api/v1/credentials/{ticketId}/reveal (the `RevealCredential` operationId).
func (c *ClientWithResponses) RevealCredentialWithResponse(ctx context.Context, ticketId TicketId, reqEditors ...RequestEditorFn) (*RevealCredentialResponse, error) {
	rsp, err := c.RevealCredential(ctx, ticketId, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseRevealCredentialResponse(rsp)
}

// ListNotificationTypesWithResponse List every notification type
//
// The catalogue of everything this platform can notify about, with the default delivery for
// each type and whether it can be configured. It is the source for a settings interface.
//
// This operation does not require authentication: the catalogue is the same for everyone and
// describes the product rather than any account.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /api/v1/notification-types (the `ListNotificationTypes` operationId).
func (c *ClientWithResponses) ListNotificationTypesWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*ListNotificationTypesResponse, error) {
	rsp, err := c.ListNotificationTypes(ctx, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseListNotificationTypesResponse(rsp)
}

// ListNotificationsWithResponse List notifications addressed to you
//
// Returns notifications in reverse chronological order, newest first.
//
// Announcements published to the whole platform appear here alongside notifications
// addressed to you individually and are distinguished by `kind`. They are read and archived
// through the same operations.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /api/v1/notifications (the `ListNotifications` operationId).
func (c *ClientWithResponses) ListNotificationsWithResponse(ctx context.Context, params *ListNotificationsParams, reqEditors ...RequestEditorFn) (*ListNotificationsResponse, error) {
	rsp, err := c.ListNotifications(ctx, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseListNotificationsResponse(rsp)
}

// MarkNotificationsReadWithBodyWithResponse Mark several notifications as read
//
// Marks every listed notification as read. Notifications already read are left alone, and the
// operation succeeds whether or not any of them changed.
//
// Passing an empty list marks everything currently unread as read.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /api/v1/notifications/read (the `MarkNotificationsRead` operationId).
func (c *ClientWithResponses) MarkNotificationsReadWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*MarkNotificationsReadResponse, error) {
	rsp, err := c.MarkNotificationsReadWithBody(ctx, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseMarkNotificationsReadResponse(rsp)
}

// MarkNotificationsReadWithResponse Mark several notifications as read
//
// Marks every listed notification as read. Notifications already read are left alone, and the
// operation succeeds whether or not any of them changed.
//
// Passing an empty list marks everything currently unread as read.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /api/v1/notifications/read (the `MarkNotificationsRead` operationId).
func (c *ClientWithResponses) MarkNotificationsReadWithResponse(ctx context.Context, body MarkNotificationsReadJSONRequestBody, reqEditors ...RequestEditorFn) (*MarkNotificationsReadResponse, error) {
	rsp, err := c.MarkNotificationsRead(ctx, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseMarkNotificationsReadResponse(rsp)
}

// CountUnreadNotificationsWithResponse Count unread notifications
//
// Returns the number of unread notifications. This is the value behind the badge in the
// console, and it is also carried on every realtime event, so it does not normally need to be
// requested on its own.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /api/v1/notifications/unread-count (the `CountUnreadNotifications` operationId).
func (c *ClientWithResponses) CountUnreadNotificationsWithResponse(ctx context.Context, params *CountUnreadNotificationsParams, reqEditors ...RequestEditorFn) (*CountUnreadNotificationsResponse, error) {
	rsp, err := c.CountUnreadNotifications(ctx, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseCountUnreadNotificationsResponse(rsp)
}

// GetNotificationWithResponse Get one notification
//
// A notification addressed to someone else returns `NOTIFICATION_NOT_FOUND`, the same answer
// as one that does not exist.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /api/v1/notifications/{notificationId} (the `GetNotification` operationId).
func (c *ClientWithResponses) GetNotificationWithResponse(ctx context.Context, notificationId NotificationId, reqEditors ...RequestEditorFn) (*GetNotificationResponse, error) {
	rsp, err := c.GetNotification(ctx, notificationId, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetNotificationResponse(rsp)
}

// ArchiveNotificationWithResponse Archive one notification
//
// Archiving removes a notification from the default listing without deleting it; pass
// `include_archived` to see it again. Archiving also marks it read.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /api/v1/notifications/{notificationId}/archive (the `ArchiveNotification` operationId).
func (c *ClientWithResponses) ArchiveNotificationWithResponse(ctx context.Context, notificationId NotificationId, reqEditors ...RequestEditorFn) (*ArchiveNotificationResponse, error) {
	rsp, err := c.ArchiveNotification(ctx, notificationId, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseArchiveNotificationResponse(rsp)
}

// MarkNotificationReadWithResponse Mark one notification as read
//
// Marking a notification that is already read succeeds and changes nothing.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /api/v1/notifications/{notificationId}/read (the `MarkNotificationRead` operationId).
func (c *ClientWithResponses) MarkNotificationReadWithResponse(ctx context.Context, notificationId NotificationId, reqEditors ...RequestEditorFn) (*MarkNotificationReadResponse, error) {
	rsp, err := c.MarkNotificationRead(ctx, notificationId, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseMarkNotificationReadResponse(rsp)
}

// GetNotificationPreferencesWithResponse Get your delivery preferences
//
// Returns the account-wide settings: the language notifications are written in, the time zone
// times are stated in, and the address email is delivered to.
//
// A person who has never configured anything receives the defaults rather than an error.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /api/v1/preferences (the `GetNotificationPreferences` operationId).
func (c *ClientWithResponses) GetNotificationPreferencesWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*GetNotificationPreferencesResponse, error) {
	rsp, err := c.GetNotificationPreferences(ctx, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetNotificationPreferencesResponse(rsp)
}

// UpdateNotificationPreferencesWithBodyWithResponse Update your delivery preferences
//
// Fields that are omitted are left alone.
//
// Setting `email_override` starts verification of that address: email continues to be
// delivered to the address on the account until the new one is confirmed. Setting it to null
// returns delivery to the account address.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with PATCH /api/v1/preferences (the `UpdateNotificationPreferences` operationId).
func (c *ClientWithResponses) UpdateNotificationPreferencesWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*UpdateNotificationPreferencesResponse, error) {
	rsp, err := c.UpdateNotificationPreferencesWithBody(ctx, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseUpdateNotificationPreferencesResponse(rsp)
}

// UpdateNotificationPreferencesWithResponse Update your delivery preferences
//
// Fields that are omitted are left alone.
//
// Setting `email_override` starts verification of that address: email continues to be
// delivered to the address on the account until the new one is confirmed. Setting it to null
// returns delivery to the account address.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with PATCH /api/v1/preferences (the `UpdateNotificationPreferences` operationId).
func (c *ClientWithResponses) UpdateNotificationPreferencesWithResponse(ctx context.Context, body UpdateNotificationPreferencesJSONRequestBody, reqEditors ...RequestEditorFn) (*UpdateNotificationPreferencesResponse, error) {
	rsp, err := c.UpdateNotificationPreferences(ctx, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseUpdateNotificationPreferencesResponse(rsp)
}

// ListTypePreferencesWithResponse List your per-type preferences
//
// Returns one entry per notification type, whether or not it has been configured. Entries
// that have never been configured are reported with `inherited: true` and carry the defaults
// from the catalogue.
//
// A preference applies either to one project or to every project. Both are returned; the one
// naming a project takes precedence for that project.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /api/v1/preferences/types (the `ListTypePreferences` operationId).
func (c *ClientWithResponses) ListTypePreferencesWithResponse(ctx context.Context, params *ListTypePreferencesParams, reqEditors ...RequestEditorFn) (*ListTypePreferencesResponse, error) {
	rsp, err := c.ListTypePreferences(ctx, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseListTypePreferencesResponse(rsp)
}

// DeleteTypePreferenceWithResponse Restore the default for one type
//
// Removes the configured preference so the type follows the default in the catalogue again.
//
// **This does not stop the type from being delivered.** To stop receiving it, set `muted`
// instead. Deleting a preference that was never configured succeeds and changes nothing.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with DELETE /api/v1/preferences/types/{type} (the `DeleteTypePreference` operationId).
func (c *ClientWithResponses) DeleteTypePreferenceWithResponse(ctx context.Context, pType NotificationType, params *DeleteTypePreferenceParams, reqEditors ...RequestEditorFn) (*DeleteTypePreferenceResponse, error) {
	rsp, err := c.DeleteTypePreference(ctx, pType, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseDeleteTypePreferenceResponse(rsp)
}

// UpdateTypePreferenceWithBodyWithResponse Set the preference for one type
//
// States how this type should be delivered, replacing whatever was configured before.
//
// Omitting `project_id` configures every project. Naming one configures that project alone
// and takes precedence there.
//
// Configuring a type marked `mandatory` returns `NOTIFICATION_TYPE_MANDATORY`. Naming a
// channel the type does not support returns `NOTIFICATION_CHANNEL_UNSUPPORTED`.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with PUT /api/v1/preferences/types/{type} (the `UpdateTypePreference` operationId).
func (c *ClientWithResponses) UpdateTypePreferenceWithBodyWithResponse(ctx context.Context, pType NotificationType, params *UpdateTypePreferenceParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*UpdateTypePreferenceResponse, error) {
	rsp, err := c.UpdateTypePreferenceWithBody(ctx, pType, params, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseUpdateTypePreferenceResponse(rsp)
}

// UpdateTypePreferenceWithResponse Set the preference for one type
//
// States how this type should be delivered, replacing whatever was configured before.
//
// Omitting `project_id` configures every project. Naming one configures that project alone
// and takes precedence there.
//
// Configuring a type marked `mandatory` returns `NOTIFICATION_TYPE_MANDATORY`. Naming a
// channel the type does not support returns `NOTIFICATION_CHANNEL_UNSUPPORTED`.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with PUT /api/v1/preferences/types/{type} (the `UpdateTypePreference` operationId).
func (c *ClientWithResponses) UpdateTypePreferenceWithResponse(ctx context.Context, pType NotificationType, params *UpdateTypePreferenceParams, body UpdateTypePreferenceJSONRequestBody, reqEditors ...RequestEditorFn) (*UpdateTypePreferenceResponse, error) {
	rsp, err := c.UpdateTypePreference(ctx, pType, params, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseUpdateTypePreferenceResponse(rsp)
}

// DescribeRealtimeConnectionWithResponse Describe the realtime connection
//
// Returns everything needed to open a realtime connection, and the names of the channels to
// subscribe to. The parameters are not constants: they differ between environments, so they
// must be requested rather than compiled in.
//
// Realtime delivery is an optimisation. Everything it announces is also readable through the
// listing operations, so a client that cannot connect stays correct by polling or by
// refreshing.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /api/v1/realtime (the `DescribeRealtimeConnection` operationId).
func (c *ClientWithResponses) DescribeRealtimeConnectionWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*DescribeRealtimeConnectionResponse, error) {
	rsp, err := c.DescribeRealtimeConnection(ctx, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseDescribeRealtimeConnectionResponse(rsp)
}

// AuthorizeRealtimeChannelWithBodyWithResponse Authorize a realtime channel subscription
//
// Authorizes one subscription. Realtime clients call this on their own while subscribing; it
// is not normally called directly.
//
// Only two channels are authorized: the one naming you, and the one naming the project in
// the token. Any other channel returns `REALTIME_CHANNEL_FORBIDDEN`.
//
// **Authorization is evaluated once, when the subscription is established.** A subscription
// already open is not revoked when project membership changes, so realtime events carry
// identifiers and counts only, never the contents of a notification.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /api/v1/realtime/auth (the `AuthorizeRealtimeChannel` operationId).
func (c *ClientWithResponses) AuthorizeRealtimeChannelWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*AuthorizeRealtimeChannelResponse, error) {
	rsp, err := c.AuthorizeRealtimeChannelWithBody(ctx, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseAuthorizeRealtimeChannelResponse(rsp)
}

// AuthorizeRealtimeChannelWithResponse Authorize a realtime channel subscription
//
// Authorizes one subscription. Realtime clients call this on their own while subscribing; it
// is not normally called directly.
//
// Only two channels are authorized: the one naming you, and the one naming the project in
// the token. Any other channel returns `REALTIME_CHANNEL_FORBIDDEN`.
//
// **Authorization is evaluated once, when the subscription is established.** A subscription
// already open is not revoked when project membership changes, so realtime events carry
// identifiers and counts only, never the contents of a notification.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /api/v1/realtime/auth (the `AuthorizeRealtimeChannel` operationId).
func (c *ClientWithResponses) AuthorizeRealtimeChannelWithResponse(ctx context.Context, body AuthorizeRealtimeChannelJSONRequestBody, reqEditors ...RequestEditorFn) (*AuthorizeRealtimeChannelResponse, error) {
	rsp, err := c.AuthorizeRealtimeChannel(ctx, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseAuthorizeRealtimeChannelResponse(rsp)
}

// ParseListUserChannelsResponse parses an HTTP response from a ListUserChannelsWithResponse call
func ParseListUserChannelsResponse(rsp *http.Response) (*ListUserChannelsResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ListUserChannelsResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest ChannelListResponseBody
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && true:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSONDefault = &dest

	}

	return response, nil
}

// ParseCreateUserChannelResponse parses an HTTP response from a CreateUserChannelWithResponse call
func ParseCreateUserChannelResponse(rsp *http.Response) (*CreateUserChannelResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &CreateUserChannelResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 201:
		var dest ChannelResource
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON201 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && true:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSONDefault = &dest

	}

	return response, nil
}

// ParseDeleteUserChannelResponse parses an HTTP response from a DeleteUserChannelWithResponse call
func ParseDeleteUserChannelResponse(rsp *http.Response) (*DeleteUserChannelResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &DeleteUserChannelResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case rsp.StatusCode == 204:
		break // No content-type

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && true:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSONDefault = &dest

	}

	return response, nil
}

// ParseUpdateUserChannelResponse parses an HTTP response from a UpdateUserChannelWithResponse call
func ParseUpdateUserChannelResponse(rsp *http.Response) (*UpdateUserChannelResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &UpdateUserChannelResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest ChannelResource
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && true:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSONDefault = &dest

	}

	return response, nil
}

// ParseVerifyUserChannelResponse parses an HTTP response from a VerifyUserChannelWithResponse call
func ParseVerifyUserChannelResponse(rsp *http.Response) (*VerifyUserChannelResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &VerifyUserChannelResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest ChannelResource
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && true:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSONDefault = &dest

	}

	return response, nil
}

// ParseDescribeCredentialTicketResponse parses an HTTP response from a DescribeCredentialTicketWithResponse call
func ParseDescribeCredentialTicketResponse(rsp *http.Response) (*DescribeCredentialTicketResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &DescribeCredentialTicketResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest CredentialTicketResource
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && true:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSONDefault = &dest

	}

	return response, nil
}

// ParseRevealCredentialResponse parses an HTTP response from a RevealCredentialWithResponse call
func ParseRevealCredentialResponse(rsp *http.Response) (*RevealCredentialResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &RevealCredentialResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest RevealedCredentialResource
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && true:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSONDefault = &dest

	}

	return response, nil
}

// ParseListNotificationTypesResponse parses an HTTP response from a ListNotificationTypesWithResponse call
func ParseListNotificationTypesResponse(rsp *http.Response) (*ListNotificationTypesResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ListNotificationTypesResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest NotificationTypeListResponseBody
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && true:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSONDefault = &dest

	}

	return response, nil
}

// ParseListNotificationsResponse parses an HTTP response from a ListNotificationsWithResponse call
func ParseListNotificationsResponse(rsp *http.Response) (*ListNotificationsResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ListNotificationsResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest LengthAwarePageNotificationResource
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && true:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSONDefault = &dest

	}

	return response, nil
}

// ParseMarkNotificationsReadResponse parses an HTTP response from a MarkNotificationsReadWithResponse call
func ParseMarkNotificationsReadResponse(rsp *http.Response) (*MarkNotificationsReadResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &MarkNotificationsReadResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest UnreadCountResource
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && true:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSONDefault = &dest

	}

	return response, nil
}

// ParseCountUnreadNotificationsResponse parses an HTTP response from a CountUnreadNotificationsWithResponse call
func ParseCountUnreadNotificationsResponse(rsp *http.Response) (*CountUnreadNotificationsResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &CountUnreadNotificationsResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest UnreadCountResource
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && true:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSONDefault = &dest

	}

	return response, nil
}

// ParseGetNotificationResponse parses an HTTP response from a GetNotificationWithResponse call
func ParseGetNotificationResponse(rsp *http.Response) (*GetNotificationResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetNotificationResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest NotificationResource
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && true:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSONDefault = &dest

	}

	return response, nil
}

// ParseArchiveNotificationResponse parses an HTTP response from a ArchiveNotificationWithResponse call
func ParseArchiveNotificationResponse(rsp *http.Response) (*ArchiveNotificationResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ArchiveNotificationResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest NotificationResource
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && true:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSONDefault = &dest

	}

	return response, nil
}

// ParseMarkNotificationReadResponse parses an HTTP response from a MarkNotificationReadWithResponse call
func ParseMarkNotificationReadResponse(rsp *http.Response) (*MarkNotificationReadResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &MarkNotificationReadResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest NotificationResource
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && true:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSONDefault = &dest

	}

	return response, nil
}

// ParseGetNotificationPreferencesResponse parses an HTTP response from a GetNotificationPreferencesWithResponse call
func ParseGetNotificationPreferencesResponse(rsp *http.Response) (*GetNotificationPreferencesResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetNotificationPreferencesResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest PreferencesResource
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && true:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSONDefault = &dest

	}

	return response, nil
}

// ParseUpdateNotificationPreferencesResponse parses an HTTP response from a UpdateNotificationPreferencesWithResponse call
func ParseUpdateNotificationPreferencesResponse(rsp *http.Response) (*UpdateNotificationPreferencesResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &UpdateNotificationPreferencesResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest PreferencesResource
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && true:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSONDefault = &dest

	}

	return response, nil
}

// ParseListTypePreferencesResponse parses an HTTP response from a ListTypePreferencesWithResponse call
func ParseListTypePreferencesResponse(rsp *http.Response) (*ListTypePreferencesResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ListTypePreferencesResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest TypePreferenceListResponseBody
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && true:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSONDefault = &dest

	}

	return response, nil
}

// ParseDeleteTypePreferenceResponse parses an HTTP response from a DeleteTypePreferenceWithResponse call
func ParseDeleteTypePreferenceResponse(rsp *http.Response) (*DeleteTypePreferenceResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &DeleteTypePreferenceResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case rsp.StatusCode == 204:
		break // No content-type

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && true:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSONDefault = &dest

	}

	return response, nil
}

// ParseUpdateTypePreferenceResponse parses an HTTP response from a UpdateTypePreferenceWithResponse call
func ParseUpdateTypePreferenceResponse(rsp *http.Response) (*UpdateTypePreferenceResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &UpdateTypePreferenceResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest TypePreferenceResource
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && true:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSONDefault = &dest

	}

	return response, nil
}

// ParseDescribeRealtimeConnectionResponse parses an HTTP response from a DescribeRealtimeConnectionWithResponse call
func ParseDescribeRealtimeConnectionResponse(rsp *http.Response) (*DescribeRealtimeConnectionResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &DescribeRealtimeConnectionResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest RealtimeConnectionResource
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && true:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSONDefault = &dest

	}

	return response, nil
}

// ParseAuthorizeRealtimeChannelResponse parses an HTTP response from a AuthorizeRealtimeChannelWithResponse call
func ParseAuthorizeRealtimeChannelResponse(rsp *http.Response) (*AuthorizeRealtimeChannelResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &AuthorizeRealtimeChannelResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest RealtimeAuthResource
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && true:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSONDefault = &dest

	}

	return response, nil
}
