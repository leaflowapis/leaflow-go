// Package dnsv1 provides primitives to interact with the openapi HTTP API.
//
// Code generated by github.com/oapi-codegen/oapi-codegen/v2 version v2.8.0 DO NOT EDIT.
package dnsv1

import (
	"bytes"
	"context"
	"encoding/json"
	"fmt"
	"io"
	"net/http"
	"net/url"
	"strings"
	"time"

	externalRef0 "github.com/leaflowapis/leaflow-go/type/v1"
	"github.com/oapi-codegen/runtime"
	openapi_types "github.com/oapi-codegen/runtime/types"
)

// Defines values for CredentialStatus.
const (
	Active   CredentialStatus = "active"
	Disabled CredentialStatus = "disabled"
)

// Valid indicates whether the value is a known member of the CredentialStatus enum.
func (e CredentialStatus) Valid() bool {
	switch e {
	case Active:
		return true
	case Disabled:
		return true
	default:
		return false
	}
}

// Defines values for Provider.
const (
	Alidns       Provider = "alidns"
	Cloudflare   Provider = "cloudflare"
	Route53      Provider = "route53"
	Tencentcloud Provider = "tencentcloud"
)

// Valid indicates whether the value is a known member of the Provider enum.
func (e Provider) Valid() bool {
	switch e {
	case Alidns:
		return true
	case Cloudflare:
		return true
	case Route53:
		return true
	case Tencentcloud:
		return true
	default:
		return false
	}
}

// Defines values for RecordType.
const (
	A     RecordType = "A"
	AAAA  RecordType = "AAAA"
	CAA   RecordType = "CAA"
	CNAME RecordType = "CNAME"
	MX    RecordType = "MX"
	NS    RecordType = "NS"
	SRV   RecordType = "SRV"
	TXT   RecordType = "TXT"
)

// Valid indicates whether the value is a known member of the RecordType enum.
func (e RecordType) Valid() bool {
	switch e {
	case A:
		return true
	case AAAA:
		return true
	case CAA:
		return true
	case CNAME:
		return true
	case MX:
		return true
	case NS:
		return true
	case SRV:
		return true
	case TXT:
		return true
	default:
		return false
	}
}

// AliDNSConfig Alibaba Cloud DNS credentials, created under RAM
type AliDNSConfig struct {
	AccessKeyId     string `json:"access_key_id"`
	AccessKeySecret string `json:"access_key_secret"`

	// RegionId Defaults to cn-hangzhou
	RegionId *string `json:"region_id,omitempty"`
}

// CloudflareConfig Cloudflare credentials, created under My Profile then API Tokens
type CloudflareConfig struct {
	// ApiToken An API token with the Zone.DNS edit permission
	ApiToken string `json:"api_token"`

	// ZoneToken Optional, and required only when api_token is scoped to a single zone, in which case it is used to enumerate zones
	ZoneToken *string `json:"zone_token,omitempty"`
}

// CreateCredentialRequestBody `provider` selects which configuration block is read. The remaining blocks are ignored.
type CreateCredentialRequestBody struct {
	// Alidns Alibaba Cloud DNS credentials, created under RAM
	Alidns *AliDNSConfig `json:"alidns,omitempty"`

	// Cloudflare Cloudflare credentials, created under My Profile then API Tokens
	Cloudflare *CloudflareConfig `json:"cloudflare,omitempty"`

	// Name A display name for this credential
	Name string `json:"name"`

	// Provider A supported DNS provider.
	Provider Provider `json:"provider"`

	// Route53 AWS Route 53 credentials
	Route53 *Route53Config `json:"route53,omitempty"`

	// Tencentcloud Tencent Cloud DNSPod credentials, created under Access Management then Access Keys
	Tencentcloud *TencentCloudConfig `json:"tencentcloud,omitempty"`
}

// CredentialResource A registered provider credential. The credential material itself is never included.
type CredentialResource struct {
	CreatedAt time.Time `json:"created_at"`

	// Fingerprint Equal fingerprints denote the same credential material
	Fingerprint string             `json:"fingerprint"`
	Id          openapi_types.UUID `json:"id"`

	// LastVerifiedAt When the credential was last confirmed to work; null if it never has
	LastVerifiedAt *time.Time `json:"last_verified_at"`

	// LastVerifyError Why the most recent check failed; null if the most recent check succeeded
	LastVerifyError *string `json:"last_verify_error"`
	Name            string  `json:"name"`

	// Provider A supported DNS provider.
	Provider Provider `json:"provider"`

	// Redacted A partially masked fragment of the credential, for visual identification only
	Redacted string `json:"redacted"`

	// Status `active` is usable. `disabled` means the platform has disabled this credential and requires
	// contact with support.
	//
	// A project that has been suspended is reported separately, in `suspended_at`.
	Status CredentialStatus `json:"status"`

	// SuspendedAt When the credential was suspended because the project was suspended; null if it was not. Cleared when the project is restored
	SuspendedAt *time.Time `json:"suspended_at"`
	UpdatedAt   time.Time  `json:"updated_at"`
}

// CredentialStatus `active` is usable. `disabled` means the platform has disabled this credential and requires
// contact with support.
//
// A project that has been suspended is reported separately, in `suspended_at`.
type CredentialStatus string

// Error defines model for Error.
type Error = externalRef0.Error

// LengthAwarePageCredentialResource defines model for LengthAwarePageCredentialResource.
type LengthAwarePageCredentialResource struct {
	// Items The contents of this page
	Items []CredentialResource `json:"items"`

	// Limit Maximum items per page, echoing the value in the request
	Limit int64 `json:"limit"`

	// Offset Items skipped, echoing the value in the request
	Offset int64 `json:"offset"`

	// Total Total number of matching items, not only those in this page
	Total int64 `json:"total"`
}

// LengthAwarePageRecordSetResource defines model for LengthAwarePageRecordSetResource.
type LengthAwarePageRecordSetResource struct {
	// Items The contents of this page
	Items []RecordSetResource `json:"items"`

	// Limit Maximum items per page, echoing the value in the request
	Limit int64 `json:"limit"`

	// Offset Items skipped, echoing the value in the request
	Offset int64 `json:"offset"`

	// Total Total number of matching items, not only those in this page
	Total int64 `json:"total"`
}

// ModifyRecordSetRequestBody Names the values to add and to remove. Anything not named is left in place. At least one of `add` and `remove` is required, and no value may appear in both.
type ModifyRecordSetRequestBody struct {
	// Add The values to add. Values already present are rejected by the provider. Syntax as described on RecordSetResource.values
	Add []string `json:"add,omitempty"`

	// Remove The values to remove. Values that are not present are skipped. Syntax as described on RecordSetResource.values
	Remove []string `json:"remove,omitempty"`

	// Ttl Time to live, in seconds, applied to the values in `add`. 0 leaves the choice to the provider. The time to live of values already in the set is not changed; use `PUT` for that
	Ttl *int64 `json:"ttl,omitempty"`
}

// Provider A supported DNS provider.
type Provider string

// RecordSetResource Every record sharing a name and a type.
type RecordSetResource struct {
	// Name The name relative to the domain. `@` denotes the domain itself
	Name string `json:"name"`

	// Ttl Time to live, in seconds. 0 leaves the choice to the provider
	Ttl int64 `json:"ttl"`

	// Type A DNS record type.
	//
	// The listed types are those this API supports uniformly across every provider. HTTPS and
	// SVCB are not yet available.
	Type RecordType `json:"type"`

	// Values Every value in this record set, in unescaped zone-file RDATA syntax:
	//
	// - `A`, `AAAA`: `192.0.2.1`, `2001:db8::1`
	// - `CNAME`, `NS`: `target.example.com.`, absolute, with a trailing dot
	// - `TXT`: the text itself, neither quoted nor escaped
	// - `MX`: `10 mail.example.com.`
	// - `SRV`: `10 20 443 target.example.com.`
	// - `CAA`: `0 issue "letsencrypt.org"`
	Values []string `json:"values"`

	// Zone The domain in absolute form, with a trailing dot
	Zone string `json:"zone"`
}

// RecordType A DNS record type.
//
// The listed types are those this API supports uniformly across every provider. HTTPS and
// SVCB are not yet available.
type RecordType string

// RenameCredentialRequestBody defines model for RenameCredentialRequestBody.
type RenameCredentialRequestBody struct {
	Name string `json:"name"`
}

// Route53Config AWS Route 53 credentials
type Route53Config struct {
	AccessKeyId string `json:"access_key_id"`

	// Region Defaults to us-east-1. Route 53 is a global service, so this affects request signing only
	Region          *string `json:"region,omitempty"`
	SecretAccessKey string  `json:"secret_access_key"`

	// SessionToken Optional, and required only for temporary STS credentials. Such credentials expire, after which every operation against this provider fails
	SessionToken *string `json:"session_token,omitempty"`
}

// SetRecordSetRequestBody `values` states the final contents of the record set. Values omitted here are removed.
type SetRecordSetRequestBody struct {
	// Ttl Time to live, in seconds. 0 leaves the choice to the provider. Providers enforce their own minimum and raise lower values to it
	Ttl *int64 `json:"ttl,omitempty"`

	// Values The final contents of the record set. Syntax as described on RecordSetResource.values
	Values []string `json:"values"`
}

// TencentCloudConfig Tencent Cloud DNSPod credentials, created under Access Management then Access Keys
type TencentCloudConfig struct {
	SecretId  string `json:"secret_id"`
	SecretKey string `json:"secret_key"`
}

// ZoneListResponseBody defines model for ZoneListResponseBody.
type ZoneListResponseBody struct {
	Items []ZoneResource `json:"items"`

	// Stale True when at least one provider could not be reached while producing this listing, so
	// recent additions or removals may be missing.
	//
	// The listing remains usable and write operations are unaffected.
	Stale bool `json:"stale"`
}

// ZoneResource defines model for ZoneResource.
type ZoneResource struct {
	// Authoritative Whether this entry is the one currently serving queries for the domain.
	//
	// `null` means no determination was made: either the domain appears only once, or its
	// delegation does not identify any registered provider. While this is `null` and the
	// domain appears more than once, write operations require an explicit `credential_id`.
	Authoritative *bool `json:"authoritative"`

	// CredentialId The credential through which this domain is reachable
	CredentialId openapi_types.UUID `json:"credential_id"`

	// Provider A supported DNS provider.
	Provider Provider `json:"provider"`

	// Zone The domain in absolute form, with a trailing dot
	Zone string `json:"zone"`
}

// ListCredentialsParams defines parameters for ListCredentials.
type ListCredentialsParams struct {
	// Limit Maximum number of items to return in this page
	Limit *int64 `form:"limit,omitempty" json:"limit,omitempty"`

	// Offset Number of items to skip
	Offset *int64 `form:"offset,omitempty" json:"offset,omitempty"`

	// Provider Return only credentials for this provider; omit for all providers
	Provider *Provider `form:"provider,omitempty" json:"provider,omitempty"`
}

// ListZonesParams defines parameters for ListZones.
type ListZonesParams struct {
	// CredentialId Return only domains reachable through this credential
	CredentialId *openapi_types.UUID `form:"credential_id,omitempty" json:"credential_id,omitempty"`

	// Refresh Return the current set of domains rather than a possibly slightly stale one. Slower, and consumes provider API quota
	Refresh *bool `form:"refresh,omitempty" json:"refresh,omitempty"`
}

// ListRecordsParams defines parameters for ListRecords.
type ListRecordsParams struct {
	// CredentialId Which credential to use. When omitted, the credential is determined from the domain; see ZONE_AMBIGUOUS
	CredentialId *openapi_types.UUID `form:"credential_id,omitempty" json:"credential_id,omitempty"`

	// Name Return only record sets with this name, given relative to the domain. `@` denotes the domain itself
	Name *string `form:"name,omitempty" json:"name,omitempty"`

	// Type Return only record sets of this type
	Type *RecordType `form:"type,omitempty" json:"type,omitempty"`

	// Limit Maximum number of items to return in this page
	Limit *int64 `form:"limit,omitempty" json:"limit,omitempty"`

	// Offset Number of items to skip
	Offset *int64 `form:"offset,omitempty" json:"offset,omitempty"`
}

// DeleteRecordSetParams defines parameters for DeleteRecordSet.
type DeleteRecordSetParams struct {
	// CredentialId Which credential to use. When omitted, the credential is determined from the domain; see ZONE_AMBIGUOUS
	CredentialId *openapi_types.UUID `form:"credential_id,omitempty" json:"credential_id,omitempty"`
}

// ModifyRecordSetParams defines parameters for ModifyRecordSet.
type ModifyRecordSetParams struct {
	// CredentialId Which credential to use. When omitted, the credential is determined from the domain; see ZONE_AMBIGUOUS
	CredentialId *openapi_types.UUID `form:"credential_id,omitempty" json:"credential_id,omitempty"`
}

// SetRecordSetParams defines parameters for SetRecordSet.
type SetRecordSetParams struct {
	// CredentialId Which credential to use. When omitted, the credential is determined from the domain; see ZONE_AMBIGUOUS
	CredentialId *openapi_types.UUID `form:"credential_id,omitempty" json:"credential_id,omitempty"`
}

// CreateCredentialJSONRequestBody defines body for CreateCredential for application/json ContentType.
type CreateCredentialJSONRequestBody = CreateCredentialRequestBody

// RenameCredentialJSONRequestBody defines body for RenameCredential for application/json ContentType.
type RenameCredentialJSONRequestBody = RenameCredentialRequestBody

// ModifyRecordSetJSONRequestBody defines body for ModifyRecordSet for application/json ContentType.
type ModifyRecordSetJSONRequestBody = ModifyRecordSetRequestBody

// SetRecordSetJSONRequestBody defines body for SetRecordSet for application/json ContentType.
type SetRecordSetJSONRequestBody = SetRecordSetRequestBody

// RequestEditorFn is the function signature for the RequestEditor callback function
type RequestEditorFn func(ctx context.Context, req *http.Request) error

// Doer performs HTTP requests.
//
// The standard http.Client implements this interface.
type HttpRequestDoer interface {
	Do(req *http.Request) (*http.Response, error)
}

// Client which conforms to the OpenAPI3 specification for this service.
type Client struct {
	// The endpoint of the server conforming to this interface, with scheme,
	// https://api.deepmap.com for example. This can contain a path relative
	// to the server, such as https://api.deepmap.com/dev-test, and all the
	// paths in the swagger spec will be appended to the server.
	Server string

	// Doer for performing requests, typically a *http.Client with any
	// customized settings, such as certificate chains.
	Client HttpRequestDoer

	// A list of callbacks for modifying requests which are generated before sending over
	// the network.
	RequestEditors []RequestEditorFn
}

// ClientOption allows setting custom parameters during construction
type ClientOption func(*Client) error

// Creates a new Client, with reasonable defaults
func NewClient(server string, opts ...ClientOption) (*Client, error) {
	// create a client with sane default values
	client := Client{
		Server: server,
	}
	// mutate client and add all optional params
	for _, o := range opts {
		if err := o(&client); err != nil {
			return nil, err
		}
	}
	// ensure the server URL always has a trailing slash
	if !strings.HasSuffix(client.Server, "/") {
		client.Server += "/"
	}
	// create httpClient, if not already present
	if client.Client == nil {
		client.Client = &http.Client{}
	}
	return &client, nil
}

// WithHTTPClient allows overriding the default Doer, which is
// automatically created using http.Client. This is useful for tests.
func WithHTTPClient(doer HttpRequestDoer) ClientOption {
	return func(c *Client) error {
		c.Client = doer
		return nil
	}
}

// WithRequestEditorFn allows setting up a callback function, which will be
// called right before sending the request. This can be used to mutate the request.
func WithRequestEditorFn(fn RequestEditorFn) ClientOption {
	return func(c *Client) error {
		c.RequestEditors = append(c.RequestEditors, fn)
		return nil
	}
}

// The interface specification for the client above.
type ClientInterface interface {

	// ListCredentials List provider credentials
	//
	// Corresponds with GET /api/v1/credentials (the `ListCredentials` operationId).
	ListCredentials(ctx context.Context, params *ListCredentialsParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// CreateCredentialWithBody Register a provider credential
	//
	// The credential is validated against the provider before it is accepted. A credential that
	// cannot list domains is rejected with `CREDENTIAL_REJECTED_UPSTREAM` and is not stored.
	//
	// `provider` selects which configuration block is required: `provider: cloudflare` requires
	// the `cloudflare` block, and the remaining blocks are ignored. Omitting the matching block
	// returns `CREDENTIAL_CONFIG_MISSING`.
	//
	// Registering a credential that is already held by this project returns
	// `CREDENTIAL_DUPLICATE`. Provider rate limits apply per credential, so a second copy grants
	// no additional capacity.
	//
	// The submitted credential material is not returned by this or any other operation.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /api/v1/credentials (the `CreateCredential` operationId).
	CreateCredentialWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// CreateCredential Register a provider credential
	//
	// The credential is validated against the provider before it is accepted. A credential that
	// cannot list domains is rejected with `CREDENTIAL_REJECTED_UPSTREAM` and is not stored.
	//
	// `provider` selects which configuration block is required: `provider: cloudflare` requires
	// the `cloudflare` block, and the remaining blocks are ignored. Omitting the matching block
	// returns `CREDENTIAL_CONFIG_MISSING`.
	//
	// Registering a credential that is already held by this project returns
	// `CREDENTIAL_DUPLICATE`. Provider rate limits apply per credential, so a second copy grants
	// no additional capacity.
	//
	// The submitted credential material is not returned by this or any other operation.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /api/v1/credentials (the `CreateCredential` operationId).
	CreateCredential(ctx context.Context, body CreateCredentialJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// DeleteCredential Delete a provider credential
	//
	// Domains reachable only through this credential stop appearing in `GET /zones` and can no
	// longer be read or written through this API.
	//
	// **No DNS record is modified.** Deleting a credential withdraws this project's access to the
	// provider account; it does not remove anything that was published there.
	//
	// Corresponds with DELETE /api/v1/credentials/{credentialId} (the `DeleteCredential` operationId).
	DeleteCredential(ctx context.Context, credentialId openapi_types.UUID, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetCredential Get a provider credential
	//
	// Corresponds with GET /api/v1/credentials/{credentialId} (the `GetCredential` operationId).
	GetCredential(ctx context.Context, credentialId openapi_types.UUID, reqEditors ...RequestEditorFn) (*http.Response, error)

	// RenameCredentialWithBody Rename a provider credential
	//
	// Only the display name can be changed. To use different credential material, delete this
	// credential and register a new one: a credential's identity does not change over its
	// lifetime.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with PATCH /api/v1/credentials/{credentialId} (the `RenameCredential` operationId).
	RenameCredentialWithBody(ctx context.Context, credentialId openapi_types.UUID, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// RenameCredential Rename a provider credential
	//
	// Only the display name can be changed. To use different credential material, delete this
	// credential and register a new one: a credential's identity does not change over its
	// lifetime.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with PATCH /api/v1/credentials/{credentialId} (the `RenameCredential` operationId).
	RenameCredential(ctx context.Context, credentialId openapi_types.UUID, body RenameCredentialJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// VerifyCredential Re-validate a provider credential
	//
	// Checks the credential against the provider and records the outcome in `last_verified_at`
	// and `last_verify_error`.
	//
	// A credential is validated when it is registered. This operation exists for credentials whose
	// permissions or keys were subsequently changed at the provider, which is a change this API is
	// not notified of.
	//
	// **A failed check returns 200, not an error.** The outcome is reported in
	// `last_verify_error`: the request itself succeeded, and the answer it obtained is that the
	// credential is no longer usable.
	//
	// Corresponds with POST /api/v1/credentials/{credentialId}/verification (the `VerifyCredential` operationId).
	VerifyCredential(ctx context.Context, credentialId openapi_types.UUID, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ListZones List domains
	//
	// Lists every domain reachable through the credentials registered by this project.
	//
	// Listings may briefly lag behind domains added or removed at the provider. Set `refresh` for
	// an up-to-date answer.
	//
	// ## A domain that appears more than once
	//
	// While a domain is being migrated it commonly exists in both provider accounts at once. Such
	// a domain is returned once per credential, with the same `zone` and different
	// `credential_id`.
	//
	// `authoritative` reports which entry is the one currently serving queries, determined from
	// the domain's delegation. `null` means no determination was made: either the domain appears
	// only once, or its delegation does not identify any of the registered providers — for
	// example when custom nameservers are in use, or the domain is not delegated at all.
	//
	// **Write operations are refused while the determination is `null` and the domain appears more
	// than once**, with `ZONE_AMBIGUOUS`. Supply `credential_id` on the write request to select a
	// credential explicitly.
	//
	// Corresponds with GET /api/v1/zones (the `ListZones` operationId).
	ListZones(ctx context.Context, params *ListZonesParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ListRecords List record sets
	//
	// Returns the domain's records grouped into record sets: all records sharing a name and a type
	// are reported as one entry, with their values in `values`.
	//
	// `limit` and `offset` reduce the size of the response but not the time taken to produce it.
	// For domains with many records, filter by `name` or `type`.
	//
	// Corresponds with GET /api/v1/zones/{zone}/records (the `ListRecords` operationId).
	ListRecords(ctx context.Context, zone string, params *ListRecordsParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// DeleteRecordSet Delete a record set
	//
	// Removes every record under this name and type. **To remove part of a set, use `PATCH`**,
	// which is safe to issue concurrently; this operation and `PUT` are not.
	//
	// Providers reject removal of the last NS record set at the apex of a domain, which would
	// withdraw the domain from DNS entirely.
	//
	// Corresponds with DELETE /api/v1/zones/{zone}/records/{name}/{type} (the `DeleteRecordSet` operationId).
	DeleteRecordSet(ctx context.Context, zone string, name string, pType RecordType, params *DeleteRecordSetParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetRecordSet Get a record set
	//
	// Returns `RECORD_SET_NOT_FOUND` when no record of this type exists under this name.
	//
	// Corresponds with GET /api/v1/zones/{zone}/records/{name}/{type} (the `GetRecordSet` operationId).
	GetRecordSet(ctx context.Context, zone string, name string, pType RecordType, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ModifyRecordSetWithBody Add or remove values in a record set
	//
	// **Names values to add and remove; anything not named is left in place.** Nothing is read
	// first, so this is the only operation on a record set safe to issue concurrently. The set is
	// created if it does not exist.
	//
	// Certificate issuance must use this operation for both the challenge record and its cleanup.
	// `PUT` and `DELETE` state or remove the whole set, so a concurrent issuance's challenge
	// record is discarded — with every request returning 2xx, and the failure surfacing as the
	// second certificate failing validation.
	//
	// `add` is applied before `remove`, so a request carrying both changes a value without the
	// name ever resolving without it: the set briefly holds one value too many rather than one too
	// few. The reverse order, as two requests, leaves a window in which the value is absent — for
	// a single-valued set, the name does not resolve at all — and an addition that then fails
	// leaves it gone.
	//
	// A value appearing in both `add` and `remove` is rejected rather than resolved in one
	// direction.
	//
	// Values in `remove` that are absent are skipped, so a retried cleanup reaches the same
	// result. Values in `add` that are already present are rejected by the provider: a record set
	// cannot hold the same value twice.
	//
	// Removing every value leaves an empty set, reported as `values: []`, not `RECORD_SET_NOT_FOUND`.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with PATCH /api/v1/zones/{zone}/records/{name}/{type} (the `ModifyRecordSet` operationId).
	ModifyRecordSetWithBody(ctx context.Context, zone string, name string, pType RecordType, params *ModifyRecordSetParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ModifyRecordSet Add or remove values in a record set
	//
	// **Names values to add and remove; anything not named is left in place.** Nothing is read
	// first, so this is the only operation on a record set safe to issue concurrently. The set is
	// created if it does not exist.
	//
	// Certificate issuance must use this operation for both the challenge record and its cleanup.
	// `PUT` and `DELETE` state or remove the whole set, so a concurrent issuance's challenge
	// record is discarded — with every request returning 2xx, and the failure surfacing as the
	// second certificate failing validation.
	//
	// `add` is applied before `remove`, so a request carrying both changes a value without the
	// name ever resolving without it: the set briefly holds one value too many rather than one too
	// few. The reverse order, as two requests, leaves a window in which the value is absent — for
	// a single-valued set, the name does not resolve at all — and an addition that then fails
	// leaves it gone.
	//
	// A value appearing in both `add` and `remove` is rejected rather than resolved in one
	// direction.
	//
	// Values in `remove` that are absent are skipped, so a retried cleanup reaches the same
	// result. Values in `add` that are already present are rejected by the provider: a record set
	// cannot hold the same value twice.
	//
	// Removing every value leaves an empty set, reported as `values: []`, not `RECORD_SET_NOT_FOUND`.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with PATCH /api/v1/zones/{zone}/records/{name}/{type} (the `ModifyRecordSet` operationId).
	ModifyRecordSet(ctx context.Context, zone string, name string, pType RecordType, params *ModifyRecordSetParams, body ModifyRecordSetJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// SetRecordSetWithBody Replace a record set
	//
	// **This replaces the entire record set.** After this request, the only records under this
	// name and type are the ones listed in `values`; any value present beforehand and absent here
	// is removed.
	//
	// The record set is created if it does not exist, so this operation both creates and replaces,
	// and is idempotent.
	//
	// Two concurrent requests against the same domain conflict; the second receives `ZONE_BUSY`
	// and may be retried.
	//
	// **Use `PATCH` to add or remove individual values.** Reading the set and submitting a
	// modified list is a read-modify-write: values added by anything else between those two steps
	// are stated to be absent, and are therefore removed, with both requests returning 200.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with PUT /api/v1/zones/{zone}/records/{name}/{type} (the `SetRecordSet` operationId).
	SetRecordSetWithBody(ctx context.Context, zone string, name string, pType RecordType, params *SetRecordSetParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// SetRecordSet Replace a record set
	//
	// **This replaces the entire record set.** After this request, the only records under this
	// name and type are the ones listed in `values`; any value present beforehand and absent here
	// is removed.
	//
	// The record set is created if it does not exist, so this operation both creates and replaces,
	// and is idempotent.
	//
	// Two concurrent requests against the same domain conflict; the second receives `ZONE_BUSY`
	// and may be retried.
	//
	// **Use `PATCH` to add or remove individual values.** Reading the set and submitting a
	// modified list is a read-modify-write: values added by anything else between those two steps
	// are stated to be absent, and are therefore removed, with both requests returning 200.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with PUT /api/v1/zones/{zone}/records/{name}/{type} (the `SetRecordSet` operationId).
	SetRecordSet(ctx context.Context, zone string, name string, pType RecordType, params *SetRecordSetParams, body SetRecordSetJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)
}

// ListCredentials List provider credentials
//
// Corresponds with GET /api/v1/credentials (the `ListCredentials` operationId).
func (c *Client) ListCredentials(ctx context.Context, params *ListCredentialsParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewListCredentialsRequest(c.Server, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// CreateCredentialWithBody Register a provider credential
//
// The credential is validated against the provider before it is accepted. A credential that
// cannot list domains is rejected with `CREDENTIAL_REJECTED_UPSTREAM` and is not stored.
//
// `provider` selects which configuration block is required: `provider: cloudflare` requires
// the `cloudflare` block, and the remaining blocks are ignored. Omitting the matching block
// returns `CREDENTIAL_CONFIG_MISSING`.
//
// Registering a credential that is already held by this project returns
// `CREDENTIAL_DUPLICATE`. Provider rate limits apply per credential, so a second copy grants
// no additional capacity.
//
// The submitted credential material is not returned by this or any other operation.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /api/v1/credentials (the `CreateCredential` operationId).
func (c *Client) CreateCredentialWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewCreateCredentialRequestWithBody(c.Server, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// CreateCredential Register a provider credential
//
// The credential is validated against the provider before it is accepted. A credential that
// cannot list domains is rejected with `CREDENTIAL_REJECTED_UPSTREAM` and is not stored.
//
// `provider` selects which configuration block is required: `provider: cloudflare` requires
// the `cloudflare` block, and the remaining blocks are ignored. Omitting the matching block
// returns `CREDENTIAL_CONFIG_MISSING`.
//
// Registering a credential that is already held by this project returns
// `CREDENTIAL_DUPLICATE`. Provider rate limits apply per credential, so a second copy grants
// no additional capacity.
//
// The submitted credential material is not returned by this or any other operation.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /api/v1/credentials (the `CreateCredential` operationId).
func (c *Client) CreateCredential(ctx context.Context, body CreateCredentialJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewCreateCredentialRequest(c.Server, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// DeleteCredential Delete a provider credential
//
// Domains reachable only through this credential stop appearing in `GET /zones` and can no
// longer be read or written through this API.
//
// **No DNS record is modified.** Deleting a credential withdraws this project's access to the
// provider account; it does not remove anything that was published there.
//
// Corresponds with DELETE /api/v1/credentials/{credentialId} (the `DeleteCredential` operationId).
func (c *Client) DeleteCredential(ctx context.Context, credentialId openapi_types.UUID, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewDeleteCredentialRequest(c.Server, credentialId)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetCredential Get a provider credential
//
// Corresponds with GET /api/v1/credentials/{credentialId} (the `GetCredential` operationId).
func (c *Client) GetCredential(ctx context.Context, credentialId openapi_types.UUID, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetCredentialRequest(c.Server, credentialId)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// RenameCredentialWithBody Rename a provider credential
//
// Only the display name can be changed. To use different credential material, delete this
// credential and register a new one: a credential's identity does not change over its
// lifetime.
//
// Takes any type of body and a specified content type.
//
// Corresponds with PATCH /api/v1/credentials/{credentialId} (the `RenameCredential` operationId).
func (c *Client) RenameCredentialWithBody(ctx context.Context, credentialId openapi_types.UUID, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewRenameCredentialRequestWithBody(c.Server, credentialId, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// RenameCredential Rename a provider credential
//
// Only the display name can be changed. To use different credential material, delete this
// credential and register a new one: a credential's identity does not change over its
// lifetime.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with PATCH /api/v1/credentials/{credentialId} (the `RenameCredential` operationId).
func (c *Client) RenameCredential(ctx context.Context, credentialId openapi_types.UUID, body RenameCredentialJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewRenameCredentialRequest(c.Server, credentialId, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// VerifyCredential Re-validate a provider credential
//
// Checks the credential against the provider and records the outcome in `last_verified_at`
// and `last_verify_error`.
//
// A credential is validated when it is registered. This operation exists for credentials whose
// permissions or keys were subsequently changed at the provider, which is a change this API is
// not notified of.
//
// **A failed check returns 200, not an error.** The outcome is reported in
// `last_verify_error`: the request itself succeeded, and the answer it obtained is that the
// credential is no longer usable.
//
// Corresponds with POST /api/v1/credentials/{credentialId}/verification (the `VerifyCredential` operationId).
func (c *Client) VerifyCredential(ctx context.Context, credentialId openapi_types.UUID, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewVerifyCredentialRequest(c.Server, credentialId)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ListZones List domains
//
// Lists every domain reachable through the credentials registered by this project.
//
// Listings may briefly lag behind domains added or removed at the provider. Set `refresh` for
// an up-to-date answer.
//
// ## A domain that appears more than once
//
// While a domain is being migrated it commonly exists in both provider accounts at once. Such
// a domain is returned once per credential, with the same `zone` and different
// `credential_id`.
//
// `authoritative` reports which entry is the one currently serving queries, determined from
// the domain's delegation. `null` means no determination was made: either the domain appears
// only once, or its delegation does not identify any of the registered providers — for
// example when custom nameservers are in use, or the domain is not delegated at all.
//
// **Write operations are refused while the determination is `null` and the domain appears more
// than once**, with `ZONE_AMBIGUOUS`. Supply `credential_id` on the write request to select a
// credential explicitly.
//
// Corresponds with GET /api/v1/zones (the `ListZones` operationId).
func (c *Client) ListZones(ctx context.Context, params *ListZonesParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewListZonesRequest(c.Server, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ListRecords List record sets
//
// Returns the domain's records grouped into record sets: all records sharing a name and a type
// are reported as one entry, with their values in `values`.
//
// `limit` and `offset` reduce the size of the response but not the time taken to produce it.
// For domains with many records, filter by `name` or `type`.
//
// Corresponds with GET /api/v1/zones/{zone}/records (the `ListRecords` operationId).
func (c *Client) ListRecords(ctx context.Context, zone string, params *ListRecordsParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewListRecordsRequest(c.Server, zone, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// DeleteRecordSet Delete a record set
//
// Removes every record under this name and type. **To remove part of a set, use `PATCH`**,
// which is safe to issue concurrently; this operation and `PUT` are not.
//
// Providers reject removal of the last NS record set at the apex of a domain, which would
// withdraw the domain from DNS entirely.
//
// Corresponds with DELETE /api/v1/zones/{zone}/records/{name}/{type} (the `DeleteRecordSet` operationId).
func (c *Client) DeleteRecordSet(ctx context.Context, zone string, name string, pType RecordType, params *DeleteRecordSetParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewDeleteRecordSetRequest(c.Server, zone, name, pType, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetRecordSet Get a record set
//
// Returns `RECORD_SET_NOT_FOUND` when no record of this type exists under this name.
//
// Corresponds with GET /api/v1/zones/{zone}/records/{name}/{type} (the `GetRecordSet` operationId).
func (c *Client) GetRecordSet(ctx context.Context, zone string, name string, pType RecordType, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetRecordSetRequest(c.Server, zone, name, pType)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ModifyRecordSetWithBody Add or remove values in a record set
//
// **Names values to add and remove; anything not named is left in place.** Nothing is read
// first, so this is the only operation on a record set safe to issue concurrently. The set is
// created if it does not exist.
//
// Certificate issuance must use this operation for both the challenge record and its cleanup.
// `PUT` and `DELETE` state or remove the whole set, so a concurrent issuance's challenge
// record is discarded — with every request returning 2xx, and the failure surfacing as the
// second certificate failing validation.
//
// `add` is applied before `remove`, so a request carrying both changes a value without the
// name ever resolving without it: the set briefly holds one value too many rather than one too
// few. The reverse order, as two requests, leaves a window in which the value is absent — for
// a single-valued set, the name does not resolve at all — and an addition that then fails
// leaves it gone.
//
// A value appearing in both `add` and `remove` is rejected rather than resolved in one
// direction.
//
// Values in `remove` that are absent are skipped, so a retried cleanup reaches the same
// result. Values in `add` that are already present are rejected by the provider: a record set
// cannot hold the same value twice.
//
// Removing every value leaves an empty set, reported as `values: []`, not `RECORD_SET_NOT_FOUND`.
//
// Takes any type of body and a specified content type.
//
// Corresponds with PATCH /api/v1/zones/{zone}/records/{name}/{type} (the `ModifyRecordSet` operationId).
func (c *Client) ModifyRecordSetWithBody(ctx context.Context, zone string, name string, pType RecordType, params *ModifyRecordSetParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewModifyRecordSetRequestWithBody(c.Server, zone, name, pType, params, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ModifyRecordSet Add or remove values in a record set
//
// **Names values to add and remove; anything not named is left in place.** Nothing is read
// first, so this is the only operation on a record set safe to issue concurrently. The set is
// created if it does not exist.
//
// Certificate issuance must use this operation for both the challenge record and its cleanup.
// `PUT` and `DELETE` state or remove the whole set, so a concurrent issuance's challenge
// record is discarded — with every request returning 2xx, and the failure surfacing as the
// second certificate failing validation.
//
// `add` is applied before `remove`, so a request carrying both changes a value without the
// name ever resolving without it: the set briefly holds one value too many rather than one too
// few. The reverse order, as two requests, leaves a window in which the value is absent — for
// a single-valued set, the name does not resolve at all — and an addition that then fails
// leaves it gone.
//
// A value appearing in both `add` and `remove` is rejected rather than resolved in one
// direction.
//
// Values in `remove` that are absent are skipped, so a retried cleanup reaches the same
// result. Values in `add` that are already present are rejected by the provider: a record set
// cannot hold the same value twice.
//
// Removing every value leaves an empty set, reported as `values: []`, not `RECORD_SET_NOT_FOUND`.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with PATCH /api/v1/zones/{zone}/records/{name}/{type} (the `ModifyRecordSet` operationId).
func (c *Client) ModifyRecordSet(ctx context.Context, zone string, name string, pType RecordType, params *ModifyRecordSetParams, body ModifyRecordSetJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewModifyRecordSetRequest(c.Server, zone, name, pType, params, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// SetRecordSetWithBody Replace a record set
//
// **This replaces the entire record set.** After this request, the only records under this
// name and type are the ones listed in `values`; any value present beforehand and absent here
// is removed.
//
// The record set is created if it does not exist, so this operation both creates and replaces,
// and is idempotent.
//
// Two concurrent requests against the same domain conflict; the second receives `ZONE_BUSY`
// and may be retried.
//
// **Use `PATCH` to add or remove individual values.** Reading the set and submitting a
// modified list is a read-modify-write: values added by anything else between those two steps
// are stated to be absent, and are therefore removed, with both requests returning 200.
//
// Takes any type of body and a specified content type.
//
// Corresponds with PUT /api/v1/zones/{zone}/records/{name}/{type} (the `SetRecordSet` operationId).
func (c *Client) SetRecordSetWithBody(ctx context.Context, zone string, name string, pType RecordType, params *SetRecordSetParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewSetRecordSetRequestWithBody(c.Server, zone, name, pType, params, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// SetRecordSet Replace a record set
//
// **This replaces the entire record set.** After this request, the only records under this
// name and type are the ones listed in `values`; any value present beforehand and absent here
// is removed.
//
// The record set is created if it does not exist, so this operation both creates and replaces,
// and is idempotent.
//
// Two concurrent requests against the same domain conflict; the second receives `ZONE_BUSY`
// and may be retried.
//
// **Use `PATCH` to add or remove individual values.** Reading the set and submitting a
// modified list is a read-modify-write: values added by anything else between those two steps
// are stated to be absent, and are therefore removed, with both requests returning 200.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with PUT /api/v1/zones/{zone}/records/{name}/{type} (the `SetRecordSet` operationId).
func (c *Client) SetRecordSet(ctx context.Context, zone string, name string, pType RecordType, params *SetRecordSetParams, body SetRecordSetJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewSetRecordSetRequest(c.Server, zone, name, pType, params, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// NewListCredentialsRequest constructs an http.Request for the ListCredentials method
func NewListCredentialsRequest(server string, params *ListCredentialsParams) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/api/v1/credentials")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if params.Limit != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", false, "limit", *params.Limit, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "integer", Format: "int64"}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Offset != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", false, "offset", *params.Offset, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "integer", Format: "int64"}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Provider != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", false, "provider", *params.Provider, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewCreateCredentialRequest calls the generic CreateCredential builder with application/json body
func NewCreateCredentialRequest(server string, body CreateCredentialJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewCreateCredentialRequestWithBody(server, "application/json", bodyReader)
}

// NewCreateCredentialRequestWithBody constructs an http.Request for the CreateCredential method, with any body, and a specified content type
func NewCreateCredentialRequestWithBody(server string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/api/v1/credentials")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewDeleteCredentialRequest constructs an http.Request for the DeleteCredential method
func NewDeleteCredentialRequest(server string, credentialId openapi_types.UUID) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "credentialId", credentialId, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: "uuid"})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/api/v1/credentials/%s", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodDelete, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewGetCredentialRequest constructs an http.Request for the GetCredential method
func NewGetCredentialRequest(server string, credentialId openapi_types.UUID) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "credentialId", credentialId, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: "uuid"})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/api/v1/credentials/%s", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewRenameCredentialRequest calls the generic RenameCredential builder with application/json body
func NewRenameCredentialRequest(server string, credentialId openapi_types.UUID, body RenameCredentialJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewRenameCredentialRequestWithBody(server, credentialId, "application/json", bodyReader)
}

// NewRenameCredentialRequestWithBody constructs an http.Request for the RenameCredential method, with any body, and a specified content type
func NewRenameCredentialRequestWithBody(server string, credentialId openapi_types.UUID, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "credentialId", credentialId, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: "uuid"})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/api/v1/credentials/%s", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPatch, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewVerifyCredentialRequest constructs an http.Request for the VerifyCredential method
func NewVerifyCredentialRequest(server string, credentialId openapi_types.UUID) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "credentialId", credentialId, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: "uuid"})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/api/v1/credentials/%s/verification", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewListZonesRequest constructs an http.Request for the ListZones method
func NewListZonesRequest(server string, params *ListZonesParams) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/api/v1/zones")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if params.CredentialId != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", false, "credential_id", *params.CredentialId, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: "uuid"}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Refresh != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", false, "refresh", *params.Refresh, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "boolean", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewListRecordsRequest constructs an http.Request for the ListRecords method
func NewListRecordsRequest(server string, zone string, params *ListRecordsParams) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "zone", zone, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/api/v1/zones/%s/records", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if params.CredentialId != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", false, "credential_id", *params.CredentialId, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: "uuid"}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Name != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", false, "name", *params.Name, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Type != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", false, "type", *params.Type, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Limit != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", false, "limit", *params.Limit, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "integer", Format: "int64"}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Offset != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", false, "offset", *params.Offset, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "integer", Format: "int64"}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewDeleteRecordSetRequest constructs an http.Request for the DeleteRecordSet method
func NewDeleteRecordSetRequest(server string, zone string, name string, pType RecordType, params *DeleteRecordSetParams) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "zone", zone, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	var pathParam1 string

	pathParam1, err = runtime.StyleParamWithOptions("simple", false, "name", name, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	var pathParam2 string

	pathParam2, err = runtime.StyleParamWithOptions("simple", false, "type", pType, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/api/v1/zones/%s/records/%s/%s", pathParam0, pathParam1, pathParam2)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if params.CredentialId != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", false, "credential_id", *params.CredentialId, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: "uuid"}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodDelete, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewGetRecordSetRequest constructs an http.Request for the GetRecordSet method
func NewGetRecordSetRequest(server string, zone string, name string, pType RecordType) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "zone", zone, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	var pathParam1 string

	pathParam1, err = runtime.StyleParamWithOptions("simple", false, "name", name, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	var pathParam2 string

	pathParam2, err = runtime.StyleParamWithOptions("simple", false, "type", pType, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/api/v1/zones/%s/records/%s/%s", pathParam0, pathParam1, pathParam2)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewModifyRecordSetRequest calls the generic ModifyRecordSet builder with application/json body
func NewModifyRecordSetRequest(server string, zone string, name string, pType RecordType, params *ModifyRecordSetParams, body ModifyRecordSetJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewModifyRecordSetRequestWithBody(server, zone, name, pType, params, "application/json", bodyReader)
}

// NewModifyRecordSetRequestWithBody constructs an http.Request for the ModifyRecordSet method, with any body, and a specified content type
func NewModifyRecordSetRequestWithBody(server string, zone string, name string, pType RecordType, params *ModifyRecordSetParams, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "zone", zone, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	var pathParam1 string

	pathParam1, err = runtime.StyleParamWithOptions("simple", false, "name", name, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	var pathParam2 string

	pathParam2, err = runtime.StyleParamWithOptions("simple", false, "type", pType, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/api/v1/zones/%s/records/%s/%s", pathParam0, pathParam1, pathParam2)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if params.CredentialId != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", false, "credential_id", *params.CredentialId, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: "uuid"}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodPatch, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewSetRecordSetRequest calls the generic SetRecordSet builder with application/json body
func NewSetRecordSetRequest(server string, zone string, name string, pType RecordType, params *SetRecordSetParams, body SetRecordSetJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewSetRecordSetRequestWithBody(server, zone, name, pType, params, "application/json", bodyReader)
}

// NewSetRecordSetRequestWithBody constructs an http.Request for the SetRecordSet method, with any body, and a specified content type
func NewSetRecordSetRequestWithBody(server string, zone string, name string, pType RecordType, params *SetRecordSetParams, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "zone", zone, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	var pathParam1 string

	pathParam1, err = runtime.StyleParamWithOptions("simple", false, "name", name, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	var pathParam2 string

	pathParam2, err = runtime.StyleParamWithOptions("simple", false, "type", pType, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/api/v1/zones/%s/records/%s/%s", pathParam0, pathParam1, pathParam2)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if params.CredentialId != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", false, "credential_id", *params.CredentialId, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: "uuid"}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodPut, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

func (c *Client) applyEditors(ctx context.Context, req *http.Request, additionalEditors []RequestEditorFn) error {
	for _, r := range c.RequestEditors {
		if err := r(ctx, req); err != nil {
			return err
		}
	}
	for _, r := range additionalEditors {
		if err := r(ctx, req); err != nil {
			return err
		}
	}
	return nil
}

// ClientWithResponses builds on ClientInterface to offer response payloads
type ClientWithResponses struct {
	ClientInterface
}

// NewClientWithResponses creates a new ClientWithResponses, which wraps
// Client with return type handling
func NewClientWithResponses(server string, opts ...ClientOption) (*ClientWithResponses, error) {
	client, err := NewClient(server, opts...)
	if err != nil {
		return nil, err
	}
	return &ClientWithResponses{client}, nil
}

// WithBaseURL overrides the baseURL.
func WithBaseURL(baseURL string) ClientOption {
	return func(c *Client) error {
		newBaseURL, err := url.Parse(baseURL)
		if err != nil {
			return err
		}
		c.Server = newBaseURL.String()
		return nil
	}
}

// ClientWithResponsesInterface is the interface specification for the client with responses above.
type ClientWithResponsesInterface interface {

	// ListCredentialsWithResponse List provider credentials
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /api/v1/credentials (the `ListCredentials` operationId).
	ListCredentialsWithResponse(ctx context.Context, params *ListCredentialsParams, reqEditors ...RequestEditorFn) (*ListCredentialsResponse, error)

	// CreateCredentialWithBodyWithResponse Register a provider credential
	//
	// The credential is validated against the provider before it is accepted. A credential that
	// cannot list domains is rejected with `CREDENTIAL_REJECTED_UPSTREAM` and is not stored.
	//
	// `provider` selects which configuration block is required: `provider: cloudflare` requires
	// the `cloudflare` block, and the remaining blocks are ignored. Omitting the matching block
	// returns `CREDENTIAL_CONFIG_MISSING`.
	//
	// Registering a credential that is already held by this project returns
	// `CREDENTIAL_DUPLICATE`. Provider rate limits apply per credential, so a second copy grants
	// no additional capacity.
	//
	// The submitted credential material is not returned by this or any other operation.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /api/v1/credentials (the `CreateCredential` operationId).
	CreateCredentialWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*CreateCredentialResponse, error)

	// CreateCredentialWithResponse Register a provider credential
	//
	// The credential is validated against the provider before it is accepted. A credential that
	// cannot list domains is rejected with `CREDENTIAL_REJECTED_UPSTREAM` and is not stored.
	//
	// `provider` selects which configuration block is required: `provider: cloudflare` requires
	// the `cloudflare` block, and the remaining blocks are ignored. Omitting the matching block
	// returns `CREDENTIAL_CONFIG_MISSING`.
	//
	// Registering a credential that is already held by this project returns
	// `CREDENTIAL_DUPLICATE`. Provider rate limits apply per credential, so a second copy grants
	// no additional capacity.
	//
	// The submitted credential material is not returned by this or any other operation.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /api/v1/credentials (the `CreateCredential` operationId).
	CreateCredentialWithResponse(ctx context.Context, body CreateCredentialJSONRequestBody, reqEditors ...RequestEditorFn) (*CreateCredentialResponse, error)

	// DeleteCredentialWithResponse Delete a provider credential
	//
	// Domains reachable only through this credential stop appearing in `GET /zones` and can no
	// longer be read or written through this API.
	//
	// **No DNS record is modified.** Deleting a credential withdraws this project's access to the
	// provider account; it does not remove anything that was published there.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with DELETE /api/v1/credentials/{credentialId} (the `DeleteCredential` operationId).
	DeleteCredentialWithResponse(ctx context.Context, credentialId openapi_types.UUID, reqEditors ...RequestEditorFn) (*DeleteCredentialResponse, error)

	// GetCredentialWithResponse Get a provider credential
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /api/v1/credentials/{credentialId} (the `GetCredential` operationId).
	GetCredentialWithResponse(ctx context.Context, credentialId openapi_types.UUID, reqEditors ...RequestEditorFn) (*GetCredentialResponse, error)

	// RenameCredentialWithBodyWithResponse Rename a provider credential
	//
	// Only the display name can be changed. To use different credential material, delete this
	// credential and register a new one: a credential's identity does not change over its
	// lifetime.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with PATCH /api/v1/credentials/{credentialId} (the `RenameCredential` operationId).
	RenameCredentialWithBodyWithResponse(ctx context.Context, credentialId openapi_types.UUID, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*RenameCredentialResponse, error)

	// RenameCredentialWithResponse Rename a provider credential
	//
	// Only the display name can be changed. To use different credential material, delete this
	// credential and register a new one: a credential's identity does not change over its
	// lifetime.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with PATCH /api/v1/credentials/{credentialId} (the `RenameCredential` operationId).
	RenameCredentialWithResponse(ctx context.Context, credentialId openapi_types.UUID, body RenameCredentialJSONRequestBody, reqEditors ...RequestEditorFn) (*RenameCredentialResponse, error)

	// VerifyCredentialWithResponse Re-validate a provider credential
	//
	// Checks the credential against the provider and records the outcome in `last_verified_at`
	// and `last_verify_error`.
	//
	// A credential is validated when it is registered. This operation exists for credentials whose
	// permissions or keys were subsequently changed at the provider, which is a change this API is
	// not notified of.
	//
	// **A failed check returns 200, not an error.** The outcome is reported in
	// `last_verify_error`: the request itself succeeded, and the answer it obtained is that the
	// credential is no longer usable.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /api/v1/credentials/{credentialId}/verification (the `VerifyCredential` operationId).
	VerifyCredentialWithResponse(ctx context.Context, credentialId openapi_types.UUID, reqEditors ...RequestEditorFn) (*VerifyCredentialResponse, error)

	// ListZonesWithResponse List domains
	//
	// Lists every domain reachable through the credentials registered by this project.
	//
	// Listings may briefly lag behind domains added or removed at the provider. Set `refresh` for
	// an up-to-date answer.
	//
	// ## A domain that appears more than once
	//
	// While a domain is being migrated it commonly exists in both provider accounts at once. Such
	// a domain is returned once per credential, with the same `zone` and different
	// `credential_id`.
	//
	// `authoritative` reports which entry is the one currently serving queries, determined from
	// the domain's delegation. `null` means no determination was made: either the domain appears
	// only once, or its delegation does not identify any of the registered providers — for
	// example when custom nameservers are in use, or the domain is not delegated at all.
	//
	// **Write operations are refused while the determination is `null` and the domain appears more
	// than once**, with `ZONE_AMBIGUOUS`. Supply `credential_id` on the write request to select a
	// credential explicitly.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /api/v1/zones (the `ListZones` operationId).
	ListZonesWithResponse(ctx context.Context, params *ListZonesParams, reqEditors ...RequestEditorFn) (*ListZonesResponse, error)

	// ListRecordsWithResponse List record sets
	//
	// Returns the domain's records grouped into record sets: all records sharing a name and a type
	// are reported as one entry, with their values in `values`.
	//
	// `limit` and `offset` reduce the size of the response but not the time taken to produce it.
	// For domains with many records, filter by `name` or `type`.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /api/v1/zones/{zone}/records (the `ListRecords` operationId).
	ListRecordsWithResponse(ctx context.Context, zone string, params *ListRecordsParams, reqEditors ...RequestEditorFn) (*ListRecordsResponse, error)

	// DeleteRecordSetWithResponse Delete a record set
	//
	// Removes every record under this name and type. **To remove part of a set, use `PATCH`**,
	// which is safe to issue concurrently; this operation and `PUT` are not.
	//
	// Providers reject removal of the last NS record set at the apex of a domain, which would
	// withdraw the domain from DNS entirely.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with DELETE /api/v1/zones/{zone}/records/{name}/{type} (the `DeleteRecordSet` operationId).
	DeleteRecordSetWithResponse(ctx context.Context, zone string, name string, pType RecordType, params *DeleteRecordSetParams, reqEditors ...RequestEditorFn) (*DeleteRecordSetResponse, error)

	// GetRecordSetWithResponse Get a record set
	//
	// Returns `RECORD_SET_NOT_FOUND` when no record of this type exists under this name.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /api/v1/zones/{zone}/records/{name}/{type} (the `GetRecordSet` operationId).
	GetRecordSetWithResponse(ctx context.Context, zone string, name string, pType RecordType, reqEditors ...RequestEditorFn) (*GetRecordSetResponse, error)

	// ModifyRecordSetWithBodyWithResponse Add or remove values in a record set
	//
	// **Names values to add and remove; anything not named is left in place.** Nothing is read
	// first, so this is the only operation on a record set safe to issue concurrently. The set is
	// created if it does not exist.
	//
	// Certificate issuance must use this operation for both the challenge record and its cleanup.
	// `PUT` and `DELETE` state or remove the whole set, so a concurrent issuance's challenge
	// record is discarded — with every request returning 2xx, and the failure surfacing as the
	// second certificate failing validation.
	//
	// `add` is applied before `remove`, so a request carrying both changes a value without the
	// name ever resolving without it: the set briefly holds one value too many rather than one too
	// few. The reverse order, as two requests, leaves a window in which the value is absent — for
	// a single-valued set, the name does not resolve at all — and an addition that then fails
	// leaves it gone.
	//
	// A value appearing in both `add` and `remove` is rejected rather than resolved in one
	// direction.
	//
	// Values in `remove` that are absent are skipped, so a retried cleanup reaches the same
	// result. Values in `add` that are already present are rejected by the provider: a record set
	// cannot hold the same value twice.
	//
	// Removing every value leaves an empty set, reported as `values: []`, not `RECORD_SET_NOT_FOUND`.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with PATCH /api/v1/zones/{zone}/records/{name}/{type} (the `ModifyRecordSet` operationId).
	ModifyRecordSetWithBodyWithResponse(ctx context.Context, zone string, name string, pType RecordType, params *ModifyRecordSetParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*ModifyRecordSetResponse, error)

	// ModifyRecordSetWithResponse Add or remove values in a record set
	//
	// **Names values to add and remove; anything not named is left in place.** Nothing is read
	// first, so this is the only operation on a record set safe to issue concurrently. The set is
	// created if it does not exist.
	//
	// Certificate issuance must use this operation for both the challenge record and its cleanup.
	// `PUT` and `DELETE` state or remove the whole set, so a concurrent issuance's challenge
	// record is discarded — with every request returning 2xx, and the failure surfacing as the
	// second certificate failing validation.
	//
	// `add` is applied before `remove`, so a request carrying both changes a value without the
	// name ever resolving without it: the set briefly holds one value too many rather than one too
	// few. The reverse order, as two requests, leaves a window in which the value is absent — for
	// a single-valued set, the name does not resolve at all — and an addition that then fails
	// leaves it gone.
	//
	// A value appearing in both `add` and `remove` is rejected rather than resolved in one
	// direction.
	//
	// Values in `remove` that are absent are skipped, so a retried cleanup reaches the same
	// result. Values in `add` that are already present are rejected by the provider: a record set
	// cannot hold the same value twice.
	//
	// Removing every value leaves an empty set, reported as `values: []`, not `RECORD_SET_NOT_FOUND`.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with PATCH /api/v1/zones/{zone}/records/{name}/{type} (the `ModifyRecordSet` operationId).
	ModifyRecordSetWithResponse(ctx context.Context, zone string, name string, pType RecordType, params *ModifyRecordSetParams, body ModifyRecordSetJSONRequestBody, reqEditors ...RequestEditorFn) (*ModifyRecordSetResponse, error)

	// SetRecordSetWithBodyWithResponse Replace a record set
	//
	// **This replaces the entire record set.** After this request, the only records under this
	// name and type are the ones listed in `values`; any value present beforehand and absent here
	// is removed.
	//
	// The record set is created if it does not exist, so this operation both creates and replaces,
	// and is idempotent.
	//
	// Two concurrent requests against the same domain conflict; the second receives `ZONE_BUSY`
	// and may be retried.
	//
	// **Use `PATCH` to add or remove individual values.** Reading the set and submitting a
	// modified list is a read-modify-write: values added by anything else between those two steps
	// are stated to be absent, and are therefore removed, with both requests returning 200.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with PUT /api/v1/zones/{zone}/records/{name}/{type} (the `SetRecordSet` operationId).
	SetRecordSetWithBodyWithResponse(ctx context.Context, zone string, name string, pType RecordType, params *SetRecordSetParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*SetRecordSetResponse, error)

	// SetRecordSetWithResponse Replace a record set
	//
	// **This replaces the entire record set.** After this request, the only records under this
	// name and type are the ones listed in `values`; any value present beforehand and absent here
	// is removed.
	//
	// The record set is created if it does not exist, so this operation both creates and replaces,
	// and is idempotent.
	//
	// Two concurrent requests against the same domain conflict; the second receives `ZONE_BUSY`
	// and may be retried.
	//
	// **Use `PATCH` to add or remove individual values.** Reading the set and submitting a
	// modified list is a read-modify-write: values added by anything else between those two steps
	// are stated to be absent, and are therefore removed, with both requests returning 200.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with PUT /api/v1/zones/{zone}/records/{name}/{type} (the `SetRecordSet` operationId).
	SetRecordSetWithResponse(ctx context.Context, zone string, name string, pType RecordType, params *SetRecordSetParams, body SetRecordSetJSONRequestBody, reqEditors ...RequestEditorFn) (*SetRecordSetResponse, error)
}

type ListCredentialsResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *LengthAwarePageCredentialResource
	// JSONDefault the response for an HTTP default `application/json` response
	JSONDefault *Error
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r ListCredentialsResponse) GetJSON200() *LengthAwarePageCredentialResource {
	return r.JSON200
}

// GetJSONDefault returns the response for an HTTP default `application/json` response
func (r ListCredentialsResponse) GetJSONDefault() *Error {
	return r.JSONDefault
}

// GetBody returns the raw response body bytes
func (r ListCredentialsResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ListCredentialsResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ListCredentialsResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ListCredentialsResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type CreateCredentialResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON201 the response for an HTTP 201 `application/json` response
	JSON201 *CredentialResource
	// JSONDefault the response for an HTTP default `application/json` response
	JSONDefault *Error
}

// GetJSON201 returns the response for an HTTP 201 `application/json` response
func (r CreateCredentialResponse) GetJSON201() *CredentialResource {
	return r.JSON201
}

// GetJSONDefault returns the response for an HTTP default `application/json` response
func (r CreateCredentialResponse) GetJSONDefault() *Error {
	return r.JSONDefault
}

// GetBody returns the raw response body bytes
func (r CreateCredentialResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r CreateCredentialResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r CreateCredentialResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r CreateCredentialResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type DeleteCredentialResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSONDefault the response for an HTTP default `application/json` response
	JSONDefault *Error
}

// GetJSONDefault returns the response for an HTTP default `application/json` response
func (r DeleteCredentialResponse) GetJSONDefault() *Error {
	return r.JSONDefault
}

// GetBody returns the raw response body bytes
func (r DeleteCredentialResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r DeleteCredentialResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r DeleteCredentialResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r DeleteCredentialResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type GetCredentialResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *CredentialResource
	// JSONDefault the response for an HTTP default `application/json` response
	JSONDefault *Error
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r GetCredentialResponse) GetJSON200() *CredentialResource {
	return r.JSON200
}

// GetJSONDefault returns the response for an HTTP default `application/json` response
func (r GetCredentialResponse) GetJSONDefault() *Error {
	return r.JSONDefault
}

// GetBody returns the raw response body bytes
func (r GetCredentialResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetCredentialResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetCredentialResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetCredentialResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type RenameCredentialResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *CredentialResource
	// JSONDefault the response for an HTTP default `application/json` response
	JSONDefault *Error
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r RenameCredentialResponse) GetJSON200() *CredentialResource {
	return r.JSON200
}

// GetJSONDefault returns the response for an HTTP default `application/json` response
func (r RenameCredentialResponse) GetJSONDefault() *Error {
	return r.JSONDefault
}

// GetBody returns the raw response body bytes
func (r RenameCredentialResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r RenameCredentialResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r RenameCredentialResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r RenameCredentialResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type VerifyCredentialResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *CredentialResource
	// JSONDefault the response for an HTTP default `application/json` response
	JSONDefault *Error
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r VerifyCredentialResponse) GetJSON200() *CredentialResource {
	return r.JSON200
}

// GetJSONDefault returns the response for an HTTP default `application/json` response
func (r VerifyCredentialResponse) GetJSONDefault() *Error {
	return r.JSONDefault
}

// GetBody returns the raw response body bytes
func (r VerifyCredentialResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r VerifyCredentialResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r VerifyCredentialResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r VerifyCredentialResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type ListZonesResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *ZoneListResponseBody
	// JSONDefault the response for an HTTP default `application/json` response
	JSONDefault *Error
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r ListZonesResponse) GetJSON200() *ZoneListResponseBody {
	return r.JSON200
}

// GetJSONDefault returns the response for an HTTP default `application/json` response
func (r ListZonesResponse) GetJSONDefault() *Error {
	return r.JSONDefault
}

// GetBody returns the raw response body bytes
func (r ListZonesResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ListZonesResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ListZonesResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ListZonesResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type ListRecordsResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *LengthAwarePageRecordSetResource
	// JSONDefault the response for an HTTP default `application/json` response
	JSONDefault *Error
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r ListRecordsResponse) GetJSON200() *LengthAwarePageRecordSetResource {
	return r.JSON200
}

// GetJSONDefault returns the response for an HTTP default `application/json` response
func (r ListRecordsResponse) GetJSONDefault() *Error {
	return r.JSONDefault
}

// GetBody returns the raw response body bytes
func (r ListRecordsResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ListRecordsResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ListRecordsResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ListRecordsResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type DeleteRecordSetResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSONDefault the response for an HTTP default `application/json` response
	JSONDefault *Error
}

// GetJSONDefault returns the response for an HTTP default `application/json` response
func (r DeleteRecordSetResponse) GetJSONDefault() *Error {
	return r.JSONDefault
}

// GetBody returns the raw response body bytes
func (r DeleteRecordSetResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r DeleteRecordSetResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r DeleteRecordSetResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r DeleteRecordSetResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type GetRecordSetResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *RecordSetResource
	// JSONDefault the response for an HTTP default `application/json` response
	JSONDefault *Error
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r GetRecordSetResponse) GetJSON200() *RecordSetResource {
	return r.JSON200
}

// GetJSONDefault returns the response for an HTTP default `application/json` response
func (r GetRecordSetResponse) GetJSONDefault() *Error {
	return r.JSONDefault
}

// GetBody returns the raw response body bytes
func (r GetRecordSetResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetRecordSetResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetRecordSetResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetRecordSetResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type ModifyRecordSetResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *RecordSetResource
	// JSONDefault the response for an HTTP default `application/json` response
	JSONDefault *Error
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r ModifyRecordSetResponse) GetJSON200() *RecordSetResource {
	return r.JSON200
}

// GetJSONDefault returns the response for an HTTP default `application/json` response
func (r ModifyRecordSetResponse) GetJSONDefault() *Error {
	return r.JSONDefault
}

// GetBody returns the raw response body bytes
func (r ModifyRecordSetResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ModifyRecordSetResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ModifyRecordSetResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ModifyRecordSetResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type SetRecordSetResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *RecordSetResource
	// JSONDefault the response for an HTTP default `application/json` response
	JSONDefault *Error
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r SetRecordSetResponse) GetJSON200() *RecordSetResource {
	return r.JSON200
}

// GetJSONDefault returns the response for an HTTP default `application/json` response
func (r SetRecordSetResponse) GetJSONDefault() *Error {
	return r.JSONDefault
}

// GetBody returns the raw response body bytes
func (r SetRecordSetResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r SetRecordSetResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r SetRecordSetResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r SetRecordSetResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

// ListCredentialsWithResponse List provider credentials
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /api/v1/credentials (the `ListCredentials` operationId).
func (c *ClientWithResponses) ListCredentialsWithResponse(ctx context.Context, params *ListCredentialsParams, reqEditors ...RequestEditorFn) (*ListCredentialsResponse, error) {
	rsp, err := c.ListCredentials(ctx, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseListCredentialsResponse(rsp)
}

// CreateCredentialWithBodyWithResponse Register a provider credential
//
// The credential is validated against the provider before it is accepted. A credential that
// cannot list domains is rejected with `CREDENTIAL_REJECTED_UPSTREAM` and is not stored.
//
// `provider` selects which configuration block is required: `provider: cloudflare` requires
// the `cloudflare` block, and the remaining blocks are ignored. Omitting the matching block
// returns `CREDENTIAL_CONFIG_MISSING`.
//
// Registering a credential that is already held by this project returns
// `CREDENTIAL_DUPLICATE`. Provider rate limits apply per credential, so a second copy grants
// no additional capacity.
//
// The submitted credential material is not returned by this or any other operation.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /api/v1/credentials (the `CreateCredential` operationId).
func (c *ClientWithResponses) CreateCredentialWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*CreateCredentialResponse, error) {
	rsp, err := c.CreateCredentialWithBody(ctx, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseCreateCredentialResponse(rsp)
}

// CreateCredentialWithResponse Register a provider credential
//
// The credential is validated against the provider before it is accepted. A credential that
// cannot list domains is rejected with `CREDENTIAL_REJECTED_UPSTREAM` and is not stored.
//
// `provider` selects which configuration block is required: `provider: cloudflare` requires
// the `cloudflare` block, and the remaining blocks are ignored. Omitting the matching block
// returns `CREDENTIAL_CONFIG_MISSING`.
//
// Registering a credential that is already held by this project returns
// `CREDENTIAL_DUPLICATE`. Provider rate limits apply per credential, so a second copy grants
// no additional capacity.
//
// The submitted credential material is not returned by this or any other operation.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /api/v1/credentials (the `CreateCredential` operationId).
func (c *ClientWithResponses) CreateCredentialWithResponse(ctx context.Context, body CreateCredentialJSONRequestBody, reqEditors ...RequestEditorFn) (*CreateCredentialResponse, error) {
	rsp, err := c.CreateCredential(ctx, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseCreateCredentialResponse(rsp)
}

// DeleteCredentialWithResponse Delete a provider credential
//
// Domains reachable only through this credential stop appearing in `GET /zones` and can no
// longer be read or written through this API.
//
// **No DNS record is modified.** Deleting a credential withdraws this project's access to the
// provider account; it does not remove anything that was published there.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with DELETE /api/v1/credentials/{credentialId} (the `DeleteCredential` operationId).
func (c *ClientWithResponses) DeleteCredentialWithResponse(ctx context.Context, credentialId openapi_types.UUID, reqEditors ...RequestEditorFn) (*DeleteCredentialResponse, error) {
	rsp, err := c.DeleteCredential(ctx, credentialId, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseDeleteCredentialResponse(rsp)
}

// GetCredentialWithResponse Get a provider credential
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /api/v1/credentials/{credentialId} (the `GetCredential` operationId).
func (c *ClientWithResponses) GetCredentialWithResponse(ctx context.Context, credentialId openapi_types.UUID, reqEditors ...RequestEditorFn) (*GetCredentialResponse, error) {
	rsp, err := c.GetCredential(ctx, credentialId, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetCredentialResponse(rsp)
}

// RenameCredentialWithBodyWithResponse Rename a provider credential
//
// Only the display name can be changed. To use different credential material, delete this
// credential and register a new one: a credential's identity does not change over its
// lifetime.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with PATCH /api/v1/credentials/{credentialId} (the `RenameCredential` operationId).
func (c *ClientWithResponses) RenameCredentialWithBodyWithResponse(ctx context.Context, credentialId openapi_types.UUID, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*RenameCredentialResponse, error) {
	rsp, err := c.RenameCredentialWithBody(ctx, credentialId, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseRenameCredentialResponse(rsp)
}

// RenameCredentialWithResponse Rename a provider credential
//
// Only the display name can be changed. To use different credential material, delete this
// credential and register a new one: a credential's identity does not change over its
// lifetime.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with PATCH /api/v1/credentials/{credentialId} (the `RenameCredential` operationId).
func (c *ClientWithResponses) RenameCredentialWithResponse(ctx context.Context, credentialId openapi_types.UUID, body RenameCredentialJSONRequestBody, reqEditors ...RequestEditorFn) (*RenameCredentialResponse, error) {
	rsp, err := c.RenameCredential(ctx, credentialId, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseRenameCredentialResponse(rsp)
}

// VerifyCredentialWithResponse Re-validate a provider credential
//
// Checks the credential against the provider and records the outcome in `last_verified_at`
// and `last_verify_error`.
//
// A credential is validated when it is registered. This operation exists for credentials whose
// permissions or keys were subsequently changed at the provider, which is a change this API is
// not notified of.
//
// **A failed check returns 200, not an error.** The outcome is reported in
// `last_verify_error`: the request itself succeeded, and the answer it obtained is that the
// credential is no longer usable.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /api/v1/credentials/{credentialId}/verification (the `VerifyCredential` operationId).
func (c *ClientWithResponses) VerifyCredentialWithResponse(ctx context.Context, credentialId openapi_types.UUID, reqEditors ...RequestEditorFn) (*VerifyCredentialResponse, error) {
	rsp, err := c.VerifyCredential(ctx, credentialId, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseVerifyCredentialResponse(rsp)
}

// ListZonesWithResponse List domains
//
// Lists every domain reachable through the credentials registered by this project.
//
// Listings may briefly lag behind domains added or removed at the provider. Set `refresh` for
// an up-to-date answer.
//
// ## A domain that appears more than once
//
// While a domain is being migrated it commonly exists in both provider accounts at once. Such
// a domain is returned once per credential, with the same `zone` and different
// `credential_id`.
//
// `authoritative` reports which entry is the one currently serving queries, determined from
// the domain's delegation. `null` means no determination was made: either the domain appears
// only once, or its delegation does not identify any of the registered providers — for
// example when custom nameservers are in use, or the domain is not delegated at all.
//
// **Write operations are refused while the determination is `null` and the domain appears more
// than once**, with `ZONE_AMBIGUOUS`. Supply `credential_id` on the write request to select a
// credential explicitly.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /api/v1/zones (the `ListZones` operationId).
func (c *ClientWithResponses) ListZonesWithResponse(ctx context.Context, params *ListZonesParams, reqEditors ...RequestEditorFn) (*ListZonesResponse, error) {
	rsp, err := c.ListZones(ctx, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseListZonesResponse(rsp)
}

// ListRecordsWithResponse List record sets
//
// Returns the domain's records grouped into record sets: all records sharing a name and a type
// are reported as one entry, with their values in `values`.
//
// `limit` and `offset` reduce the size of the response but not the time taken to produce it.
// For domains with many records, filter by `name` or `type`.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /api/v1/zones/{zone}/records (the `ListRecords` operationId).
func (c *ClientWithResponses) ListRecordsWithResponse(ctx context.Context, zone string, params *ListRecordsParams, reqEditors ...RequestEditorFn) (*ListRecordsResponse, error) {
	rsp, err := c.ListRecords(ctx, zone, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseListRecordsResponse(rsp)
}

// DeleteRecordSetWithResponse Delete a record set
//
// Removes every record under this name and type. **To remove part of a set, use `PATCH`**,
// which is safe to issue concurrently; this operation and `PUT` are not.
//
// Providers reject removal of the last NS record set at the apex of a domain, which would
// withdraw the domain from DNS entirely.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with DELETE /api/v1/zones/{zone}/records/{name}/{type} (the `DeleteRecordSet` operationId).
func (c *ClientWithResponses) DeleteRecordSetWithResponse(ctx context.Context, zone string, name string, pType RecordType, params *DeleteRecordSetParams, reqEditors ...RequestEditorFn) (*DeleteRecordSetResponse, error) {
	rsp, err := c.DeleteRecordSet(ctx, zone, name, pType, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseDeleteRecordSetResponse(rsp)
}

// GetRecordSetWithResponse Get a record set
//
// Returns `RECORD_SET_NOT_FOUND` when no record of this type exists under this name.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /api/v1/zones/{zone}/records/{name}/{type} (the `GetRecordSet` operationId).
func (c *ClientWithResponses) GetRecordSetWithResponse(ctx context.Context, zone string, name string, pType RecordType, reqEditors ...RequestEditorFn) (*GetRecordSetResponse, error) {
	rsp, err := c.GetRecordSet(ctx, zone, name, pType, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetRecordSetResponse(rsp)
}

// ModifyRecordSetWithBodyWithResponse Add or remove values in a record set
//
// **Names values to add and remove; anything not named is left in place.** Nothing is read
// first, so this is the only operation on a record set safe to issue concurrently. The set is
// created if it does not exist.
//
// Certificate issuance must use this operation for both the challenge record and its cleanup.
// `PUT` and `DELETE` state or remove the whole set, so a concurrent issuance's challenge
// record is discarded — with every request returning 2xx, and the failure surfacing as the
// second certificate failing validation.
//
// `add` is applied before `remove`, so a request carrying both changes a value without the
// name ever resolving without it: the set briefly holds one value too many rather than one too
// few. The reverse order, as two requests, leaves a window in which the value is absent — for
// a single-valued set, the name does not resolve at all — and an addition that then fails
// leaves it gone.
//
// A value appearing in both `add` and `remove` is rejected rather than resolved in one
// direction.
//
// Values in `remove` that are absent are skipped, so a retried cleanup reaches the same
// result. Values in `add` that are already present are rejected by the provider: a record set
// cannot hold the same value twice.
//
// Removing every value leaves an empty set, reported as `values: []`, not `RECORD_SET_NOT_FOUND`.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with PATCH /api/v1/zones/{zone}/records/{name}/{type} (the `ModifyRecordSet` operationId).
func (c *ClientWithResponses) ModifyRecordSetWithBodyWithResponse(ctx context.Context, zone string, name string, pType RecordType, params *ModifyRecordSetParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*ModifyRecordSetResponse, error) {
	rsp, err := c.ModifyRecordSetWithBody(ctx, zone, name, pType, params, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseModifyRecordSetResponse(rsp)
}

// ModifyRecordSetWithResponse Add or remove values in a record set
//
// **Names values to add and remove; anything not named is left in place.** Nothing is read
// first, so this is the only operation on a record set safe to issue concurrently. The set is
// created if it does not exist.
//
// Certificate issuance must use this operation for both the challenge record and its cleanup.
// `PUT` and `DELETE` state or remove the whole set, so a concurrent issuance's challenge
// record is discarded — with every request returning 2xx, and the failure surfacing as the
// second certificate failing validation.
//
// `add` is applied before `remove`, so a request carrying both changes a value without the
// name ever resolving without it: the set briefly holds one value too many rather than one too
// few. The reverse order, as two requests, leaves a window in which the value is absent — for
// a single-valued set, the name does not resolve at all — and an addition that then fails
// leaves it gone.
//
// A value appearing in both `add` and `remove` is rejected rather than resolved in one
// direction.
//
// Values in `remove` that are absent are skipped, so a retried cleanup reaches the same
// result. Values in `add` that are already present are rejected by the provider: a record set
// cannot hold the same value twice.
//
// Removing every value leaves an empty set, reported as `values: []`, not `RECORD_SET_NOT_FOUND`.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with PATCH /api/v1/zones/{zone}/records/{name}/{type} (the `ModifyRecordSet` operationId).
func (c *ClientWithResponses) ModifyRecordSetWithResponse(ctx context.Context, zone string, name string, pType RecordType, params *ModifyRecordSetParams, body ModifyRecordSetJSONRequestBody, reqEditors ...RequestEditorFn) (*ModifyRecordSetResponse, error) {
	rsp, err := c.ModifyRecordSet(ctx, zone, name, pType, params, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseModifyRecordSetResponse(rsp)
}

// SetRecordSetWithBodyWithResponse Replace a record set
//
// **This replaces the entire record set.** After this request, the only records under this
// name and type are the ones listed in `values`; any value present beforehand and absent here
// is removed.
//
// The record set is created if it does not exist, so this operation both creates and replaces,
// and is idempotent.
//
// Two concurrent requests against the same domain conflict; the second receives `ZONE_BUSY`
// and may be retried.
//
// **Use `PATCH` to add or remove individual values.** Reading the set and submitting a
// modified list is a read-modify-write: values added by anything else between those two steps
// are stated to be absent, and are therefore removed, with both requests returning 200.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with PUT /api/v1/zones/{zone}/records/{name}/{type} (the `SetRecordSet` operationId).
func (c *ClientWithResponses) SetRecordSetWithBodyWithResponse(ctx context.Context, zone string, name string, pType RecordType, params *SetRecordSetParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*SetRecordSetResponse, error) {
	rsp, err := c.SetRecordSetWithBody(ctx, zone, name, pType, params, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseSetRecordSetResponse(rsp)
}

// SetRecordSetWithResponse Replace a record set
//
// **This replaces the entire record set.** After this request, the only records under this
// name and type are the ones listed in `values`; any value present beforehand and absent here
// is removed.
//
// The record set is created if it does not exist, so this operation both creates and replaces,
// and is idempotent.
//
// Two concurrent requests against the same domain conflict; the second receives `ZONE_BUSY`
// and may be retried.
//
// **Use `PATCH` to add or remove individual values.** Reading the set and submitting a
// modified list is a read-modify-write: values added by anything else between those two steps
// are stated to be absent, and are therefore removed, with both requests returning 200.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with PUT /api/v1/zones/{zone}/records/{name}/{type} (the `SetRecordSet` operationId).
func (c *ClientWithResponses) SetRecordSetWithResponse(ctx context.Context, zone string, name string, pType RecordType, params *SetRecordSetParams, body SetRecordSetJSONRequestBody, reqEditors ...RequestEditorFn) (*SetRecordSetResponse, error) {
	rsp, err := c.SetRecordSet(ctx, zone, name, pType, params, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseSetRecordSetResponse(rsp)
}

// ParseListCredentialsResponse parses an HTTP response from a ListCredentialsWithResponse call
func ParseListCredentialsResponse(rsp *http.Response) (*ListCredentialsResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ListCredentialsResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest LengthAwarePageCredentialResource
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && true:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSONDefault = &dest

	}

	return response, nil
}

// ParseCreateCredentialResponse parses an HTTP response from a CreateCredentialWithResponse call
func ParseCreateCredentialResponse(rsp *http.Response) (*CreateCredentialResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &CreateCredentialResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 201:
		var dest CredentialResource
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON201 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && true:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSONDefault = &dest

	}

	return response, nil
}

// ParseDeleteCredentialResponse parses an HTTP response from a DeleteCredentialWithResponse call
func ParseDeleteCredentialResponse(rsp *http.Response) (*DeleteCredentialResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &DeleteCredentialResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case rsp.StatusCode == 204:
		break // No content-type

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && true:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSONDefault = &dest

	}

	return response, nil
}

// ParseGetCredentialResponse parses an HTTP response from a GetCredentialWithResponse call
func ParseGetCredentialResponse(rsp *http.Response) (*GetCredentialResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetCredentialResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest CredentialResource
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && true:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSONDefault = &dest

	}

	return response, nil
}

// ParseRenameCredentialResponse parses an HTTP response from a RenameCredentialWithResponse call
func ParseRenameCredentialResponse(rsp *http.Response) (*RenameCredentialResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &RenameCredentialResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest CredentialResource
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && true:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSONDefault = &dest

	}

	return response, nil
}

// ParseVerifyCredentialResponse parses an HTTP response from a VerifyCredentialWithResponse call
func ParseVerifyCredentialResponse(rsp *http.Response) (*VerifyCredentialResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &VerifyCredentialResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest CredentialResource
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && true:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSONDefault = &dest

	}

	return response, nil
}

// ParseListZonesResponse parses an HTTP response from a ListZonesWithResponse call
func ParseListZonesResponse(rsp *http.Response) (*ListZonesResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ListZonesResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest ZoneListResponseBody
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && true:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSONDefault = &dest

	}

	return response, nil
}

// ParseListRecordsResponse parses an HTTP response from a ListRecordsWithResponse call
func ParseListRecordsResponse(rsp *http.Response) (*ListRecordsResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ListRecordsResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest LengthAwarePageRecordSetResource
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && true:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSONDefault = &dest

	}

	return response, nil
}

// ParseDeleteRecordSetResponse parses an HTTP response from a DeleteRecordSetWithResponse call
func ParseDeleteRecordSetResponse(rsp *http.Response) (*DeleteRecordSetResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &DeleteRecordSetResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case rsp.StatusCode == 204:
		break // No content-type

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && true:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSONDefault = &dest

	}

	return response, nil
}

// ParseGetRecordSetResponse parses an HTTP response from a GetRecordSetWithResponse call
func ParseGetRecordSetResponse(rsp *http.Response) (*GetRecordSetResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetRecordSetResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest RecordSetResource
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && true:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSONDefault = &dest

	}

	return response, nil
}

// ParseModifyRecordSetResponse parses an HTTP response from a ModifyRecordSetWithResponse call
func ParseModifyRecordSetResponse(rsp *http.Response) (*ModifyRecordSetResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ModifyRecordSetResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest RecordSetResource
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && true:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSONDefault = &dest

	}

	return response, nil
}

// ParseSetRecordSetResponse parses an HTTP response from a SetRecordSetWithResponse call
func ParseSetRecordSetResponse(rsp *http.Response) (*SetRecordSetResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &SetRecordSetResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest RecordSetResource
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && true:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSONDefault = &dest

	}

	return response, nil
}
