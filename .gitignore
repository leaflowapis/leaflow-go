leaflowapis/

# Python 的字节码缓存。
#
# 它一度被签了进来（五个 .pyc，其中三个还指着早就删掉的脚本）。清掉不只是为了干净：这条
# 流水线靠 `git diff --quiet` 断言「生成物和契约对得上」，而被跟踪的 .pyc 会在 CI 换一版
# Python 之后跟着变，报出来的却是「契约改了但生成物没跟着更新」——那句话会把人引向契约仓库，
# 而那边什么都没改。
__pycache__/
